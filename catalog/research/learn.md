# 学习笔记（纸交易循环）

来源：`data/btchour.sqlite` 扫描账本 + Kalshi 回放。命令：`python3 -m btchour learn`。

## 2026-08-25 20:22 → 2026-08-26 00:07 UTC

循环跑了约 4 小时，**437+ 次扫描，0 笔做T成交，已实现盈亏 0**。

三张 $0.83 `lock_wait` 全部 `wait_invalid`（卖一从未打穿）：

- 日盘 NO `T81249.99` / `T80999.99`（砸盘前）
- 砸盘后小时盘 YES `T77499.99`（现货已在 78200，合约已被决定，但卖一仍是 $0.99）

### 砸盘小时 `KXBTCD-26AUG2517`

- 扫描现货：78912 → 78190（约 −$720）
- 扫描带算出的 3 分钟动量峰值：**−$303**（20:49:25，现货 78206）
- 同一窗口扫描结果仍是 `opps=0`，只有后来的 `lock_wait`
- 分钟 K 线回放这小时也是 **0 笔 impulse_t**

结论：不是没看见动量。是动量出现时，ATM 的 NO 卖一已经高于 $0.52，规则拒单。砸盘里盘口比我们的便宜门槛更快。

### 循环空窗

扫描间隔 >30s 的空档里，最长一次：

`21:01:19` `AUG2518` → `00:06:52` `AUG2521`，**11133 秒（约 3 小时）**

这 3 小时的回放本来有：

| 小时 | 回放 |
| --- | --- |
| `AUG2518` | impulse NO @ 0.46 → `t_stop` −22% |
| `AUG2519` | impulse YES @ 0.49 → `t_clip` +17% |
| `AUG2520` | 0 笔 |

空窗原因：每个 3 秒周期都全量拉系列 / 未开盘 / 15m，并写近 300KB 的 `latest.json`。之后改为 45 秒一次全量，中间只刷新小时盘，循环报错也不再死掉。

### 规则没有改成“放宽就为了成交”

把 `impulse_max_ask` 抬到 $0.70 能在砸盘里吃到票，但 16 小时回放里贵的动量单正是大亏来源。继续拒贵的 T，把拒单原因写进 `journal`，下一小时对着日记改，而不是猜。

## 2026-08-25 手动盘（账户成交）

人的好单不是 20% 锁仓，是砸盘方向的便宜 NO：挂 0.20–0.25，2–16 分钟走，**10%–50% 都正常**。后几笔亏在人累了选错方向：翻 YES、换行权价、追 0.60+。

机器改成：10% 是下限、50% 是上限，中间动量还在就拿着；不翻面；亏了下一小时只空反方向。详见 [`manual.md`](manual.md)。

## 反复测验（2026-08-26 01:13 → 01:17 UTC）

`python3 -m btchour sweep --hours 16`：每个小时的带子只拉一次，flex / swing / lock 和 skip 开/关在同一段 K 线上对照。

整小时空仓把 `AUG2510` 同向 +19% 也跳掉了。24h flex 在 `AUG2423` 锁完 20% 又开了一笔 50¢ 动量，止损 −43%。

改完之后：锁仓走完这小时不再开 T；亏了只空反方向。默认 flex 16h **7 / 4 / +0.12**，24h **11 / 6 / +2.18**。没有放宽 $0.52 买价上限。空 candidates 的 journal 不再写 `None ask=None`。

## 反复测验（2026-08-26 01:20 这一轮）

`AUG2520` 砸盘的 0.20–0.27 NO **写在分钟收盘上**。机器 0 笔是因为模型 p 只有 0.31–0.38，过不了 `impulse_min_p=0.52`。

把 p 降到 30%、买价上限 0.35：16h **15 / 6 / −5.60**。`AUG2520` 0.27 的 NO 下一分钟 `t_stop` −57%。人手拿到的 0.37–0.51 在 1 分钟止损里活不下来。

进场时 p 已经低于 40% 的单，不再用 40% 失效线去砍（那是给高 p 进场准备的）。默认门槛不改。sweep 以后每次都带 `flex_cheap` 对照。

## 全部推进（2026-08-26）：砸盘挂 25¢，不吃卖一

人手好单是 **maker NO 0.20–0.25**，不是 ask 0.22 的 taker。分钟收盘已经有 0.19–0.32，但模型 p 只有 0.31–0.38；吃卖一会被立刻按买一计价，再被 −12% 止损在反弹里杀掉。

对照结果（同一段缓存带子）：

- 对称挂 YES：16h 看起来更好，24h 被三次 YES −50% 拖到 +1.56，低于 ATM 基准 +2.18。
- 只挂砸盘 NO，但亏后仍允许同向 wait：`AUG2518` NO 止损后，`AUG2519` 空掉 +17% YES，改挂 wait −70%。16h **−0.46**。
- 亏后下一小时空 wait、同向 taker 仍可做：默认 flex 16h **9 / 6 / +1.29**，24h **13 / 9 / +5.03**。`flex_nowait` 仍是 +0.12 / +2.18。便宜 taker 对照仍亏。

`AUG2507` wait 仍有一笔 −51%。空仓在 `AUG2519` 是对的。没有把默认改回“为了成交去吃卖一”。

## 不达成就继续（2026-08-26 ~03:27 UTC）

纸循环到这一轮仍然 **0 笔完成成交**。回放里 `AUG2520` 曾经能挂上 25¢，但 3 分钟印一淡就把单撤了；人手是 23:11 成交、扛过 23:14 反弹、23:28 在 0.51 走。

改完之后：

1. **淡了不撤**。只在对面出现 ≥+$100 涨势时撤。
2. **反弹里不填**。YES 拉到 0.86 时 25¢ 的 NO 买单在微观上会被打到，但那是最差成交：立刻标到 13¢，旧 −50% 止损下一分钟就砍。人手是砸盘里成交。
3. **卖一打到 rest 算成交**（含刚好 25¢）。`AUG2520` 23:20 第二波砸盘 `no_low=0.25`。
4. **wait 止损 80%**。50% 扛不住反弹标记；99% 会让 `AUG2507` 拿到结算 −100%。

同一段旧带子（AUG2506–AUG2521 / AUG2422–AUG2521）：

| 规则 | 16h | 24h |
| --- | ---: | ---: |
| 上一轮 dump wait | +1.29 | +5.03 |
| 只淡了不撤（反弹里成交 + 50% 止损） | +1.85 | +3.48 |
| 砸盘里成交 + 80% 止损（现在默认） | **+7.98** | **+13.46** |

`AUG2520` 现在是 `T78699` NO @ 0.25，23:20 进、23:21 `t_clip` **+41.5%**。人手是同一张合约 23:11 进、23:28 在 0.51 走。机器更晚进、更早按 10%–50% 带兑现，不再吃整段。

这仍是 **1 分钟 K 线回放**，不是纸盘成交。`AUG2507` wait 还在 −81%。便宜 taker 对照 24h 仍亏。没有把 p 降到 30%。

## 自动 loop（2026-08-26 03:28 → 05:01 UTC）

新代码上线后，纸盘 **第一次挂上** dump wait：

`03:32:57Z` `AUG2600` `T78899` NO rest 0.25，动量 −$101，卖一 0.45，日记 `wait`。约 3 分钟后动量翻到 +$101，按规矩撤单（`wait_invalid`）。这不是错撤。

随后循环从 `03:42:06` 卡到 `05:01:12`，**空窗 4746 秒**，整段 `AUG2600` 尾盘和 `AUG2601` 没扫。urllib 的 15s timeout 没能打断。完成成交仍是 0。

所以 `python3 -m btchour loop` 是监督循环：子进程跑 `run`，扫描超过 60s 没心跳就杀了重启；每一圈还有 25s 硬超时。不要再让卡死把砸盘错过。

## 第一笔纸盘 wait（2026-08-26 05:06 UTC）——挂错行权价

循环活着之后，纸盘第一次成交了 dump wait，**不是 10%–50% clip**。

| 单 | 结果 |
| --- | --- |
| `AUG2602` `T78499` NO rest 0.25，卖一 0.29，现货 78689，p 33.3% | 成交；反弹标到 0.12 再 0.03；`t_wait_stop` **−88.8%**，已实现 **−2.2204** |
| 同一扫描 `T78599` rest 0.25，卖一 0.42，离现货更近 | 当时也挂了（后来改成一小时只挂一张）；按 `(ask−rest)` 排序，便宜卖一赢了 |
| `AUG2600` `T78899` 03:32 挂 0.25 | 动量翻到 +$101 按规矩撤，不是错撤 |

80% 止损做了它该做的事。错在进场选了**卖一刚好在 25¢ 上面最便宜的那张**，不是人手那种 dump ATM。`T78499` 比现货低约 $190，反弹把 NO 砸穿。05:20 第二波砸盘日记里 `T78599` / `T78699` 卖一 0.40–0.42，但当时已经持有 T78499，不能再挂。后来涨势 YES 仍 `blocked`（p<52%），没有翻面。

只换行权价排序不够。`AUG2520` 23:09 离现货最近的是 `T78799` ask 0.44，25¢ 挂上去成交不了；人手和金带子是 `T78699` 在卖一还在 32–35¢ 的时候。纸盘 `T78499` ask 0.29 是已经砸穿的刀。

**换策略（默认 dump coupon）：**

- 只挂 NO ask **32–42¢**、离现货 ≤$150 的那张
- 8 分钟还没到 +10% 就 `t_scratch`，不再拿 80% 扛死单
- 旧的 26–48¢ / $600 / 不 scratch 宽 wait 只留在 `flex_wait_loose` 对照
- 不把 p 降到 30%。不在涨势挂 YES。亏后 `AUG2603` 空 wait

完成成交：**1 / 0 / −2.2204**。回放绿不是达成。不能实现就换策略，不再修同一把飞刀。

换完之后同一段缓存（AUG2601–AUG2502）：dump coupon 16h **10 / 7 / +1.03**（含 1 笔 scratch），nowait +0.01，旧宽 wait +5.51。`AUG2520` 新默认仍是 +41.5% clip。宽 wait 回放更绿是因为它还在吃 29¢ 的刀；纸盘已经证伪。默认留 coupon，只要求它继续赢 nowait。便宜 taker 仍亏。

## 06:00 换小时扫错场（2026-08-26）

`AUG2602` 收盘后 Kalshi 仍把它标成 open（TWAP）。焦点按 `|close−now|` 排，刚关的小时会赢大约 30 分钟，循环在 `AUG2602` / `AUG2603` 之间来回跳。dump coupon 看不到新小时前半段。改成：**还没到 close 的小时优先**。

收盘回放 `AUG2602`：dump coupon **0 笔**（29¢ 刀被 32–42¢ 门挡掉）。便宜 taker 吃了 0.31 NO，`t_stop` −42%。纸盘那笔 −2.22 是旧宽 wait。coupon 05:52 才上线，这小时没有新的纸盘成交。`AUG2603` 因亏后规则空 wait。新窗口 16h coupon **9 / 6 / +0.07**，nowait −0.95，仍留 coupon。

## 07:00 收盘 `AUG2603`：该空 wait，但 skip 卡死了

`AUG2603` 是亏后空 wait 的那一小时。循环扫了 **1022** 次，现货动量 −$155 / +$133。日记 **58** 次 coupon 级 wait：

- 06:12–06:14 `T78899` NO ask **0.37–0.39**
- 06:20 `T78899` ask **0.40**
- 06:25 `T78799` ask **0.39**

同向 taker 没过门（p<52% / gap）。涨势 YES 仍 `blocked`。纸盘 **0 笔成交**。这是规矩，不是漏单。

1 分钟回放 coupon / nowait / loose 这小时也都是 **0 笔**。分钟收盘只剩两根砸盘 K：`T78899` 已经 31¢ / 35¢。便宜 taker 对照吃了 0.19 NO，`t_stop` −39%。

新窗口（AUG2603–AUG2512 / AUG2603–AUG2504）：

| Run | 16h | 24h |
| --- | ---: | ---: |
| dump coupon（默认） | 8 / 5 / **−1.19** | 14 / 9 / **+0.94** |
| flex_nowait | 3 / 1 / −2.21 | 8 / 4 / −0.53 |
| flex_wait_loose | 8 / 6 / +3.29 | 16 / 12 / +6.22 |
| flex cheap p30/ask35 | 14 / 5 / −5.55 | 22 / 8 / −7.95 |

16h 从 +0.07 落到 −1.19，是窗口滚掉 `AUG2511` YES clip +26%，不是 coupon 这小时亏了。coupon 仍赢 nowait。`AUG2520` 仍是 +41.5%。便宜 taker 仍红。不换策略。

真正挡住纸盘的是另一件事：账本每次扫描只从成交重建 `SessionMemory`，`skipped_event` 丢了。亏后「空下一小时 wait」变成**之后每一小时都空 wait**。`AUG2604` 本来该是第一小时真挂 coupon，却会被当成第二个 skip 小时。已改成：skip 小时 = `last_loss_event` 的下一张小时 ticker。`AUG2604` 现在 `skip_next=False`。

完成成交仍是 **1 / 0 / −2.2204**。回放绿不是达成。`AUG2604` 才是 coupon 的第一小时实盘。

## 08:00 收盘 `AUG2604`：挂对了，成交在淡里

第一小时真挂 coupon：

| 时间 | 事件 |
| --- | --- |
| 07:10:33Z | `T78899` NO rest **0.25** under ask **0.37**，动量 −$115，现货 78945。一张，没翻 YES，没换行权价 |
| 07:10–07:41 | 淡了不撤。动量回到 0 附近，现货涨到 78987 |
| 07:41:30Z | ask 打到 0.25，当时 3 分钟动量约 **0**，当成成交。peak bid 0.27（费后 peak 2.5%） |
| 07:49:30Z | 8 分钟没到 +10%，`t_scratch` 在 bid **0.12**，**−1.374**（−55%） |

挂的是人手那种 32–42¢ coupon。错在**淡了还把 ask==rest 当成砸盘成交**。人手是砸盘里成交；这小时砸盘时卖一 37¢ 没打到 25¢，31 分钟后现货已经涨了 $42，NO 变便宜是因为 YES 在赢。scratch 做对了，没有拿到结算 −100%。

没把默认改回 29¢ 刀，也没把 p 降到 30%。改成交门：**只有动量仍 ≤−$100 才 promote**。淡了继续挂；反弹里不填；淡了 ask==rest 也不填。`AUG2605` 因亏后空 wait。

分钟回放这小时 coupon / nowait / loose **0 笔**。便宜对照吃了 YES @ 0.20 `t_clip` +21%——那是累了追涨，不改默认。

新窗口（AUG2604–AUG2513 / AUG2604–AUG2505），dump-only fill 之后：

| Run | 16h | 24h |
| --- | ---: | ---: |
| dump coupon（默认） | 6 / 4 / **−0.47** | 13 / 10 / **+6.98** |
| flex_nowait | 3 / 1 / −2.21 | 7 / 4 / +0.12 |
| flex_wait_loose | 7 / 6 / +4.93 | 13 / 11 / +11.83 |
| flex cheap p30/ask35 | 13 / 4 / −6.33 | 21 / 8 / −7.61 |

coupon 仍赢 nowait。`AUG2520` 仍是 +41.5%。完成成交 **2 / 0 / −3.5944**。回放绿不是达成。

## 09:00 收盘 `AUG2605`：空 wait 守住了，同向 taker 又亏

这小时是 `AUG2604` scratch 之后的 **skip-wait 小时**，不是 dump-only-fill coupon 的实盘测验。不要把「这小时没挂 coupon」当成 coupon 失败。

| 时间 | 事件 |
| --- | --- |
| 整小时 | 479 次扫描。动量 −$140 / +$211。现货 79095 → 78622 |
| 08:53–08:55 | journal 10 条 coupon 盘（`T78799` ask 0.39，随后 `T78699` ask 0.41）。引擎没挂 wait（skip）。对 |
| 涨势 YES | `blocked`（p&lt;52% / ask&gt;0.52）。没挂 YES |
| 08:55:59Z | 同向 `impulse_t` NO `T78699` @ **0.49**，p 56.2%，动量 −$139。AUG2518 规矩允许 |
| 随后 | peak bid 0.55，`t_stop` **−1.243**（−24.5%，买一 0.40） |
| 08:58 | 15m `lock_wait` `KXBTC15M-26AUG260500` rest 0.83 仍 working。不是 dump coupon，也不挡 hourly wait |

skip 守住了：0 笔 `impulse_wait` 成交。同向 taker 亏完把 `last_loss_event` 写成 `AUG2605`，下一小时 skip 变成 `AUG2606`。这是 **skip 跑步机**：coupon 亏 → 空下一小时 wait → skip 小时里再吃一笔亏的 taker → 再空一小时。AUG2518 就是为了「亏后再挂 wait」设的，所以这轮不改 skip、不关 skip 小时的同向 taker。

`不能实现就换策略` 的开关仍是：**下一小时真正挂上 coupon 并收盘之后**（不是 skip 小时）。现在还没到。`AUG2606` 空 wait；若这小时不再亏一笔 T，`AUG2607` 才是下一小时真挂。

分钟回放这小时 coupon 也是 **0 笔 wait**，只有同一笔 0.49 NO `t_stop`（1 分钟带是 −12.8% / −0.65，纸盘扫到了 0.40）。便宜对照吃了 YES @ 0.24 `t_stop` −25%——不追。

新窗口（AUG2605–AUG2514 / AUG2605–AUG2506），dump-only fill：

| Run | 16h | 24h |
| --- | ---: | ---: |
| dump coupon（默认） | 6 / 4 / **+0.81** | 13 / 9 / **+5.10** |
| flex_nowait | 3 / 1 / −0.93 | 8 / 4 / −0.53 |
| flex_wait_loose | 7 / 6 / +6.21 | 13 / 10 / +9.95 |
| flex cheap p30/ask35 | 13 / 4 / −6.11 | 21 / 8 / −8.69 |

16h 从 −0.47 到 +0.81，是窗口滚掉 `AUG2513` YES stop −1.93、滚进这小时 taker −0.65，不是 coupon 这小时赚了。coupon 仍赢 nowait。`AUG2520` 仍是 +41.5%。便宜 taker 仍红。不换 29¢ 刀，不把 p 降到 30%。

完成成交 **3 / 0 / −4.8374**。回放绿不是达成。`AUG2606` 空 wait。

## 09:30 `AUG2606` 中盘：skip 守住了，同向 taker 又亏，不再连空

`AUG2606` 还没收盘。skip-wait 守住了：journal 36 条 coupon 盘，引擎没挂。09:16 第一下砸盘 `T78599` ask **0.36**、动量 −$102——正是人手那种 32–42¢ coupon。空掉之后 09:20 同向 `impulse_t` NO `T78499` @ **0.46**（p 55%，动量 −$170），peak 0.49，`t_stop` **−0.6455**。

这是连续第二小时亏。若再按「任何一笔亏都空下一小时 wait」，`AUG2607` 又是 skip，coupon 永远挂不上。AUG2518 只要空 **一小时** wait。改成：**连续亏的小时不叠坐下一小时**。这小时刚亏过，不再开 T；`AUG2607` 真挂 coupon。

完成成交 **4 / 0 / −5.4829**。小时没收盘，不 sweep。回放绿不是达成。

## 10:00 收盘 `AUG2606`：skip 小时，不是 coupon 实盘

skip-wait 守住了：579 次扫描，36 条 coupon journal（`T78599` / `T78499` / `T78399`），0 笔 `impulse_wait`。09:16 `T78599` ask **0.36** 是人手盘，空掉了。09:20 同向 taker `T78499` @ 0.46 `t_stop` **−0.6455**。亏后这小时不再开 T（last_loss）。连续亏不叠 skip，所以 **`AUG2607` 现在真挂 coupon**。

不要把「这小时没挂 coupon」当成 coupon 失败。开关仍是下一小时 **真正挂上并收盘** 的 coupon。

分钟回放这小时 Kalshi 还没出 TWAP / live_data（`incomplete data`）。sweep 窗口滚到 AUG2606 起：

| Run | 16h | 24h |
| --- | ---: | ---: |
| dump coupon（默认） | 6 / 4 / **+0.81** | 12 / 8 / **+1.77** |
| flex_nowait | 3 / 1 / −0.93 | 8 / 4 / −0.53 |
| flex_wait_loose | 7 / 6 / +6.21 | 12 / 9 / +6.62 |
| flex cheap p30/ask35 | 14 / 5 / −5.79 | 22 / 8 / −9.16 |

16h 和上一窗口一样，这小时回放 0 笔。24h 从 +5.10 落到 +1.77，是滚掉 `AUG2506` wait clip +3.33。coupon 仍赢 nowait。`AUG2520` 仍是 +41.5%。便宜仍红。不换 29¢ 刀，不追 YES。

完成成交 **4 / 0 / −5.4829**。回放绿不是达成。`AUG2607` 真挂。

## 11:00 收盘 `AUG2607`：真挂小时，但没有砸盘

这小时 `skip_next=False`，是 dump coupon 的实盘窗口。450 次扫描，**0 条 wait**，0 笔 T。动量地板 **−$87**，从没到 −$100，没有 coupon 盘可挂。涨势 YES 到 +$117 时 `blocked`（`T78899` p 31% / `T78799` ask 0.59）——没挂 YES。`lock_wait` YES `T77399` 0.83 `wait_invalid`。

空 coupon 是 **没砸盘**，不是 coupon 失败，也不换策略。开关要的是「砸盘里挂上 32–42¢ 还是打不出人手那种 clip」。这小时没有砸盘。

`AUG2606` 分钟带现在齐了：结算带 78399.99–78499.99，回放同向 taker NO @ 0.51 `t_stop` −0.75（纸盘是 0.46 / −0.6455）。`AUG2607` 收盘瞬间仍是 `incomplete data`。

新窗口（AUG2607–AUG2516 / AUG2607–AUG2508）：

| Run | 16h | 24h |
| --- | ---: | ---: |
| dump coupon（默认） | 7 / 4 / **+0.06** | 13 / 8 / **+1.02** |
| flex_nowait | 4 / 1 / −1.68 | 9 / 4 / −1.27 |
| flex_wait_loose | 8 / 6 / +5.46 | 13 / 9 / +5.87 |
| flex cheap p30/ask35 | 15 / 5 / −6.54 | 22 / 8 / −7.88 |

16h 从 +0.81 到 +0.06，是滚进 `AUG2606` 回放 taker −0.75。coupon 仍赢 nowait。`AUG2520` 仍是 +41.5%。便宜仍红。不换 29¢ 刀，不追 YES。

完成成交 **4 / 0 / −5.4829**。回放绿不是达成。`AUG2608` 继续真挂。

## 11:45 盘中 `AUG2608`：第一次打出人手那种 dump coupon clip

这小时 `skip_next=False`，真挂。砸盘来了，coupon 挂上、砸盘里成交、10%–50% 带走。不是回放绿。

| 时间 | 事件 |
| --- | --- |
| 11:38:00Z | 动量跌破 −$100。`T78399` ask **0.49** `blocked`（p 41%→49%，过不了 52%） |
| 11:38:29Z | 最近行权价换成 `T78299` ask **0.46**，随后 0.43。p 36%–44%，taker 仍拒 |
| 11:39:40Z | rest `T78299` NO **0.25** under ask **0.34**，动量 **−$226**，现货 78350，p 42.7%，离现货约 $50。一张，没翻 YES，没换行权价 |
| 11:40:08Z | 砸盘里成交（28 秒），动量仍 **−$191**。dump-only fill 做对了 |
| 随后 | `t_clip` 在 bid **0.31**，**+18.0%**，pnl **+0.4502**。带是 10%–50%。不是每笔 20% |

`raw.peak_bid=0.23` 低于进场 0.25，但出场 note 是 clip 18% @ 0.31。不要拿 peak_bid 当失败。成交后 journal 扫过 `T78399` ask 0.49 p 56%（diagnose，没第二笔 T）。赢了 session 清掉（`last_loss_event=None`，`skip_next=False`）。

这是纸盘第一次人手那种 dump NO：32–42¢ 盘、挂 25¢、砸盘里成交、10%–50% 带走。不换策略。不放宽 taker p。不挂 YES。小时还没收盘（12:00 UTC），不 sweep。 leftover `lock_wait`（15m id 19、远 OTM `T83499`）不是 coupon。

完成成交 **5 / 1 / −5.0327**。回放绿不是达成。

## 12:00 收盘 `AUG2608`：真挂小时，砸盘里 clip 了

收盘账本没变：一张 coupon，`t_clip` **+0.4502**（+18%）。1007 次扫描，11 条 wait，10 条 open diagnose。动量地板 **−$246**，天花板 +$97。成交后涨势 YES 一直是 `no_impulse`，没挂 YES，没第二笔 T。赢了 session 清掉，**`AUG2609` 继续真挂**。

这小时砸盘了，并且打出人手那种 clip。不换策略。开关要的是「砸盘里挂上还是打不出 clip」——这次打出了。

分钟回放这小时收盘瞬间仍是 `incomplete data`（TWAP / live_data 还没出）。`AUG2607` 带现在齐了：结算带 78699.99–78799.99，回放 0 笔（纸盘也是空仓）。

新窗口（AUG2608–AUG2517 / AUG2608–AUG2509）：

| Run | 16h | 24h |
| --- | ---: | ---: |
| dump coupon（默认） | 6 / 3 / **−0.70** | 12 / 7 / **+0.15** |
| flex_nowait | 3 / 0 / −2.44 | 8 / 3 / −2.14 |
| flex_wait_loose | 7 / 5 / +4.70 | 12 / 8 / +5.01 |
| flex cheap p30/ask35 | 15 / 4 / −8.68 | 22 / 8 / −8.74 |

16h 从 +0.06 落到 −0.70，是滚掉 `AUG2516` taker clip +0.76，加上 `AUG2608` 回放 0 笔（纸盘 +0.45 还没进带子）。coupon 仍赢 nowait。`AUG2520` 仍是 +41.5%。便宜仍红。不换 29¢ 刀，不追 YES。

完成成交 **5 / 1 / −5.0327**。回放绿不是达成。`AUG2609` 真挂。

## 12:15 `AUG2608` 带子齐了；`AUG2609` 正在挂 coupon

`AUG2608` 结算带 **78399.99–78499.99**。`T78299` 结算 YES——纸盘那张 NO 若拿到结算是 −100%；`t_clip` +18% 走对了。

分钟回放和纸盘对不上。回放 11:39 吃了 `T78399` NO taker @ **0.52**，`t_stop` −1.05。纸盘同一分钟 `T78399` ask 0.49 p&lt;52% 拒了，改挂 `T78299` 0.25 并 clip。loose wait 回放挂到 25¢，`t_clip` +97%——那是 1 分钟收盘，不是改默认的理由。默认 coupon 也吃了这口回放 taker，所以 16h 变成 **7 / 3 / −1.74**，nowait **−3.49**。coupon 仍赢 nowait。`AUG2520` 仍是 +41.5%。便宜仍红。不换策略。回放绿不是达成。

| Run | 16h | 24h |
| --- | ---: | ---: |
| dump coupon（默认） | 7 / 3 / **−1.74** | 13 / 7 / **−0.89** |
| flex_nowait | 4 / 0 / −3.49 | 9 / 3 / −3.18 |
| flex_wait_loose | 8 / 6 / +7.13 | 13 / 9 / +7.43 |
| flex cheap p30/ask35 | 16 / 4 / −9.27 | 23 / 8 / −9.33 |

`AUG2609` 真挂：12:06:29 rest `T78299` NO **0.25** under ask **0.41**，动量 −$100，现货 78414（离现货 $115）。一张。砸盘里 ask 最便宜到 0.37，没打到 25¢，不成交。淡到 +$62 没撤。12:17 动量约 −$44，仍 working。不放宽 taker，不挂 YES。

完成成交仍是 **5 / 1 / −5.0327**。

## 12:30 盘中 `AUG2609`：淡了不撤，翻了才撤，第二波又挂上

第一张 coupon 没成交。12:22:52 动量 **+$102**，`wait_invalid` 撤了——这是对面翻涨 ≥+$100，不是淡了撤。涨势 YES `T78499` / `T78599` / `T78699` 一直 `blocked`，没挂 YES。

12:31:27 第二波砸盘 rest `T78199` NO **0.25** under ask **0.37**，动量 **−$281**，现货 78335，离现货 $135，p 33.6%。一张。`T78299` taker ask 0.46 仍拒。只在砸盘里成交。不放宽 taker。

12:34 动量 **+$115**，第二张也 `wait_invalid`（翻了才撤）。12:38 第三波：`T78099` ask **0.36** 是人手盘，引擎却吃了 `T78299` NO taker @ **0.51**（p 61%），peak 0.59 没到 10% 费后线 0.60，`t_stop` **−0.6498**。不是 coupon 打不出 clip——coupon 被 taker 抢走了。已改成：**有 32–42¢ coupon 先挂，不吃 0.51 taker**。这小时亏了，`AUG2610` 空 wait。

完成成交 **6 / 1 / −5.6825**。小时没关。不换策略。

改完后同一段 AUG2608 窗口：coupon 16h **7 / 4 / +0.15** vs nowait **−3.49**；24h **+2.72** vs **−3.18**。`AUG2608` 分钟收盘仍吃 0.52 taker（纸盘那张 0.34 coupon 不在 1 分钟收盘上）。`AUG2518` 不再吃亏的 taker（改挂 coupon），`AUG2519` 同向 clip +0.86。coupon 仍赢 nowait。`AUG2520` 仍是 +41.5%。

## 13:00 收盘 `AUG2609`：挂对了两张，第三波被 taker 抢走

真挂小时，砸盘了。不要当成 coupon 失败。开关要的是「砸盘里挂上 32–42¢ 还是打不出人手那种 clip」。这小时 coupon 挂上了两次，ask 没打到 25¢，翻涨撤了；第三波人手盘 `T78099` ask **0.36** 在，引擎却先吃了 0.51 taker。那是优先级，不是 coupon 打不出 clip。`AUG2608` 已经 clip 过。不换策略。

| 时间 | 事件 |
| --- | --- |
| 12:06:29Z | rest `T78299` NO **0.25** under ask **0.41**，动量 −$100，现货 78414（~$115）。一张。砸盘 ask 到 0.37，没打到 25¢ |
| 12:22:52Z | 动量 **+$102** → `wait_invalid`。翻了才撤，不是淡了撤。涨势 YES `T78499`/`T78599`/`T78699` 一直 `blocked` |
| 12:31:27Z | 第二波 rest `T78199` NO **0.25** under ask **0.37**，动量 **−$281**，现货 78335（~$135），p 33.6% |
| 12:34 | 动量 **+$115** → 第二张也撤。规矩对 |
| 12:38:31Z | 第三波：journal 已看见 `T78099` ask **0.36**（人手 coupon，p ~0.30）。引擎 `takers[:1]` 吃了 `T78299` NO @ **0.51** p 61% |
| 12:39:03Z | 成交。peak bid 0.59，费后 10% clip 要 0.60。`t_stop` **−0.6498**（−12.3%） |
| 之后 | last_loss 这小时不再开 T。coupon-first 补丁 12:45 才上，来不及挡这笔 |

981 次扫描。动量地板 **−$327**，天花板 +$157。现货 78219–78648。journal：70 wait / 123 blocked / 11 open。没挂 YES。没换行权价 hop。

分钟回放这小时收盘瞬间仍是 `incomplete data`（TWAP / live_data 还没出）。sweep 窗口滚到 AUG2609 起：

| Run | 16h | 24h |
| --- | ---: | ---: |
| dump coupon（默认） | 7 / 4 / **+0.15** | 12 / 8 / **+2.72** |
| flex_nowait | 4 / 0 / −3.49 | 8 / 3 / −2.44 |
| flex_wait_loose | 8 / 6 / +7.13 | 12 / 8 / +7.25 |
| flex cheap p30/ask35 | 15 / 3 / −9.09 | 21 / 8 / −6.61 |

16h 和补丁后 AUG2608 窗口一样（这小时回放 0 笔）。24h nowait 从 −3.18 到 −2.44，是窗口滚掉一笔亏的 taker，不是 coupon 这小时赚了。coupon 仍赢 nowait。`AUG2608` 分钟收盘仍吃 0.52 taker。便宜仍红。不换 29¢ 刀，不追 YES。

完成成交 **6 / 1 / −5.6825**。session：`last_loss_event=AUG2609`，`skip_next=True`，`skipped_event=AUG2610`。**`AUG2610` 空 wait**（连续亏不叠 skip）。回放绿不是达成。

## 13:05 `AUG2610`：空 wait 守住了，同向 taker clip 了

skip-wait 守住了：journal 看见 `T78199` / `T78099` ask **0.42** coupon 盘，引擎没挂 wait。13:05:07 同向 `impulse_t` NO `T78299` @ **0.48**，p 52.2%，动量 −$114。规矩允许（AUG2518 / AUG2605 / AUG2606）。随后 `t_clip` 在 bid **0.58**，**+13.2%**，pnl **+0.6546**。peak_bid 0.56 是标记，出场 0.58 是准的。没挂 YES。没挂 coupon。

赢了 session 清掉（`skip_next=False`）。这不是 coupon clip，是 skip 小时同向 taker。不换策略。剩下的 working 是远 OTM `lock_wait` 0.83，不是 dump coupon。完成成交 **7 / 2 / −5.0279**。

赢了之后这小时 `swing.dead`：13:06–13:11 journal 又看见 `T78099` ask **0.42**、`T77999` ask **0.41**，引擎没挂——一小时只做一笔 T，不是漏单。

## 13:16 `AUG2609` 带子齐了

结算带 **78299.99–78399.99**。`T78299` 结算 YES——纸盘那张 0.51 NO 若拿到结算是 −100%；`t_stop` −12.3% 比拿结算好。人手 coupon `T78099` 也会结算 YES，没成交所以无所谓。

分钟回放和纸盘对不上。回放 12:39 吃了同一张 `T78299` NO @ **0.51**，这次 `t_clip` +10.6% / +0.557（纸盘 peak 0.59 没到费后 0.60，停在 0.48）。纸盘那张 0.36 coupon 不在 1 分钟收盘上，coupon-first 挡不住这口回放 taker。loose wait 回放挂 25¢，`t_clip` +61% / +1.53——不改默认。

| Run | 16h | 24h |
| --- | ---: | ---: |
| dump coupon（默认） | 8 / 5 / **+0.71** | 13 / 9 / **+3.28** |
| flex_nowait | 5 / 1 / −2.93 | 9 / 4 / −1.88 |
| flex_wait_loose | 9 / 7 / +8.66 | 13 / 9 / +8.77 |
| flex cheap p30/ask35 | 16 / 4 / −7.88 | 22 / 9 / −5.40 |

16h/24h 变绿是滚进这小时回放 taker clip +0.56，不是纸盘 coupon 赚了。coupon 仍赢 nowait。便宜仍红。不换 29¢ 刀，不追 YES。回放绿不是达成。`AUG2610` 还开着，14:00 再 sweep。

## 14:00 收盘 `AUG2610`：skip 小时，同向 taker clip，不是 coupon 实盘

skip-wait 守住了：994 次扫描，85 条 coupon journal（`T78199` / `T78099` / `T77999`），**0 笔 `impulse_wait`**。13:05 同向 `impulse_t` NO `T78299` @ 0.48 `t_clip` **+13.2% / +0.6546**。赢了之后 `swing.dead`，后半小时 0.42 / 0.41 盘没挂——一小时一笔 T。涨势 YES 一直 `blocked`。没挂 YES wait。

不要把「这小时没挂 coupon」当成 coupon 失败。这是 skip 小时。开关要的是真挂小时砸盘里打不出 clip。`AUG2608` 已经 clip 过。不换策略。

分钟回放这小时收盘瞬间仍是 `incomplete data`。sweep 窗口滚到 AUG2610 起（这小时回放 0 笔）：

| Run | 16h | 24h |
| --- | ---: | ---: |
| dump coupon（默认） | 8 / 5 / **+0.71** | 12 / 8 / **+2.32** |
| flex_nowait | 5 / 2 / −1.03 | 8 / 3 / −2.84 |
| flex_wait_loose | 8 / 6 / +6.83 | 12 / 9 / +9.72 |
| flex cheap p30/ask35 | 15 / 4 / −6.45 | 21 / 9 / −4.53 |

16h 和 AUG2609 带子齐了那窗一样。24h 从 +3.28 到 +2.32，是窗口滚掉一笔赢的 T。coupon 仍赢 nowait。便宜仍红。不换 29¢ 刀。

session 已清。**`AUG2611` 真挂 coupon**。

## 14:00 `AUG2611`：开盘涨势 YES taker clip 了，不是 dump coupon

14:00:20 `impulse_t` YES `T78299` @ **0.50**，p 59.7%，动量 **+$137**。当时没有砸盘，没有 32–42¢ coupon 可挂。随后 `t_clip` 在 bid **0.60**，**+12.7%**，pnl **+0.657**。`raw.peak_bid=0.58` 是标记，出场 0.60。没挂 YES wait。没追便宜 YES。

赢了这小时 `swing.dead`：后头若再砸盘，coupon journal 可以写，引擎不再挂——一小时一笔 T。这不是 dump coupon clip。不换策略。完成成交 **8 / 3 / −4.3709**。回放绿不是达成。

## 公开方案对照（2026-08-26 16:00 ET）：窗口不可能那么少

用户说窗口不可能只有那几个。缓存 **42/42** 小时都有近 ATM **32–42¢ NO**，多数小时有 20–40 分钟这种盘口。等 −$40 / −$100 才挂，是把排队时间丢掉，不是市场没窗口。

公开材料没有「Kalshi BTC 小时盘 32–42¢ 下挂 25¢」的现成 playbook，但做市侧是同一句话：

| 来源 | 做法 | 和人手 coupon 的对应 |
| --- | --- | --- |
| Whelan *Makers and Takers*（30 万+ Kalshi 合约） | Taker 系统亏，Maker 相对赢；便宜合约对吃卖一的人更亏 | 挂 25¢，不要去吃 0.45–0.51 |
| favorite–longshot fade（[agiprolabs/prediction-market-strategy](https://github.com/agiprolabs/claude-trading-skills/blob/main/skills/prediction-market-strategy/SKILL.md)） | 在 ~5–20¢ 的 NO 上 **持续** rest maker，不等人砸完再挂 | 32–42¢ **看见就挂** 25¢；成交仍只要砸盘 |
| newyorkcompute `optimism-tax` | 在尾部区间做定向报价 | 只在 32–42¢ 这档挂，不追已经砸穿的 29¢ |
| Bitcoin Edge（IBIT 期权 vs KXBTCD） | 模型价和 Kalshi 价有缺口就做，不是等波动极值 | 25¢ rest 对 32–42¢ ask 本身就是缺口 |
| Kalshi View resting orders | 不成交、撤单都不收费 | 淡了不撤；没砸盘就空挂到翻面或收盘 |
| 人手 `manual.md` / `AUG2520` | 0.20–0.25 挂进；32–42¢ 看见就挂；只在砸盘中成交 | 成交门仍是 impulse ≤−$100 |

两道还在砍窗口的门，这一轮拆掉：

1. **孤零零的 25¢ 是 0.50–0.70 ATM 垫档，不是「看见 32–42¢ 就挂」。** `coupon_in_band` 已经拒垫档。`a07170b` 把挂单门也收成 3 分钟 |impulse|≥$100 之后，AUG2821 起 journal 只剩 `no_impulse`，AUG2903/05 动量到 −98.9 仍不看盘口。默认 `impulse_wait_rest_min` 回到 0：32–42¢ 看见就挂；成交、淡化、反手撤仍要同向 $100。
2. **`_taker_impulse_qualifies` 不再跳过 coupon。** 默认 taker 关着时，p≥52% 的 32–42¢ 会变成「wait 跳过 + taker 也不吃」。那是最好的 coupon。

第三道是执行层，不是策略：`AUG2617` 16:04 ET 扫描已经看见 `dump_gap NO 0.40`，`taken=1`，但 `_execute` 用「最多 3 张 working wait」把 leftover `lock_wait`（T75999 / T76499 / T76749 的 0.83）和 coupon 算在一起，coupon 没写进账本。剩锁仓单不再挡住 dump coupon。

不放宽到 0.45。不挂 YES。不默认便宜 taker。回放绿不是达成。`AUG2608` 已经 clip 过。

## 2026-08-26 16:13 ET：小时盘 = 下一个整点收盘

没有分歧。下午 4:13 要做的就是 **5 点截止** 的 `KXBTCD-26AUG2617`。不是 15 分钟盘，也不是再等还没开的 6 点 hourly。

Kalshi 把这场标成 `cadence=daily`、合约已开约 25 小时、$250 阶梯。旧代码用合约总时长 8–70 分钟当「快窗」，把这场整场拒了；15 分钟反而过得了门。coupon 改成：只做下一个整点 `KXBTCD`，看距收盘剩余 3–70 分钟；距离上限 $250，才能挂到日盘下一档（当时 T78249 离现货约 $218）。32–42¢ 带不放宽。

人手把这小时当成快线：量大（近 ATM）、趋势强（砸盘成交）、低估（32–42¢ 下挂 25¢）、赚 10%–50% 就抛、回不来就止损（scratch / 反手撤 / 硬止），不盼再落回已经砸穿的价。不因此改开 taker，也不收紧到会杀掉 `AUG2608` 那种反弹标记。

## 2026-08-26 16:42 ET：只做砸盘 NO 是错的

用户说得对：一小时 80 档不该整场空仓。4–5 点 `AUG2617` 是 **$250 日盘阶梯**，近 ATM 从 NO 0.05 跳到 0.73，中间没有 32–42¢ NO。唯一有量、模型也低估的是涨势 `T78499` YES **0.28**（p≈0.38）。旧策略只挂砸盘 NO，等于把这场快线废了。

改成跟趋势挂：涨 YES（28–42¢），跌/静 NO（32–42¢）。仍不吃 taker。29¢ NO 飞刀仍拒。成交仍要同向 ±$100。

## 2026-08-26：人手 `AUG2520` 一小时三档

人手同一小时挂了 `T78699` / `T78599` / `T78799`。机器改成一小时最多同时 rest **3** 档近 ATM。第一笔 clip 之后仍 dead，不 hop 新行权价。不吃 taker。29¢ 飞刀仍拒。
