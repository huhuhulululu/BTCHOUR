# 灵活编排（默认 playbook = `flex`）

> **2026-09-03 一年级回测把这一页的两条门量出来了**（1544 小时 / 66.2 天，
> [`../research/hourly-backtest-2026-09-03.md`](../research/hourly-backtest-2026-09-03.md)）：
>
> 1. **32–42¢ 这条带子被系统性高估 1.66pp**，吃 taker 每张 −3.28¢（t=−6.24）。008「不吃
>    32–42¢ taker」被证实。整条阶梯 0.58 以下每一格都显著为负（ADR 018）。
> 2. **成交的 coupon 进场是公道的**（持到结算 −0.86¢，t=−0.33），**亏在出场栈**：套上
>    10–50% 带 / −80% 止 / 8 分钟 scratch 之后是 −4.14¢（t=−4.99），日历两半都显著。
>    每张 3.28¢ 的代价。见 ADR 017，开关 `BTCHOUR_IMPULSE_WAIT_HOLD`。
>
> **017 已由用户授权生效**：`impulse_wait` 成交后**持到结算**（默认
> `BTCHOUR_IMPULSE_WAIT_HOLD=1`）。本页的挂单门原文不改（005/006/007/008 一字不动）——改的
> 只有成交之后怎么收尾。下表里 `impulse_wait` 那一行的「10%–50% 带；8 分钟没到 +10% 就
> scratch」**已作废**，其余照旧。`t_clip` / `t_stop` 对 `swing_t` / `impulse_t` 仍生效。
> 018 是事实登记，不是门。

默认不再只锁仓、也不只逆势刮头。`flex` 把几套玩法叠在一起，**先拿已经决定的 20%；砸盘里有 32–42¢ coupon 就挂 25¢，一小时最多同时 rest 3 档近 ATM。默认不吃 `impulse_t`。** 纸盘 10 笔完成里 7 笔 taker 把账打到 −4.46；目标是人手那种 dump NO **挂进**（017 之后收尾是结算，不是 clip），不是涨势 YES / skip 小时吃货。

## 这一小时就是快线盘

Kalshi BTC 小时盘 = **每小时开一次、整点收一次** 的快线。4:13 做 5 点截止的那本账。人手怎么选，机器就怎么译：

| 人手 | 机器 |
| --- | --- |
| 量大 | 扫下一个整点整条 `KXBTCD` 阶梯（$600，日盘两档）。**这一整条近 ATM 阶梯是工作盘**（常 100+ 档），不是 15 分钟仓那种「没边就坐着」。空仓而 32–42¢ 活着 = 策略失败。量在近 ATM，不在 15 分钟盘 |
| 趋势性强 | **跟带子走**：跌 \|impulse\|≥$100 且 32–42¢ 看见才挂 NO；涨 \|impulse\|≥$100 挂 YES。静/弱阳不挂。0.50–0.70 ATM 垫档不上。**成交只要同向 |impulse| ≥$100 且 ask 还在 rest**。打穿到 3¢ 不成交。反方向、淡化不成交。弱阳 YES 是乱挂，撤 |
| 当前被低估 | NO 要 32–42¢（29¢ 是飞刀）。YES 可以 28–42¢——5 点日盘 $250 档常常只有这一档中间价（活盘 `T78499` YES 0.28） |
| 赚了就抛 | **017 之后不再对 coupon 成立。** 348 张成交上，clip 每张比持有差 3.28¢（t=−4.99）。coupon 成交后**拿到结算**。`t_clip` 只剩给 `swing_t` / `impulse_t`（两者默认关） |
| 波动大，回不来就止损 | **挂单**阶段仍然照旧：反手 ≥+$100 撤挂、错边撤、淡化不成交。**成交之后**不再止损——`t_scratch` / −80% `t_wait_stop` 与 clip 是同一个不对称截断，一起作废（017）。默认不吃同向 taker |

不是换策略。不放宽 NO 32–42¢ / YES 28–42¢。跟趋势挂 YES。不默认便宜 taker。（`AUG2608` 的**挂进**仍是样板；它的 `t_clip` 不再是——那一档实际结算判 `yes`，NO 持到结算归零，见 GOALS 的两张样板表。单张说明不了方向，依据是 348 张的每张净值。）

| Play | 何时进 | 何时出 |
| --- | --- | --- |
| `lock_hold` | σ≥3.2、p≥99.8%、ask≤$0.82、费后 b 和 EV 都 ≥20% | 拿到结算；盘口若已锁 20% 也可以提前走。**不做 T、不在 TWAP 前 flatten** |
| `impulse_t` | **默认关**（`BTCHOUR_IMPULSE_TAKER=0`）。只留在 sweep 的 `flex_nowait` / cheap 对照。纸盘 skip 小时同向 taker `AUG2605`/`AUG2606` 止损 −1.89，涨势 YES `AUG2614` 止损 −0.65 | sweep 对照仍用 10%–50% 带 / −12% 止损 |
| `impulse_wait` | **趋势 coupon**。下一个整点整条阶梯（$600）。跌 \|impulse\|≥$100 且 32–42¢ 看见才挂 NO；涨 \|impulse\|≥$100 挂 YES（28–42¢）。静/弱阳不挂。`AUG2911` 在 +0 / ATM 0.42 挂 NO 是错的。rest $0.25。一小时最多同时挂 **3** 档近 ATM。0.50–0.70 垫档不上。同时挂 YES+NO 不是默认。成交只要同向 |impulse| ≥$100。29¢ NO 仍是飞刀。不吃 taker。第一笔成交后 dead，不 hop（007 不变） | **持到结算**（017）。挂着的时候仍会反手撤、错边撤；成交之后不 clip、不 trail、不 scratch、不 −80% 止、不 TWAP flatten |
| `swing_t` | 小时/15m、距行权价 ≤$600、ask $0.18–$0.72、p≥55%、p−ask≥8% | 同一条 10%–50% 带 / 4¢ 回撤 / p 淡化 12 点；同一小时一张合约，不翻面 |
| `lock_wait` | 已经决定但卖一还贵：在 $0.83 挂等 | **不成交不算入账** |
| `hold_edge` / `markout_scalp` | 旧玩法，设 `BTCHOUR_PLAYBOOK=hold` 或 `scalp` | 见下 |

单独只用短线：`BTCHOUR_PLAYBOOK=swing`。单独只用锁仓：`BTCHOUR_PLAYBOOK=lock`（见 [`lock.md`](lock.md)）。做T细则见 [`swing.md`](swing.md)。

反复对照：`python3 -m btchour sweep --hours 16`。每个小时的 K 线只拉一次（缓存在 `data/replay-cache/`），再在同一段带子上跑 flex / swing / lock、skip 开/关，以及「便宜 NO / 低 p」对照。`--hours 16` 也会拉满 24 小时并同时报告两个窗口。便宜对照目前更亏，默认仍是 p≥52% / ask≤$0.52。

## 盘口锁定 / clip

买入成本 `C`（含 taker 费）。在买价 `P` 卖出（击买一 = taker）后：

```
round_trip_roi = (P − exit_fee − C) / C
```

`lock_exit_price` 是使该 ROI 达到目标的最低整分价格。taker 买在 $0.62，大约 **$0.73 锁 12%**、**$0.78 锁 20%**。深 ITM 的 $0.95–$0.99 做不出 T，也锁不住 20%。

## 默认门槛

- `BTCHOUR_PLAYBOOK=flex`
- 锁仓：σ 3.2 / p 99.8% / $0.82–$0.83（见 [`lock.md`](lock.md)）
- 做T（`swing_t` / `impulse_t`，默认都关）：动量方向、ask $0.18–$0.52、兑现 **10%–50%**、不翻面（见 [`swing.md`](swing.md)）
- coupon 成交后：**持到结算**，`BTCHOUR_IMPULSE_WAIT_HOLD=1`（017）
- 失效：`INVALIDATE_P=0.40`（只作用于非锁仓仓位）
- 收盘前：`FLATTEN_SECONDS=40`
- `ALLOW_EARLY_EXIT=1`

灵活的意思是：**能锁 20% 就锁；砸盘里看见 32–42¢ coupon 才挂 25¢（一小时最多 3 档近 ATM），静/弱阳不挂，成交仍只要同向 |impulse| ≥$100；第一笔成交后不再 hop 新行权价；剩的 $0.83 lock_wait 不占 coupon 名额；默认不吃 taker；亏了下一小时整小时不做 T（挂单和 taker 都停，结算亏一样算亏）；连续亏不叠坐下一小时；锁过这小时不再开 T；**成交之后拿到结算**；不翻面。** 不是每笔保证 20%。没有票就空仓——但空仓必须先说清整点档 / `$600` 内 / 活档还剩几档（011）。不要把便宜 NO 改成 taker——那条带子已经对照过，更亏。不要把 15m 坐等抄过来。
