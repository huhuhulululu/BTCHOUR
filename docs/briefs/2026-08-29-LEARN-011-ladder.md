# brief — LEARN — 011-use-the-ladder
日期: 2026-08-29
角色: LEARN
角色卡: docs/roles/LEARN.md
模型: claude-fable-5-thinking-high
负责人（A）: CMD
执行（R）: LEARN

## 目标（一句）
用户要提权重视小时盘相对 15 分钟研究仓的操作优势：每小时一百多档，不可能机会那么少。说明为什么 `AUG2914` 188 档只做成 1 笔噪声 live（375），并提出**下一小时起**怎么用满近 ATM 阶梯（最多 3 纸 / 1 真），且不回退看见就挂。

## 必读
- docs/roles/LEARN.md
- docs/GOALS.md
- docs/decisions.md（005–008、**011**、007 clip 不 hop）
- catalog/rules/plays.md
- catalog/research/manual.md（AUG2520 多档 NO、AUG2608）
- catalog/snapshot/sweep.json（对照，绿了也不是达成）
- btchour/strategy.py：`coupon_sides` / `coupon_rest_ready` / `pick_dump_wait`
- ops/notes/learn-20260829-375.md（若在）

## 可写路径
- ops/notes/learn-20260829-011-ladder.md

## 禁区（默认 + 本任务）
- 不切整条 loop live；`trading_active=true` 不是切 live
- 不新开第二张 live_one；已有 resting 不再挂
- 不写 data/*.sqlite；不提交 .env / keys / latest.json
- **不建议**看见 32–42¢ 就挂、成交门 0.45、吃 taker、放宽 p=0.30
- 不把回放绿 / `flex_nowait` 写成达成或现行路径
- 不改 btchour/；不 git commit / push
- 不对用户直接播报
- 本小时 `AUG2914` 已 clip，**禁止建议 hop**
- 不要把 15m 仓「没边就坐着」写成小时盘默认

## 步骤
1. 用 `catalog/snapshot/latest.json`（只读，勿提交）或 board 逻辑数清：整点档 / `$600` 内 / 32–42¢。抽查至少 1 个数字。
2. 对照金带子：`AUG2520` 是**同一小时多档** NO，不是 1 档碰运气。
3. 拆现行门如何把 188 档压成 0–1 笔（静/弱阳不挂、live_one=1、clip 不 hop、成交仍要 $100）。哪些是该守的，哪些是没用上阶梯。
4. 只给 **AUG2915 起** 的提案：如何在 3 纸 / 1 真里选最好的活档、怎么排、何时空仓才算正当。判决词必须有。
5. 写明是否建议进 ADV。改门数字（005）没有用户点头不能建议马上 ENG。

## 完成定义（Done）
- 判决词 ∈ {已否定, 弱候选, 可采纳候选, 不可判定, 不是达成}
- ≤3 个关键数字 + 路径
- 明确：本小时不 hop；不回退看见就挂
- 写到 `ops/notes/learn-20260829-011-ladder.md`

## 交付格式
- 给 CMD 的内部稿用中文
- 最后一条消息：结论 + ≤3 关键数字 + 路径 + 是否进 ADV

## 主控抽查点
- 188 或当时快照整点档数是否对得上 latest.json `current_hour.market_count`
- 提案有没有偷改 0.45 / taker / p=0.30 / 看见就挂

## 不可逆 / 须请示
- 任何改 005 数字 → 只标「须 ADV + 用户」，不要当已批准
