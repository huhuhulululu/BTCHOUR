# Decisions（小时盘冻结门）

> 追加写入，不修改已有条目。格式: `### [编号] [标题]`
> 这不是 15 分钟盘的 ADR 全集。只记本仓库已经对用户生效的门。改门必须走 ARCH 草稿 → ADV → CMD → 用户。

---

### 001 只做 BTC 小时盘 `KXBTCD`

- 日期: 2026-08-25
- 状态: accepted
- 背景: Kalshi 还有 `KXBTC` 区间盘、`KXBTC15M` 15 分钟涨跌。
- 决策: 主路径只做下一个整点收的 `KXBTCD-{YY}{MON}{DD}{HH}`。午夜是 `AUG2900`，不是 `AUG2824`。
- 理由: 用户要的是小时盘。15 分钟盘研究在 `huhuhulululu/kalshi`。

### 002 计分 `EV = p · b − (1 − p)`

- 日期: 2026-08-25
- 状态: accepted
- 决策: 所有进场/对照用同一公式。~20% 是过滤，不是每笔保证。

### 003 默认 paper，禁止整条 loop 切 live

- 日期: 2026-08-26
- 状态: accepted
- 决策: `BTCHOUR_MODE=paper`。`GET /exchange/status` 的 `trading_active=true` 只是开盘门，不是切 live。
- 理由: 用户明确拒绝「整条 loop 切 live」。

### 004 实盘只允许 `live_one` 一张 post-only

- 日期: 2026-08-28
- 状态: accepted
- 决策: 每次最多 1 张真单。已有 1 张 resting 不再挂第二张。撤单必须带 `exchange_index`（`KXBTCD` = 2）。
- 理由: 必须一张一张实测；同时乱挂会一边倒吃瘪。

### 005 coupon 门（`952cb97`）

- 日期: 2026-08-29
- 状态: accepted
- 决策:
  - 涨 = 3 分钟 `|impulse| ≥ $100` 才挂 YES（28–42¢）
  - 跌 = `impulse < 0` 且 32–42¢ 看见才挂 NO（29¢ 飞刀不接）
  - 静 / 弱阳不挂
  - 成交 / 淡化 / 反手撤仍要同向 `|impulse| ≥ $100` 且 ask 还在 rest
  - rest $0.25；不吃 taker；0.50–0.70 垫档不上
- 理由: 「看见 32–42¢ 就挂」在 `AUG2911` +0 / ATM 0.42 停 34 分钟，`t_wait_stop` −85%。「3 分钟 $100 才挂」又找不到点。两头都不回退。

### 006 亏了空一小时；0 成交不叠 skip

- 日期: 2026-08-26
- 状态: accepted
- 决策: 亏了下一小时整小时不做 T。连亏不叠。0 成交不是亏损。

### 007 clip 之后本小时不 hop

- 日期: 2026-08-27
- 状态: accepted
- 决策: 第一笔 coupon clip 后这小时不再开新行权价、不翻面。

### 008 不靠放宽门成交

- 日期: 2026-08-26
- 状态: accepted
- 决策: 不把成交门放到 0.45；不吃 0.45–0.70 taker；不放宽 `p=0.30`；不回退成「不挂 YES」。
- 理由: `flex_cheap` / `flex_nowait` 对照更亏或不是现行路径。回放绿不是达成。

### 009 核盘只用表格、纽约时间、中文

- 日期: 2026-08-28
- 状态: accepted
- 决策: `python3 -m btchour board`。不画图。不提交 `latest.json` / `index.json` / `probe.json` / `.env` / `data/` / keys。整点 sweep 只强制提交 `catalog/snapshot/sweep.json` + `replay.json`。

### 010 用户只跟 CMD 说话；每笔实盘单必须复盘

- 日期: 2026-08-29
- 状态: accepted
- 背景: 用户裁决：以后只跟主控沟通，由 CMD 做角色调用和分工。第一条指令是每一笔实盘单都要仔细分析。
- 决策:
  - 对外频道只有 CMD。专员不对用户直接说话。
  - 每笔 `live_one`（成交或撤单）CMD 必须派 **LEARN**（像不像金带子）和 **RISK**（该不该挂），抽查后用表格复盘。
  - 复盘不是改门许可。改门仍走 LEARN→ADV→用户。
  - 样本：372 撤、373 stop、374 clip。漏复盘 = 违规。

### 011 小时盘阶梯操作权高于 15 分钟研究仓

- 日期: 2026-08-29
- 状态: accepted
- 背景: 用户 13:47 EDT：每个小时那么多盘，不可能机会那么少。本仓库敏锐度 / 实际操作可能性远比另外的仓库（`huhuhulululu/kalshi`，15 分钟盘，结论偏无波动率溢价、坐等）更厉害，要把这个更加提权重视。本小时 `AUG2914` 快照 **188** 档，只做成 1 笔噪声 live（375）。
- 决策:
  - 小时盘整条近 ATM 阶梯（距离 **$600**，活盘 **32–42¢**）是**工作盘**，不是 15 分钟仓那种「没边就坐着」。
  - **空仓而近 ATM 阶梯活着 = 策略失败**，不是「没机会」。
  - CMD 心跳必须报：整点档数 / `$600` 内 / 32–42¢ 活档。不能只报空仓。
  - 操作强度优先于 15 分钟研究姿态。不把 15m 坐等抄过来。
  - 本小时已 clip **不 hop**（007）。用满阶梯的提案给下一小时。
  - **不因此回退**：看见 32–42¢ 就挂、成交门 0.45、吃 taker、放宽 `p=0.30`、整条 loop 切 live。
- 理由: 一百多档近 ATM 阶梯是本仓库相对 15m 仓的优势。机会少是策略没用上阶梯。

### 012 CMD 要最聪明、最灵活、产出最高

- 日期: 2026-08-29
- 状态: accepted
- 背景: 用户 14:26 EDT：你应该是最聪明、最灵活，以后也是你产出最高。
- 决策:
  - 这张小时盘桌由 **CMD** 主控。聪明 = 用满近 ATM 阶梯、先自己核盘再派专员；灵活 = `flex`、跟带子换边、3 纸 / 1 真用满；产出最高 = 真动量小时打出人手那种 clip，压过 15m 研究仓。
  - 产出用「工作盘有没有用上」衡量（011），**不用噪声单刷量**，也不用坐等充聪明。
  - CMD 心跳和用户指令里先自己判阶梯；专员只做 010 复盘和改门对抗。
  - **不因此回退**：看见 32–42¢ 就挂、成交门 0.45、吃 taker、放宽 `p=0.30`、整条 loop 切 live。
- 理由: 用户要的是这张桌的敏锐度和实际操作，不是另一个仓库的坐等。

### 013 真单出场必须交易所成交后才落账

- 日期: 2026-08-29
- 状态: accepted
- 背景: 378 第一次 flatten IOC 买 YES 0.64 被撤（盘口更差），`fill_count=0`，引擎仍 `paper_close`。用户在 Kalshi 看见仓还在。
- 决策:
  - live flatten **按现价交叉**（买 YES ≥ ask，卖 YES ≤ bid），不按 clip 标记价傻挂。
  - `fill_count=0` **不得** `paper_close`。账上的出场价用交易所 `average_fill_price`。
  - 每轮对账：sqlite 已平但交易所还持有我们的 `live_one` → 再 flatten。残留未平禁止再挂第二张。
  - 开仓仍不吃 taker。平仓 IOC 可以交叉。
- 理由: 账平仓在是愚蠢错误，不是策略。

### 014 停 `live_one`（安全停机补丁）；停机期间跌侧挂门 |impulse| ≥ $100

- 日期: 2026-08-30
- 状态: accepted
- 定性: **安全停机补丁**。不是金带改进，不是 005 的可采纳修订。006 / 007 / 008 一字不动。
- 背景: 用户 07:42 EDT「如果没有新的改善或变化，就不要做了」；07:48 EDT「所有角色都参与」。今天 382–388 七张真挂，挂时动量 −15 到 −0.2，无一张 NO |impulse| ≥ $100，无一张是金带子那种 dump。live_one 17 张、成交 14 张净 +0.0677 是噪声。引擎事实：live_one 由交易所成交直接 promote，005 的同向 $100 成交门只对纸盘 rest 生效。
- 全员: LEARN 可采纳候选、不是达成。ADV 攻得倒、降级为停机补丁。ARCH 推荐 D。RISK 黄、现在停 live。MON 无滞留。ENG 102 绿。QA 可合并。DOC 对齐 plays.md。RPT=CMD。
- 决策:
  - `live_one` 默认 **False**。真单通道保留（004 不撤），但默认不往交易所送。
  - 停机期间跌侧挂门：挂 NO 要 `impulse < 0` **且** `|impulse| ≥ $100`，且 32–42¢ 看见。涨跌同门。YES 仍 28–42¢，不回退「不挂 YES」。005 原文不改；重开 live 时必须重新论证 $100。
  - live_one 真单淡化即撤（`wait_fade`）；纸盘淡了仍坐。该分支在 `live_one=False` 下不可达，回放不算验证。
  - 重开 live_one 需同时满足并经用户批准：① 纸盘出现 AUG2608 型（挂时同向 |impulse| ≥ $100、带内、真成交、10–50% clip）；② 015 有 ADV 对开结论；③ `wait_fade` 经真单或专测验证。
  - 关停前置：交易所零持仓零 resting（013 对账当前挂在 live_one 开关下）。
- 后果: 跌侧挂单变少；384/386 那种未到 $100 或 ask≤rest 的真砸也会被挡。不得把「不挂了所以不亏了」写成达成。停机期间金带子出现也不自动发交易所。
- 不因此回退: 成交门 0.45、吃 taker、放宽 p=0.30、整条 loop 切 live、「不挂 YES」、看见 32–42¢ 就挂。
- 理由: 送的是同型噪声单。先关真钱通道。

### 015 挂单语义题（占位，未决）

- 日期: 2026-08-30
- 状态: proposed
- 题面: 现行 rest 0.25 语义要求市场先给 8¢ 逆向折扣，等价于「只成输单、排除赢单」。AUG2520 人手是在砸盘启动时买在市场正在交易的价位。这是构造性错位，不是阈值松紧。
- 不预设结论。不得在本题下夹带 0.45 / taker / p=0.30 / 整条 live。
- **2026-09-03 补充量化（不改本条状态）**：1544 小时 / 66.2 天回测里，按 005 原文挂 2389 张
  coupon，成交 **348 张 = 14.6%**。折扣确实存在。但成交本身**不是**输单：持到结算 −0.86¢
  t=−0.33，统计上公道。亏的是出场栈（见 017）。题面的「只成输单」这半句被数据否掉，
  「构造性错位」那半句（14.6%）被证实。

### 016 `cushion_hold` / `edge` playbook（候选，**未通过样本外，不接线**）

- 日期: 2026-09-03
- 状态: **rejected-pending-data**（代码已并入，默认 **关**；留作 `sweep` 对照）
- 背景: 用户要求借鉴 `huhuhulululu/kalshi`（只读，未改动）提升 EV。那边 48 个 phase 里唯一
  的跨币种失效点是 phase1 的「偏强 0.85–0.95 被低估 1.5–2pp」。移植的不是它的参数，是机制：
  现货已把行权价甩开 ≥N 个**剩余波动** σ（`sigma_cushion`，`lock_hold` 用的同一函数），而
  该侧卖一还在 0.70–0.90 → 吃卖一，持到结算。
- 证据: `catalog/research/hourly-backtest-2026-09-03.md` 第 4 节。全样本 n=1612、**+0.53¢、
  t=0.62、CI [−1.15, +2.21]**；日历三分之一 −0.08 / −0.62 / +2.28；**前后两半符号相反**
  （σ2.0–2.5×0.80–0.90：前半 −3.27pp、后半 +5.60pp）；**+1¢ 滑点即翻负**（−0.42¢）。
  九条预注册候选里唯二过 Bonferroni 的是两条负对照（coupon taker −2.20¢ t=−4.05、
  cheap 0.18–0.30 −3.30¢ t=−5.48）。
- **模型修正后逐次重测（同日）**: 上面那组数字用的是修正前的 `digital_prob`。

  | 模型 | n | 每张净 | t | 日历三分之一 |
  | --- | ---: | ---: | ---: | --- |
  | 修正前 | 1612 | +0.53¢ | 0.62 | −0.08 / −0.62 / +2.28 |
  | +019（TWAP 方差） | 1665 | +0.47¢ | 0.56 | −0.06 / −0.36 / +1.83 |
  | +019+021（再加小时内季节性） | 1879 | **+0.07¢** | **0.09** | −0.02 / −1.55 / +1.78 |

  **模型越准，这条「边」越小。** 这本身就是判决：所谓「盘口没跟上模型」里，相当一部分
  是**模型自己错**，不是盘口错。判决不变，而且比之前更硬。
- 决策:
  - **不接线。** `BTCHOUR_CUSHION_HOLD` 默认 `0`；`--playbook edge` 与 sweep 的
    `edge` / `flex_cushion` 只作对照，地位等同 `flex_cheap` / `flex_nowait`。
  - 首次看见它是在 18 小时→18 天的样本上（+2.7¢ t=2.2），样本一长就塌。**小样本变绿不是达成**
    在进场规则上同样成立。
  - 重开条件：样本翻倍（≥130 天）后按 `research/study_rule.py` 原脚本重测，两个日历半样本
    同号且 +1¢ 滑点后仍为正，才允许再提。
  - **重开条件不可回填（022）**：Kalshi 只保留约 66 天的已结算市场记录，更早的事件连
    `result` 和 `expiration_value` 都没有。130 天只能**往前攒**——从 2026-09-03 起每天跑
    `research/pull_hourly.py`，约 65 天后才够。中断超过 66 天，那段永久丢失。
- 结构保证（这条 play 即使被打开也不碰任何冻结门）: 卖一下限 **0.70**，所以 008 的
  「不吃 0.45–0.70 taker」结构上不可达；不挂单，所以不碰 005；不做 T，所以不碰 006 / 007。
- 不因此回退: 0.45 成交门、吃 taker、p=0.30、整条 loop 切 live、「看见 32–42¢ 就挂」。

### 017 出场栈是本仓库当前最大的可量漏洞

- 日期: 2026-09-03
- 状态: **accepted**（用户 2026-09-03「授权你」）。`BTCHOUR_IMPULSE_WAIT_HOLD` 默认 **1**；005 的进场原文一字不动，这是**出场**改动
- 背景: 016 的回测顺带把「同一批进场，持有 vs 出场栈」量了出来。两组互不相干的进场给出同一
  答案，方向一致、都显著。
- 证据（`catalog/research/hourly-backtest-2026-09-03.md` 第 2、3 节）:

  | 同一批进场 | 持到结算 | 套现行出场栈 | 差 |
  | --- | ---: | ---: | ---: |
  | coupon 成交 348 张 | −0.86¢ (t=−0.33) | **−4.14¢ (t=−4.99)** | **−3.28¢** |
  | cushion 进场 1612 张 | +0.53¢ (t=+0.62) | **−1.61¢ (t=−4.73)** | **−2.13¢** |

  coupon 那组在日历两半都显著（早 −7.96¢ t=−4.60 / 晚 −3.31¢ t=−3.06）。15 分钟仓在另一个
  合约上同向（phase4 G1 十一条出场全负；phase12b 止盈在每个价带都比持有差 1.3–2.7¢）。
- 机制: 不对称截断。赢家被 `t_clip` 截在 +10%（上限 50% 很少摸到），输家吃满 −80% `t_wait_stop`
  或 8 分钟 `t_scratch`。这不是阈值松紧，调 10%/50%/−80% 都改不掉符号。
- 决策:
  - 成交的 coupon 按**结算**了结：不 `t_clip`、不 `t_trail`、不 `t_wait_stop`、不 `t_scratch`、
    不 `lock_on_book`（25¢ 上的 20% 锁就是 `t_clip` 换名字）、不 TWAP flatten。
  - 默认 `BTCHOUR_IMPULSE_WAIT_HOLD=1`。设 `0` 恢复旧栈，作为 sweep 的 `flex_clipstack` 对照。
  - **005 / 006 / 007 / 008 原文不动。** 挂单门、skip 小时、不 hop、不吃 taker 全部照旧；
    改的只有成交之后怎么收尾。
  - `cushion_hold` 与 `lock_*` 已经由 `strategy.is_settle_play()` 结构性豁免出场栈。
  - **006 不受影响**: 结算亏一样算「亏了」。`store.session_memory()` 本来就读 settled；
    `replay_bars` 的 settle 分支同步补上 `remember_session_exit`，否则 skip 小时会随着
    clip 栈一起悄悄失效。
- 后果: GOALS 的「金带子」定义已按本条重写（用户同一次授权）。兑现那一半从 `t_clip 10%–50%`
  改成**持到结算**；判据从单张 ROI 改成一批单的每张净值。
- **014 的重开条件 ① 随之改写**（不是放宽，是换成现行定义，否则它永远不可达）: 原文要求
  「纸盘出现 AUG2608 型（挂时同向 |impulse| ≥ $100、带内、真成交、**10–50% clip**）」。
  017 之后没有 clip 这一步，① 改成「挂时同向 |impulse| ≥ $100、带内、真成交、**持到结算**」。
  014 的 ② ③ 与「经用户批准」原文不动，`live_one` **仍然默认关**。
- **校正（写进 ADR 以免再被引用错）**: 起草时曾写「`AUG2608` 那张 NO 结算是 $1.00，clip 只拿了
  一小块」。**这是错的。** 回测库核过：`KXBTCD-26AUG2608` 结算 78470.96，`T78299.99` 判 `yes`，
  那张 NO **持到结算归零**——clip 救了这一笔。`AUG2520` 反过来：结算 78513.03，`T78599.99`
  判 `no`，NO 持到结算赢满。两张真单方向相反，这正是本条的依据必须是 348 张成交的每张净值、
  而不是任何一张样板的原因。
- 不因此回退: 0.45 成交门、吃 taker、p=0.30、整条 loop 切 live、「看见 32–42¢ 就挂」。

### 018 小时盘阶梯的价带图（事实登记，非门）

- 日期: 2026-09-03
- 状态: accepted（事实登记）
- 事实: 1544 小时 / 66.2 天，吃卖一持到结算，按卖一价带分组，聚类 t：**0.58 以下每一格都显著
  为负**（0.25–0.32 −4.52¢ t=−7.19；32–42¢ coupon 带 −3.28¢ t=−6.24；0.18–0.25 −3.33¢
  t=−6.02），**0.58 以上没有一格与零可区分**（最好的 0.75–0.82 是 +0.13¢ t=0.23）。
- 含义: 便宜侧被系统性高估 1–3pp（不是价差：价差会对称）；偏强侧的毛边 +0.7~1.3pp 正好等于
  该价位的二次 taker 费。maker 挂偏强侧在严格穿越口径下被逆向选择 1–2.7pp（第 5 节）。
- 这条**不是**「所以空仓」的许可（011 不变）。它是说：机会不在 32–42¢ 的挂等里。
- 产物: `catalog/research/hourly-backtest-2026-09-03.md`、`research/`。

### 019 结算是 60 秒均值，`digital_prob` 的有效方差时间要按均值算

- 日期: 2026-09-03
- 状态: accepted（**修正**，不是新的边）
- 背景: 第 7 节量出实现分布比模型窄，且越接近收盘越窄——`(结算 − 现货)/σ_模型` 的标准差
  >30 分钟是 1.05、10–30 分钟 0.96、**最后 10 分钟 0.82**。漂移可忽略（均值 +0.014σ）。
- 事实: `KXBTCD` 结算 = 最后 60 个 BRTI 报价的**简单平均**（`catalog/rules/settlement.md`），
  不是最后一跳。对布朗路径取尾部均值，方差小于取端点。
- 旧实现是**反的**: `seconds <= 60` 时把 tau **抬到** 60 秒地板，等于宣称「还剩 10 秒时仍有
  整整一分钟波动没走完」。实际此时均值窗口已经印掉大半。
- 决策: `model.twap_variance_seconds(τ, T=60)` 给有效方差时间，`digital_prob` 与
  `sigma_cushion` 共用（两者必须描述同一个分布）：

  ```
  τ ≥ T :  τ − 2T/3          # 大 τ 时 ≈ τ；τ=60s 时 = 20s
  τ < T :  τ³ / (3T²)        # 已经在均值窗口内；两支在 τ=T 相接
  ```

  推导: 结算 = (1/T)∫ 路径 over [τ−T, τ]，Var = (1/T²)∬min(s,t)。
- 证据: 1544 小时 / 15.2 万条观测，Brier 在**每一个**时间桶都变好——全样本
  0.08727 → **0.08716**，10–30 分钟 0.07778 → 0.07761，**最后 10 分钟 0.05288 → 0.05231**。
- 幅度诚实说: 全样本 Brier 只好 0.13%，最后十分钟好 1.1%。**这是把模型算对，不是找到边。**
  它让每一道用 `model_p` 的门（`swing_min_gap`、`invalidate_p`、`lock_min_p`、cushion）更准，
  不产生新的进场。
- 局限: `τ < T` 那一支是**推导的、未被样本验证**——回测样本从 τ=120s 起。它主要影响
  `flatten_seconds` 附近的判断，那一段没有历史可核。
- 后果: 016 的数字按新模型重测（n=1665、+0.47¢、t=0.56），**判决不变**。
- 不因此回退: 0.45 成交门、吃 taker、p=0.30、整条 loop 切 live、「看见 32–42¢ 就挂」。

### 020 两腿 / 区间族已关闭（事实登记，非门）

- 日期: 2026-09-03
- 状态: accepted（事实登记）
- 背景: 前面所有测试都是**单腿**，价带图结构上看不到市场对**分布形状**的定价。`KXBTCD` 的
  两条腿可以合成区间：`YES(K1) + NO(K2)` = 永远 $1，外加 $1 若 `K1 < S ≤ K2`，真实价格
  `yes_ask(K1) + no_ask(K2) − 1`。区间越宽两腿越深、二次费越小（±$1000 两腿合计约 0.14¢）。
- 事实: 隐含密度确实比实现宽——中价口径下 z<0 侧实现高出隐含 1.4–1.75pp、z>0 侧低 0.8–1.4pp
  （n=15 万）。但买区间（两腿都吃卖一）全样本每张净 −0.12 到 +0.39¢，t 全部 <0.4。
  最好的一格是 ±$200 进 TWAP 窗口 +1.51¢ **t=1.65、每天 4.4 笔**，不够。
- 含义: 密度那 1pp 正好等于两个半价差加两次费。**两腿 / 区间不是路**，与单腿 taker（018）、
  maker 挂偏强侧（第 5 节）同一堵墙。
- 产物: `research/study_density.py`；`catalog/research/hourly-backtest-2026-09-03.md` 第 6 节。

### 021 小时内波动季节性：trailing 估计器按位置系统性偏差

- 日期: 2026-09-03
- 状态: accepted（**修正**，不是新的边）
- 背景: 019 接线后残差仍有结构——`(结算 − 现货)/σ_模型` 的 sd 按距收盘分桶是
  45–60m **1.072** / 30–45m 1.059 / 20–30m 1.025 / 10–20m 0.951 / 5–10m **0.880**。
  模型在小时早段偏窄、晚段偏宽，不是随机噪声。
- 机制: BTC 在小时内不是匀速走的。每分钟 |对数收益| 中位从第 5 分钟的 **1.61bp** 降到
  第 55 分钟的 **1.21bp**。trailing-15m 估计器在第 5 分钟量的主要是**上一小时的尾部低波动**、
  却用到本小时的高波动开盘段（跑冷）；在第 50 分钟量的是中段高波动、用到收盘前的静段（跑热）。
- 决策: `model.variance_seconds(seconds, minute)` = 019 的 TWAP 有效方差时间 × 按小时内
  profile 重整的比例。权重是每 10 分钟一块的 `E[r²]`（均值归一）：

  ```
  HOUR_VARIANCE_WEIGHTS = (1.14, 1.03, 0.94, 1.16, 0.94, 0.78)
  ```

  `minute=None` 时比例恒为 1.0——**任何拿不到小时内位置的调用点行为完全不变**。
  `strategy.hour_minute()` 只对 `is_hourly_window` 为真的合约给位置，15m / 日盘 / 周盘
  一律走原来的平坦路径。
- 口径纪律: 权重**只在日历前半**（772 小时）估计，**在后半验证**。
  - 样本外 Brier **0.08829 → 0.08799**（可交易切片 ≥180s 同为 0.08799）
  - 样本外 realised/model sd 的平均 |误差| **0.053 → 0.038**
  - **60 点逐分钟 profile 的样本外分数是 0.08800——和 6 块的 0.08799 一样。**
    形状是结构性的，不是拟合出来的；六个数就是诚实的分辨率。
- 幅度诚实说: 0.34%。**这不是边，是把模型算对。** 增益集中在最后十分钟，而本仓库
  `swing_min_seconds=180` / `flatten_seconds=40`，本来就不在那一段交易。它的价值是让每一道
  读 `model_p` 的门（`swing_min_gap`、`invalidate_p`、`lock_min_p`）少一点系统性偏差。
- 副产物（比修正本身更重要）: 016 的 cushion 信号随模型变准而**塌向零**
  （+0.53¢ → +0.47¢ → **+0.07¢ t=0.09**）。所谓「盘口没跟上模型」里相当一部分是模型自己错。
- 不因此回退: 0.45 成交门、吃 taker、p=0.30、整条 loop 切 live、「看见 32–42¢ 就挂」。

### 022 Kalshi 已结算记录只保留约 66 天——历史不可回填，只能往前攒

- 日期: 2026-09-03
- 状态: accepted（事实登记 + 运维要求）
- 事实（二分查找定位）:
  - `/events?series_ticker=KXBTCD&status=settled` 仍然**列出 8000 个**事件，最早回到
    **2025-08-06**。看上去有一年历史。
  - 但 `GET /events/{ticker}` 对其中 **6443 个**返回 **markets 为空**：没有 `result`、
    没有 `expiration_value`、没有 candlesticks。`/live_data` 同样没有。
  - 边界很硬：`KXBTCD-26JUN2819`（66 天）空，`KXBTCD-26JUN2820`（66 天）有。
    **可取回的历史 = 1557 小时 ≈ 65 天。**
- 含义:
  - 本仓库 2026-09-03 建的那份 1546 小时数据集**不是「一年历史里的一段样本」，它就是公开
    记录的全部**。任何人在这个 API 上都拿不到更多。
  - 016 的「≥130 天」重开条件**无法回填**，只能从今天起往前攒，约 65 天后达到。
  - **任何超过 66 天没被归档的时段，永久丢失。**
- 运维要求（新）:
  - `research/pull_hourly.py` 本来就是增量的（已存的 event 跳过），所以它同时是归档器。
    **每天跑一次**；`--coverage` 报已存范围、窗口内缺口、以及一周内将要滚出窗口的时段。
  - 断档超过 66 天 = 那段样本再也补不回来。这不是「以后补」的事项。
- 顺带: 现货代理（Coinbase 1m）也救不了回填——旧事件连结算值都没有，代理只能给现货、
  给不了 `result`。（代理本身质量倒是够：66 天重叠上 BRTI − Coinbase 均值 **+0.26bp**、
  sd 6.0bp，没有 15 分钟仓 phase44 那种 +8.7bp 系统基差；但没有结算真相就没有回测。）
- 产物: `research/pull_hourly.py --coverage`。

### 023 档位成交量不携带价格以外的信息（事实登记，非门）

- 日期: 2026-09-03
- 状态: accepted（事实登记）
- 立题: 数据库里本来就有每档每分钟的 `volume` 和 `open_interest`，从没测过。人手直觉是
  「量大的那一档是聪明钱在动」。
- 方法: 每个 (event, strike, side) 按卖一价带分组，再按**该档成交量相对本小时中位档**分成
  cold(≤0.5×) / mid / hot(≥2×)。吃卖一持到结算，聚类 t。
- 结论: **否定。** 同一价带内三档流量给出的校准几乎一样，且没有单调性：

  | 卖一带 | cold | mid | hot |
  | --- | ---: | ---: | ---: |
  | 0.10–0.30 | −2.20pp | −1.67pp | −1.81pp |
  | 0.70–0.90 | +0.94pp | +0.72pp | +0.84pp |
  | 0.90–0.99 | +0.17pp | +0.03pp | +0.18pp |

  每一格里 favorite–longshot 那条曲线都原样重现（018）。**量说不出价格没说过的话。**
  与 15 分钟仓 phase5 H4（`flow` 最大 ΔR²=0.0023）同向。
- 这是本轮数据里最后一个没试过的现成信号。再往下要的是 L2 深度或逐笔，本仓库没有采集器，
  且 15 分钟仓 phase45 已证秒级 lag=0，先验低。

### 024 `impulse` 没有方向性；005 的**理由**是错的，但它的**折扣**是对的

- 日期: 2026-09-03
- 状态: accepted（事实登记 + 对 005 / 008 / 015 的重新解释；**005 原文不改，门不动**）
- 背景: 之前测的是 coupon 这个**玩法**，从没测过 `impulse` 这个**信号**。005 的整句前提是
  「3 分钟 |impulse| ≥ $100 才算有方向」。1547 小时上直接量。
- A. 同向率（impulse 符号 vs 之后到结算的位移符号）:

  | 桶 | n | 同向率 | Wilson 95% 下界 |
  | --- | ---: | ---: | ---: |
  | \|imp\| ≥ $100 | 5798 | **48.34%** | 47.06% |
  | \|imp\| ≥ $200 | 878 | **44.53%** | 41.28% |
  | \|imp\| ≥ $300 | 226 | 44.69% | 38.35% |
  | $50–100 | 14431 | 49.41% | 48.60% |
  | $0–50 | 76515 | 50.53% | 50.18% |

  **门越紧，同向率越低。** ≥$200 时 44.5%，Wilson 上界都到不了 50%。这是弱**反转**，
  不是动量。顺向后续位移 $100–200 是 +7.6$（t=0.69）、$200–400 是 −1.6$（t=−0.06），
  两边都不显著。
- B. 买同向那一侧（吃卖一、持到结算），和 018 的无条件价带图对照:

  | 卖一带 | 同向条件下校准 | 无条件（018） | 差 |
  | --- | ---: | ---: | ---: |
  | 0.30–0.50 | **−4.38pp**（−6.03¢, t=−4.43） | −1.66 / −0.83pp | 差约 3pp |
  | 0.50–0.70 | **−2.73pp**（−4.39¢, t=−4.75） | −0.63pp | 差约 2pp |
  | 0.10–0.30 | −1.90pp（−2.90¢, t=−1.97） | −2.17pp | 相当 |
  | 0.70–0.95 | +0.40pp（−0.47¢, t=−0.68） | +0.74~1.33pp | 略差 |

  **「跟带子走」在卖一上不是中性，是负的。** 市场对 3 分钟那一跳**反应过度**，同向侧因此
  比同价带的平均合约还贵 2–4pp。
- 和 015 / 017 拼起来才是完整的画:
  - 同向侧在**卖一**是 −4.38pp（本条）。
  - 同向侧在 **25¢ rest**（比 32–42¢ 的书低约 8¢）持到结算是 **−0.86¢ t=−0.33，公道**（017 第 2 节）。
  - 所以 **那 8¢ 折扣正是在为这份反应过度买单。**
- 对三条既有门的重新解释（**都不改门，只改理由**）:
  - **005**: 「有方向」这个理由**错**。但它要求同向 + 折扣挂单这个**做法**是对的——折扣才是
    赚的那部分，方向不是。以后不要再用「有方向」为 005 辩护。
  - **008**「不把成交门放到 0.45」**得到定量支持**：从 25¢ 往卖一挪，每挪 1¢ 大约丢
    **0.5pp**（4.38pp / 8¢）。放到 0.45 等于把折扣全交出去。
  - **015** 的「rest 0.25 等价于只成输单」这半句**被否掉**（017 已量出成交是公道的）；
    本条补上为什么：折扣不是在筛输单，是在为同向侧的反应过度收租。
- 不因此回退: 0.45 成交门、吃 taker、p=0.30、整条 loop 切 live、「看见 32–42¢ 就挂」。
  本条如果有什么倾向，是**更不该**放宽 rest。

### 025 `lock_hold` 66 天只触发 4 次，而且它的三道门里两道是冗余的

- 日期: 2026-09-03
- 状态: accepted（事实登记；**门不动**）
- 背景: `lock_hold` 是本仓库的旗舰 play（lock.md 整页），但和 `impulse` 信号一样，
  **从没被直接量过**。1553 小时上按原文口径跑漏斗。
- 漏斗（每档每分钟算一次）:

  | 关卡 | 剩下 |
  | --- | ---: |
  | 扫过的档位 | 820,165 |
  | σ_cushion ≥ 3.2 | 131,727 |
  | 再且 p ≥ 99.8% | **131,727** |
  | 再且 ask ≤ $0.82 | **4** |
  | 再且 EV ≥ 20% | 4 |

- 两个发现:
  1. **`p ≥ 99.8%` 这道门一个都没挡。** σ≥3.2 在数学上已经蕴含 p≥0.998（3.2 个标准差的
     单尾就是 99.93%），所以它是冗余的。`EV ≥ 20%` 同理——在 p≥0.998 下，`ask ≤ 0.82`
     已经蕴含 EV≥20%。**三道门里真正生效的只有一道：`ask ≤ $0.82`。**
  2. 它砍掉 131,723 / 131,727 = **99.997%**。66 天 4 笔，**0.06 笔/天**。
     lock.md 里那句「纯 `lock` 的常见行为是挂着等，空仓也是正确的」被量化了：
     不是「常见」，是**几乎总是**。
- 这不是说门错了。20% 的 if-win 要求在 p≈1 的档位上本来就几乎不可能被满足——
  这是 002 的算术后果（`b≥0.20` ⇒ 买价 ≤$0.82，而 σ≥3.2 的档位盘口就是 0.95–0.99）。
  **登记这条是为了让「lock 很少开」这件事有数字，不要再被当成盘口异常。**
- 放宽 `ask` 上限的对照：**同样不成立，已结案。**
  - **先记一个我自己的计数错误。** 初扫时我报了「n=1275、+3.34¢、t=2.62」，并称它是本轮
    唯一两半同号且 t>2.5 的形状。**那个数字是重复计数吹出来的**：初扫把**同一档在不同分钟
    各算一次**，一个档位在 σ≥3.2 上挂 40 分钟就贡献 40 行。`study_rule.py` 按每
    (event, strike, side) **只取一次进场**，那才是正确口径。
  - 正确口径下（σ≥3.2、ask 0.50–0.95、持到结算）:

    | 口径 | n | 每张净 | t |
    | --- | ---: | ---: | ---: |
    | 全样本 | 224 | +1.89¢ | **1.35**（CI [−0.85, +4.64] 含零） |
    | 日历三分之一 | | +1.14 / +3.28 / +1.27 | 三段同号，但都不显著 |
    | +1¢ 滑点 | 224 | +0.96¢ | 0.68 |
    | **+2¢ 滑点** | 224 | **+0.02¢** | 0.01 |
    | 成交量 ≥1000 | 145 | +2.46¢ | 1.59 |

    打乱结算的零假设过了（观测 +1.89¢ vs 零假设 95 分位 −33.2¢），所以不是纯噪声；
    但 1 张 **$0.06/天**、两跳滑点归零、CI 含零。**和 016 同一堵墙，不接线。**
  - 教训入账：**任何按「档位 × 分钟」扫出来的 t 都要先问一句「同一个档被数了几次」。**
    这条已经骗过我一次。研究脚本里 `run_rule` 的去重是对的，临时扫描没有。

### 026 `swing_t` 进场公道、出场栈每张扣 4.2¢（017 的第四次独立复现，t 最大）

- 日期: 2026-09-03
- 状态: accepted（事实登记）。`swing_t` **默认本来就关**，本条不改任何门。
- 背景: `swing_t` 是最后一条从没被直接量过的自有门。原文口径：ask 0.18–0.72、p≥55%、
  p−ask≥8%、|S−K|≤$600、剩余≥180s。按 025 的教训，每 (event,strike,side) 只取一次进场。
- 漏斗（每档每分钟，去重前）:

  | 关卡 | 剩下 |
  | --- | ---: |
  | 扫过 | 626,670 |
  | 距离 ≤$600 | 561,216 |
  | ask 0.18–0.72 | 257,372 |
  | p ≥ 0.55 | 68,418 |
  | **p−ask ≥ 0.08** | **2,168** |

  真正卡住的是 `p−ask≥8%`（砍掉 97%）。`p≥0.55` 也在做事，距离和价带几乎不挡。
- 结果（2168 笔，952 簇）:

  | | 每张净 | t |
  | --- | ---: | ---: |
  | 进场 → **持到结算** | +1.05¢ | 1.05（CI [−0.91, +3.00] 含零） |
  | 进场 → **原文出场栈**（10–50% 带 / 4¢ 回撤 / −12% 止 / 12pp 淡化） | **−3.18¢** | **−8.58** |
  | 日历前半 / 后半（持到结算） | +2.09 / **+0.01** | 后半正好是零 |

- 两条:
  1. **进场公道，不是边。** 校准 +2.66pp 看着像有东西，但净值 +1.05¢、t=1.05、CI 含零，
     后半 +0.01¢。和阶梯上其他所有东西一样撞同一堵墙。
  2. **出场栈每张 −4.2¢，t=−8.58——本轮所有测量里最大的效应。** 这是 017 的
     **第四次独立复现**：coupon 成交 −3.28¢（t=−4.99）、cushion 进场 −2.13¢（t=−4.73）、
     15 分钟仓 phase4 G1 / phase12b，现在加上 swing_t 的 −4.2¢（t=−8.58）。
     四组互不相干的进场、两个仓库、两种合约，方向完全一致。
- **留给用户的一条**: 017 只把「持到结算」接到了 `impulse_wait`（以及结构上豁免的
  `lock_*` / `cushion_hold`）。**`swing_t` / `impulse_t` 仍然跑完整出场栈**——它们默认关，
  所以现在不亏钱；但**一旦有人 `--playbook swing`，跑的就是一个每张 −4.2¢ 的构造**。
  要不要把 017 的处理推广到这两条，是改门，走用户裁决，我不单方面动。
- 不因此回退: 0.45 成交门、吃 taker、p=0.30、整条 loop 切 live、「看见 32–42¢ 就挂」。
