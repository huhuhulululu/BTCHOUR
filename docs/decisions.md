# Decisions（小时盘冻结门）

> 追加写入，不修改已有条目。格式: `### [编号] [标题]`
> 这不是 15 分钟盘的 ADR 全集。只记本仓库已经对用户生效的门。改门必须走 ARCH 草稿 → ADV → CMD → 用户。

---

### 001 只做 BTC 小时盘 `KXBTCD`

- 日期: 2026-08-25
- 状态: accepted
- 背景: Kalshi 还有 `KXBTC` 区间盘、`KXBTC15M` 15 分钟涨跌。
- 决策: 主路径只做下一个整点收的 `KXBTCD-{YY}{MON}{DD}{HH}`。午夜是 `AUG2900`，不是 `AUG2824`。
- 理由: 用户要的是小时盘。15 分钟盘研究在 `huhuhulululu/kalshi`。

### 002 计分 `EV = p · b − (1 − p)`

- 日期: 2026-08-25
- 状态: accepted
- 决策: 所有进场/对照用同一公式。~20% 是过滤，不是每笔保证。

### 003 默认 paper，禁止整条 loop 切 live

- 日期: 2026-08-26
- 状态: accepted
- 决策: `BTCHOUR_MODE=paper`。`GET /exchange/status` 的 `trading_active=true` 只是开盘门，不是切 live。
- 理由: 用户明确拒绝「整条 loop 切 live」。

### 004 实盘只允许 `live_one` 一张 post-only

- 日期: 2026-08-28
- 状态: accepted
- 决策: 每次最多 1 张真单。已有 1 张 resting 不再挂第二张。撤单必须带 `exchange_index`（`KXBTCD` = 2）。
- 理由: 必须一张一张实测；同时乱挂会一边倒吃瘪。

### 005 coupon 门（`952cb97`）

- 日期: 2026-08-29
- 状态: accepted
- 决策:
  - 涨 = 3 分钟 `|impulse| ≥ $100` 才挂 YES（28–42¢）
  - 跌 = `impulse < 0` 且 32–42¢ 看见才挂 NO（29¢ 飞刀不接）
  - 静 / 弱阳不挂
  - 成交 / 淡化 / 反手撤仍要同向 `|impulse| ≥ $100` 且 ask 还在 rest
  - rest $0.25；不吃 taker；0.50–0.70 垫档不上
- 理由: 「看见 32–42¢ 就挂」在 `AUG2911` +0 / ATM 0.42 停 34 分钟，`t_wait_stop` −85%。「3 分钟 $100 才挂」又找不到点。两头都不回退。

### 006 亏了空一小时；0 成交不叠 skip

- 日期: 2026-08-26
- 状态: accepted
- 决策: 亏了下一小时整小时不做 T。连亏不叠。0 成交不是亏损。

### 007 clip 之后本小时不 hop

- 日期: 2026-08-27
- 状态: accepted
- 决策: 第一笔 coupon clip 后这小时不再开新行权价、不翻面。

### 008 不靠放宽门成交

- 日期: 2026-08-26
- 状态: accepted
- 决策: 不把成交门放到 0.45；不吃 0.45–0.70 taker；不放宽 `p=0.30`；不回退成「不挂 YES」。
- 理由: `flex_cheap` / `flex_nowait` 对照更亏或不是现行路径。回放绿不是达成。

### 009 核盘只用表格、纽约时间、中文

- 日期: 2026-08-28
- 状态: accepted
- 决策: `python3 -m btchour board`。不画图。不提交 `latest.json` / `index.json` / `probe.json` / `.env` / `data/` / keys。整点 sweep 只强制提交 `catalog/snapshot/sweep.json` + `replay.json`。

### 010 用户只跟 CMD 说话；每笔实盘单必须复盘

- 日期: 2026-08-29
- 状态: accepted
- 背景: 用户裁决：以后只跟主控沟通，由 CMD 做角色调用和分工。第一条指令是每一笔实盘单都要仔细分析。
- 决策:
  - 对外频道只有 CMD。专员不对用户直接说话。
  - 每笔 `live_one`（成交或撤单）CMD 必须派 **LEARN**（像不像金带子）和 **RISK**（该不该挂），抽查后用表格复盘。
  - 复盘不是改门许可。改门仍走 LEARN→ADV→用户。
  - 样本：372 撤、373 stop、374 clip。漏复盘 = 违规。

### 011 小时盘阶梯操作权高于 15 分钟研究仓

- 日期: 2026-08-29
- 状态: accepted
- 背景: 用户 13:47 EDT：每个小时那么多盘，不可能机会那么少。本仓库敏锐度 / 实际操作可能性远比另外的仓库（`huhuhulululu/kalshi`，15 分钟盘，结论偏无波动率溢价、坐等）更厉害，要把这个更加提权重视。本小时 `AUG2914` 快照 **188** 档，只做成 1 笔噪声 live（375）。
- 决策:
  - 小时盘整条近 ATM 阶梯（距离 **$600**，活盘 **32–42¢**）是**工作盘**，不是 15 分钟仓那种「没边就坐着」。
  - **空仓而近 ATM 阶梯活着 = 策略失败**，不是「没机会」。
  - CMD 心跳必须报：整点档数 / `$600` 内 / 32–42¢ 活档。不能只报空仓。
  - 操作强度优先于 15 分钟研究姿态。不把 15m 坐等抄过来。
  - 本小时已 clip **不 hop**（007）。用满阶梯的提案给下一小时。
  - **不因此回退**：看见 32–42¢ 就挂、成交门 0.45、吃 taker、放宽 `p=0.30`、整条 loop 切 live。
- 理由: 一百多档近 ATM 阶梯是本仓库相对 15m 仓的优势。机会少是策略没用上阶梯。

### 012 CMD 要最聪明、最灵活、产出最高

- 日期: 2026-08-29
- 状态: accepted
- 背景: 用户 14:26 EDT：你应该是最聪明、最灵活，以后也是你产出最高。
- 决策:
  - 这张小时盘桌由 **CMD** 主控。聪明 = 用满近 ATM 阶梯、先自己核盘再派专员；灵活 = `flex`、跟带子换边、3 纸 / 1 真用满；产出最高 = 真动量小时打出人手那种 clip，压过 15m 研究仓。
  - 产出用「工作盘有没有用上」衡量（011），**不用噪声单刷量**，也不用坐等充聪明。
  - CMD 心跳和用户指令里先自己判阶梯；专员只做 010 复盘和改门对抗。
  - **不因此回退**：看见 32–42¢ 就挂、成交门 0.45、吃 taker、放宽 `p=0.30`、整条 loop 切 live。
- 理由: 用户要的是这张桌的敏锐度和实际操作，不是另一个仓库的坐等。

### 013 真单出场必须交易所成交后才落账

- 日期: 2026-08-29
- 状态: accepted
- 背景: 378 第一次 flatten IOC 买 YES 0.64 被撤（盘口更差），`fill_count=0`，引擎仍 `paper_close`。用户在 Kalshi 看见仓还在。
- 决策:
  - live flatten **按现价交叉**（买 YES ≥ ask，卖 YES ≤ bid），不按 clip 标记价傻挂。
  - `fill_count=0` **不得** `paper_close`。账上的出场价用交易所 `average_fill_price`。
  - 每轮对账：sqlite 已平但交易所还持有我们的 `live_one` → 再 flatten。残留未平禁止再挂第二张。
  - 开仓仍不吃 taker。平仓 IOC 可以交叉。
- 理由: 账平仓在是愚蠢错误，不是策略。

### 014 停 `live_one`（安全停机补丁）；停机期间跌侧挂门 |impulse| ≥ $100

- 日期: 2026-08-30
- 状态: accepted
- 定性: **安全停机补丁**。不是金带改进，不是 005 的可采纳修订。006 / 007 / 008 一字不动。
- 背景: 用户 07:42 EDT「如果没有新的改善或变化，就不要做了」；07:48 EDT「所有角色都参与」。今天 382–388 七张真挂，挂时动量 −15 到 −0.2，无一张 NO |impulse| ≥ $100，无一张是金带子那种 dump。live_one 17 张、成交 14 张净 +0.0677 是噪声。引擎事实：live_one 由交易所成交直接 promote，005 的同向 $100 成交门只对纸盘 rest 生效。
- 全员: LEARN 可采纳候选、不是达成。ADV 攻得倒、降级为停机补丁。ARCH 推荐 D。RISK 黄、现在停 live。MON 无滞留。ENG 102 绿。QA 可合并。DOC 对齐 plays.md。RPT=CMD。
- 决策:
  - `live_one` 默认 **False**。真单通道保留（004 不撤），但默认不往交易所送。
  - 停机期间跌侧挂门：挂 NO 要 `impulse < 0` **且** `|impulse| ≥ $100`，且 32–42¢ 看见。涨跌同门。YES 仍 28–42¢，不回退「不挂 YES」。005 原文不改；重开 live 时必须重新论证 $100。
  - live_one 真单淡化即撤（`wait_fade`）；纸盘淡了仍坐。该分支在 `live_one=False` 下不可达，回放不算验证。
  - 重开 live_one 需同时满足并经用户批准：① 纸盘出现 AUG2608 型（挂时同向 |impulse| ≥ $100、带内、真成交、10–50% clip）；② 015 有 ADV 对开结论；③ `wait_fade` 经真单或专测验证。
  - 关停前置：交易所零持仓零 resting（013 对账当前挂在 live_one 开关下）。
- 后果: 跌侧挂单变少；384/386 那种未到 $100 或 ask≤rest 的真砸也会被挡。不得把「不挂了所以不亏了」写成达成。停机期间金带子出现也不自动发交易所。
- 不因此回退: 成交门 0.45、吃 taker、放宽 p=0.30、整条 loop 切 live、「不挂 YES」、看见 32–42¢ 就挂。
- 理由: 送的是同型噪声单。先关真钱通道。

### 015 挂单语义题（占位，未决）

- 日期: 2026-08-30
- 状态: proposed
- 题面: 现行 rest 0.25 语义要求市场先给 8¢ 逆向折扣，等价于「只成输单、排除赢单」。AUG2520 人手是在砸盘启动时买在市场正在交易的价位。这是构造性错位，不是阈值松紧。
- 不预设结论。不得在本题下夹带 0.45 / taker / p=0.30 / 整条 live。

### 016 先修计量：回放成交、结算模型、研究框架（提案）

- 日期: 2026-09-18
- 状态: **proposed**（代码里已落的只有计量部分，见下「已生效」）
- 背景: 015 的挂单语义题一直没数。合成盘对照（每行 200 小时）给了三条：① `fair` 世界里旧回放成交模型打出 90% 胜率 / t=7.09，而那个世界按构造谁都不该赚钱；② 换成诚实成交后 `impulse_wait` 每小时成交 0.02–0.04 笔；③ 成交那一刻付 0.25 买到的东西，公允盘里值 0.235，反应过度的盘里值 0.196。
- 决策（已生效，属计量修正，不是门）:
  - `Settings.replay_wick_fill` 默认 **False**。回放要求收盘报价落在 rest 上才算成交。`BTCHOUR_REPLAY_WICK_FILL=1` 可复现旧数字。**纸盘 loop 不受影响**（它轮询 + 要求 rest 上有真实打印）。
  - `digital_prob` / `sigma_cushion` 按 `KXBTCD` 的 60 秒均价结算算方差：`τ≥60s → τ−40s`，`τ<60s → τ³/(3·60²)`。`twap_seconds=0` 回到旧模型。
  - 新增 `btchour/research/`：合成盘、自举区间、t 值、样本外切分、真 tape 归档。`python3 -m btchour research …`。
- 决策（提案，等用户）:
  - 015 的答复方向是 **C（贴价挂，赚价差而不是要 8¢ 折扣）**，但前置是归档里有 ≥300 小时真 tape，且样本外 `maker_edge_at_fill` 为正、95% 区间不跨 0。**在此之前不改挂单语义。**
  - 「单笔 EV ≥ 20%」改成「每笔小正 EV × 笔数 + 风险上限」是 D，前置是模型对真实结算的 Brier 跑赢市场中价。**在此之前不改门。**
- 后果: 历史上所有 `sweep` / `replay` 的绿数字（`6/6/+8.32`、`5/5/+7.57` 这一类）**不能再当证据**。重跑要用新默认。
- 不因此回退: 成交门 0.45、吃 taker、放宽 p=0.30、整条 loop 切 live、「不挂 YES」、看见 32–42¢ 就挂。005 / 006 / 007 / 008 / 014 一字不动。
- 阻塞: 真 tape 需要能连 `external-api.kalshi.com`。草稿: `ops/notes/adr-draft-20260918-016.md`。

### 017 D 作废：模型跑不赢市场中价（真 tape 判定）

- 日期: 2026-09-18
- 状态: accepted（判定）/ proposed（波动率地板）
- 背景: 016 的阻塞解除，出网白名单放行 `external-api.kalshi.com`。归档 **92 小时**真实
  tape（`26SEP1414`–`26SEP1809`，48568 个近 ATM 读数），跑 016 自己定的决定性检验。
- 判定:
  - 模型 Brier **0.11519**，市场中价 **0.10319**。按小时聚类自举，差 **+0.01199**，
    95% 区间 **[+0.00844, +0.01551]**，不跨 0。
  - 门真正开火的地方（分歧 ≥5¢，33252 个读数）差距更大：**+0.01702**，区间
    **[+0.01216, +0.02150]**。
  - 四个分歧桶**全部**中价更准。24 小时子样本同结论同量级，不是时段巧合。
  - 波动率扫了 9 档（地板 0.20–0.55、realized、固定值、×0.8/×1.3）。**没有一档让模型
    赢**，最好的一档（realized 无地板）差 +0.00077，只是逼近打平。
  - 样本外：`swing` 92 小时两段都亏（−10.49 / −9.65，合计 −20.14 / 37 笔）。24 小时窗口
    里那个 t=3.18 是窗口挑出来的。默认 `flex` 92 小时动手 1 次。
  - 因此 **016 的 D（把「单笔 EV ≥ 20%」改成「小正 EV × 笔数 + 风险上限」）作废**。它的
    前置是模型 Brier 跑赢中价，该前置被真数据证否 —— 是证否，不是「还没测够」。
  - 连带作废: 任何以「`digital_prob` 看见了盘口没看见的东西」为前提的 taker 入场规则。
    门开火的那 75% 读数里，开的是模型误差。
- 已生效（计量，不是门）:
  - `.gitignore` 改 `data/*` + `!data/archive/`，tape 归档进版本库。容器是临时的，归档不
    进 git 就等于每个会话从零开始 —— 这是本仓库从来没攒够样本的直接原因。
  - `KalshiClient._request`: GET 撞 429/5xx 指数退避重试（认 `Retry-After`），请求间隔
    下限 60ms。签名 POST 不重试（重发订单会开重仓）。`tests/test_kalshi_retry.py`。
- 提案（等用户批，本线程不自行改默认）:
  - `annual_vol` 地板 0.55 → realized 不设地板（或地板 0.20）。依据见
    `catalog/research/findings.md` 第 2 节：地板把 p 拽向 0.5，中价桶 0.9–1.0 真实频率
    0.979、中价报 0.958、模型报 0.865。
  - **这条会动门**: 分歧 ≥5¢ 的开火率 75.3% → 20.0%，engine 出手会少很多。按 016 的规矩
    走提案。
  - 改完模型也只是**打平**中价。修它是为了不再自伤，不是为了拿优势。
- 不因此回退: 成交门 0.45、吃 taker、放宽 p=0.30、整条 loop 切 live、「不挂 YES」、看见
  32–42¢ 就挂。005 / 006 / 007 / 008 / 014 / 016 的计量部分一字不动。
- 下一步只剩一条: 015 的 **C（贴价挂，赚价差而不是预测方向）**。它是唯一没被这次数据否掉
  的方向，因为它不要求模型比市场准 —— 数据恰恰说明中价是准的，价差才是会付钱的那个量。
  016 给它的前置是 ≥300 小时真 tape + 样本外 `maker_edge_at_fill` 为正、区间不跨 0。
  **后半句作废**，见下。
  **当前归档 92 小时；Kalshi 分钟 K 线只回溯约 4 天，补不出来，只能持续攒。这是现在唯一
  的瓶颈。**
- 连带修正 016: `maker_edge_at_fill = fill_model_p − paid`，`fill_model_p` 就是本决定判死
  的 `digital_prob`。拿它当 C 的判据是循环论证，而且偏差单向：0.55 地板把 p 拽向 0.5，在
  0.25 挂单会成交的便宜那侧模型读数偏**高**。92 小时真 tape 实测（findings 第 7 节）：中价
  0.15–0.20 档 016 口径 **+0.023（通过）**，诚实口径 **−0.082（每张亏 8 分）**；0.20–0.25
  档 **+0.063（通过）** vs **−0.055**；合计 0.20–0.35 是 +0.097 vs +0.011。偏差一律
  +0.06~+0.12 的乐观方向。**照 016 原文验 C 会再生产一次假绿**，和旧 wick 成交模型当年造出
  90% 胜率是同一个毛病。
  - 已生效: 新增 `maker_edge_realized`（`research/evaluate.py`），不碰模型 —— 买入那侧的
    结算值 1/0 减挂单含费成本；`replay.py` 的 `_stamp_settlement` 在两个成交出口记
    `settles` / `settle_value`。两个指标并排出：合成盘看 `at_fill`，真 tape 看 `realized`。
  - C 的前置改为: ≥300 小时真 tape + 样本外 **`maker_edge_realized`** 为正、区间不跨 0。
- 不采纳: 尾部保险（0.97–0.99 买近确定的 YES）。24 小时看着每笔 +1~2% 且全胜，样本翻到
  92 小时后大部分区间开始跨 0；ask=0.99 费后需 p ≥ 0.9907，要约 800 小时才判得动；一次
  亏损要 106 次盈利补回来，风险形状与「稳定」相反。

### 018 波动率地板已改；C 预判为负；建议停止在这张桌子上找 alpha

- 日期: 2026-09-18
- 状态: accepted（波动率、C 预判）/ 建议（停止方向）
- 背景: 用户对 017 留的两个问题答「你来优选」。两个都做了测量再定。

#### 决策一：波动率地板改了（已生效）

`annual_vol` 一个数同时当「测得波动率的地板」和「测不出时的兜底」。拆开：

- `Settings.vol_floor = 0.20`（新）—— 地板。`realized_annual_vol` 内部 clamp 在 0.25，
  所以这个地板实际不起作用，模型用它测到的值。`BTCHOUR_VOL_FLOOR` 可覆盖。
- `Settings.annual_vol = 0.55`（不变）—— 只在测不出 realized 时兜底。测不出时偏高是
  安全方向，偏低不是。

效果（92 小时）: 模型 Brier **0.11519 → 0.10396**，中价 0.10319，基本打平。分歧 ≥5¢ 的
读数占比 **75.3% → 20.5%** —— 原来那些「分歧」大部分是地板造的假象。

**017 的判定不变**: 剩下的真分歧里（≥5¢ 两行）中价照样赢。模型少犯错了，但它仍然没有
市场不知道的信息。**这是计量修正，不是拿到了优势。**

- 副作用（记录在案）: `swing` 因为 p 变锐利、`p ≥ 52%` 更容易过，开火从 37 笔涨到 55 笔，
  仍然亏（−19.14）。**校准变好不等于策略变好。** 默认 `flex` 基本不受影响（1 笔）。
  `swing` 在两种波动率设置下、样本外两段全亏 —— **不要用**。

#### 决策二：C 的前置不必等 300 小时，现在就能预判，结果为负

决定 C 的那个数是**价差**，它现在就能精确测。92 小时 / 40036 个观察：

- 假设永远成交（C 的**上限**，无逆向选择）: 买价边际 **+0.0157**，区间 [−0.0220, +0.0533]
- 只算真被打到的: **−0.0098**，区间 [−0.0452, +0.0262]
- **逆向选择代价 = 2.55 分；可赚价差 = 1.06 分。逆向选择是整条价差的 2.4 倍。**
- 按价差宽度、按剩余时间切开没有一个角落为正；价差 ≥2.5 分的只有 22 个观察，**没有宽
  价差角落可躲**。

PnL 的区间是宽的（92 小时的二元市场必然如此），**但结论不靠那个区间**：价差 1.06 分是
精确量，C 要成立需要逆向选择低于 1.06 分，实测 2.55 分。再攒 300 小时只会把一个已经在
错误一侧的点估计的误差棒收窄。

#### 结构账（为什么两条路死因相同）

实时盘口 `KXBTCD`: 平均价差 1.18 分，ATM 1.20 分。Kalshi taker 费 `0.07·p·(1−p)`，
ATM = **1.75 分**。**价差 / 费 = 0.69 —— 吃一次单的手续费比整条买卖价差还贵。**

- 吃单: 1.75 分费 + 约 0.6 分半价差 ≈ 2.35 分成本，需要 ≥2.35 个百分点的真优势；实测模型
  比市场差 1.2 个 Brier 点。
- 挂单: 费 0，毛赚 1.06 分，逆向选择 2.55 分。

**同一个死因：价差比交易它的成本还薄。** 不是门没调好，是这个尺度上没给外部参与者留位置。

#### 建议（要用户拍板）

停止在 `KXBTCD` 小时盘上继续调门找 alpha。D 已证否，C 预判为负，两者的成本结构是同一个。
继续投入的期望产出是「用更窄的误差棒确认还是不行」。

- 不建议: 再攒 300 小时验 C；再调 `impulse_*` / `swing` / `lock` 任何阈值；重开 `live_one`。
- 若仍要继续，值得看的只有结构不同的东西（按成本排序）: ① `KXBTC` 区间盘价差 3.06 分，
  是 `KXBTCD` 的 2.6 倍，值得先花一天量一下它的逆向选择；② 跨场馆价差；③ 做市返佣身份。
  **这三条都还没测，不是结论，是候选。**
- `live_one` 保持 014 的关停。本决定不改任何门。

- 不因此回退: 成交门 0.45、吃 taker、放宽 p=0.30、整条 loop 切 live、「不挂 YES」、看见
  32–42¢ 就挂。005 / 006 / 007 / 008 / 014 / 016 计量部分 / 017 一字不动。

### 019 `KXBTC` 区间盘：宽价差属实，边际为 0，容量可忽略——关闭该候选

- 日期: 2026-09-18
- 状态: accepted
- 背景: 018 把 `KXBTC` 列为第 1 候选（实时价差 3.06 分 = `KXBTCD` 的 2.6 倍）。用户答
  「测完」。抓了 32 小时 `KXBTC` tape（`data/kxbtc/`，另写抓取，仓库原来只认 `KXBTCD`
  ticker），14285 个 (分钟, 档位) 观察。
- 计量修正（重要，会影响别处）:
  - 成交代理「下一分钟最优买价 ≤ 我的挂价」**在流动性差的盘口会凭空造出成交**：报价可以在
    无任何成交时漂下来，而这些幻影成交是**良性**的，于是**压低逆向选择**。
  - 代理判成交的 bar 里真有成交量的比例：`KXBTCD` **91.0%**，`KXBTC` 只有 **35.1%**。
  - 结论：**流动性差的市场必须要求成交 bar 有真实成交量**。`KXBTCD`（第 9 节 / 018）不受
    影响，代理在那里是可靠的。
- 判定:
  - `KXBTC` 每张边际：上限 +1.16 分 → 旧代理 +0.46 分 → **要求真成交 −0.03 分**
    （区间 [−1.61, +1.68]）。**那个 +0.46 是代理造出来的。**
  - 结构比 018 猜对了：`KXBTC` 价差 2.58 分 > 逆向选择 1.18 分（`KXBTCD` 是 1.07 < 2.44）。
    **这是两张桌子真实的结构差别。** 但价差覆盖住逆向选择之后剩下的 1.40 分，被排队、
    双边报价、出场成本吃光，**实测净边际是 0**。
  - 容量：近现货 26 档每小时成交 **27,320 张 / $7,029 名义**，是同口径 `KXBTCD`
    （1,798,929 张/小时）的 **1.5%**。宽价差和低成交量是同一件事的两面。
    最乐观一格（吃下全市场 25%、边际 1 分，两个都不成立）是 **每小时 $68**。
  - **关闭 018 的第 1 候选。** 不建议投入。
- 仍未测: 018 的第 2 条（跨场馆价差）、第 3 条（做市返佣身份）。
- 不因此回退: 成交门 0.45、吃 taker、放宽 p=0.30、整条 loop 切 live、「不挂 YES」、看见
  32–42¢ 就挂。005 / 006 / 007 / 008 / 014 / 016 计量部分 / 017 / 018 一字不动。
  `live_one` 保持 014 的关停。本决定不改任何门。
