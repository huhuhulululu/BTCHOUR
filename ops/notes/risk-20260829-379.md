# RISK 姿态报告 — 379 AUG2919 NO T78099.99（2026-08-29，成交后被 013 reconcile 误平，pnl −0.0132）

角色: RISK（decisions 010 触发；brief `docs/briefs/2026-08-29-RISK-379.md`）。只判「本小时（AUG2919）还挂不挂」「AUG2920 是否 skip」「无 $100 成交 + reconcile 误平是黄还是红」，不下单、不改代码、不改门、不切 live、不对用户播报。
事实来源: `/workspace/data/btchour.sqlite`（`trades#379` raw + `journal` 22:00:03–22:09:07Z）独立抽查；`python3 -m btchour board`（只读，009 允许，18:22:58 EDT 现场核对）；代码路径核对 `btchour/engine.py`（`leftover_live_one_positions` / `reconcile_live_one`，`git show 22e6172` 对比工作区未提交 diff）、`btchour/strategy.py`（`remember_session_exit` 817 行、`allow_session` 969–992 行、`remember_swing_exit`/`allow_swing` 803–899 行）；`python3 -m unittest tests.test_live_one`（只读跑测试，15/15 通过，含新回归测试）；`ps aux` 核对当前 `run --playbook flex` 进程（pid 293095，21:44 UTC 起）。对照 `ops/notes/risk-20260829-378.md`。

---

## 该不该挂（已发生，核实结果：挂单合规）

`trades#379` 挂单时刻 2026-08-29T22:00:19.614599Z（18:00:19 EDT），journal 60818 `impulse = -35.75`（brief 记 −36，一致），`ask = 0.33`，落在 NO 32–42¢ 带内，`rest = 0.25`，reason `dump_gap NO 看见 0.33 rest 0.25 动量 -36`。005 对 NO 的字面要求是 `impulse<0` 且 32–42¢ 看见——两条都满足，挂单本身不违规。

## 该不该成交：无 $100 确认，第 6 次坐实同一结构性缺口（黄，非新问题）

成交前最后一 tick（journal 60852，2026-08-29T22:02:35.953Z）：`impulse = -1.40`，`ask = 0.32`（≠ `rest = 0.25`）；`flatten_reconcile.response.ts_ms=1788040975425`（22:02:55.425Z）显示进场单已于此前 ~400ms 成交。005 的「成交仍要同向 `|impulse|≥$100` 且 ask 还在 rest」两条都未满足——这与 373/374/375/377/378 同属一个已知结构性缺口（post-only rest 被交易所真实撮合，内部门不对交易所生效），**379 是第 6 次坐实**，不是新 bug，008 禁区不建议改门。

## 新问题：013 reconcile 把「刚成交的 working」当「已平的残留」误平——根因已定位，代码已有候选修复但未提交、运行中进程未生效

这是本次判断的核心，与前 5 次「无 $100 确认」不同类：

1. **交易所侧真实发生**：进场 NO 0.25 成交后 ~400ms，`reconcile_live_one` 把 `KXBTCD-26AUG2919-T78099.99` 上的交易所持仓当「sqlite 已平但交易所还持仓」的 378 型残留，用交叉盘 IOC 买 YES 0.75 强制平掉（`response.average_fill_price=0.7500`, `fee=0.0132`），`exit_note="reconcile leftover -1.00 at 0.25"`，`pnl=-0.0132`。此时 sqlite 侧该笔仍是**刚成交、未 promote 的 working 单**，不是「已平」——013 的字面决策是「sqlite 已平但交易所还持有」才该 flatten，379 不满足这个前提。
2. **代码核实**：已提交的 `22e6172`（21:44 UTC，早于 379 挂单 22:00:19Z）版本 `reconcile_live_one` 对 `leftover_live_one_positions` 命中的每个 ticker **没有检查 sqlite `status` 是否为 `closed`**，直接当残留 flatten——这正是 379 命中的代码路径。
3. **修复已在工作区，但未提交、未进运行中进程**：当前 `btchour/engine.py`（工作区未提交改动，`git diff` 可见）已加入 `if str(row["status"] or "") != "closed": continue  # 379: working just filled. refresh_working / manage_open own it.`，且 `tests/test_live_one.py` 新增 `test_reconcile_does_not_flatten_a_working_live_fill` 直接编码 379 场景，`python3 -m unittest tests.test_live_one` 15/15 全绿（含此新测试）。**但** `ps aux` 显示当前实盘循环进程 `python3 -m btchour run --playbook flex`（pid 293095）**21:44 UTC 就已启动**，早于此修复落到文件——Python 进程不会热重载源码，该进程内存里跑的仍是**未修复**的 `reconcile_live_one`。也就是说：磁盘上有修复且测试通过，但当前跑着的循环还没吃到它，同类误平在本进程重启前**理论上还能再发生一次**（需要下一次「working 刚成交、还没 promote」与「reconcile 扫到交易所持仓」在同一轮撞上）。
4. **这不是 RISK 的改代码/改门问题，是进程是否重启的问题**——按 RISK.md「工程故障伪装成风险 → 转 MON」路径，标记给 CMD 转 MON/ENG 确认是否要重启该 `run` 进程以吃到已写好且测试通过的修复；RISK 不建议、不执行重启。

## 本小时（AUG2919）还挂不挂：不挂——市场没有点，代码两道闸都挡着

1. **市场层没有点**：`board` 现场快照（18:22:58 EDT）：188 档，`$600` 内 12 档，YES 28–42¢ / NO 32–42¢ 活档均 **0**，姿态列「空仓·带塌了」，可挂 0。
2. **代码层双重挡**：`remember_swing_exit` 对任何退出（不分理由）无条件置 `dead=True`（`btchour/strategy.py:806`），`allow_swing` 对 `memory.dead` 且 `memory.play="impulse_wait"`（不在 `{impulse_t, swing_t}` 白名单）直接 `return False`（885–893 行）——本质是 007「clip 后不 hop」的同一套代码,对 flatten_reconcile 一视同仁。`allow_session`（969–992 行）额外挡：`session.last_loss_event == 本小时事件` 且 `last_play="impulse_wait"` 不在豁免集合，`return False`——同小时任何新 T 类挂单都被挡。三道（市场/hop/session）都指向同一结论：不挂,维持空仓观察，不改门。

## AUG2920 是否 skip：是——006 机械命中，不建议豁免

`remember_session_exit`（`btchour/strategy.py:817`）判据是 `lost = (pnl is not None and pnl < 0) or reason in {...}`——`pnl=-0.0132<0` 直接命中,不看 `reason` 字符串,不区分「市场亏」还是「reconcile 误平亏」。`skip_next=True`,`last_loss_event=AUG2919`；`refresh_session`/`allow_session` 在 AUG2920 变为当前小时时把 `skipped_event` 钉成 AUG2920,对该小时所有 T 类挂单 `return False`。`board`「近几小时」现场核对：`AUG2919 | 1 | flatten_reconcile | −0.0132 | skip下一小时`,与代码判定一致。

**这笔亏损的成因是 013 reconcile 的执行 bug,不是策略判断错误**——但 006 的字面文本只豁免「0 成交」（`0成交不是亏损`），没有豁免「非市场原因的执行亏损」。RISK 的职责是解读现有门,不是给 006 加没写出来的例外——所以**不建议豁免 AUG2920 的 skip**，按现状执行。是否要在 006 里补一条「执行层误操作导致的亏损不计入 skip 判据」的例外，这是改门,只能走 ARCH 草稿 → ADV → CMD → 用户,不是本报告能建议的范围。

## 无 $100 成交 + reconcile 误平：黄，不是红

红档定义（RISK.md）是「静/弱阳真单、第二张 live、吃 taker、整条切 live」——379 都不占：挂单是真跌趋势内的合规 NO 挂单（不是静盘/弱阳挂 YES），全程只 1 张 live，进场未吃 taker（reconcile 的 IOC 平仓交叉盘按 013「平仓 IOC 可以交叉」是批准过的例外，不算违规吃 taker），未切 live。判黄的理由有两层：（a）成交确认无 $100，是第 6 次坐实的已知缺口——黄档字面定义；（b）reconcile 把刚成交的 working 单当残留误平，是一个**新的、更severe 的执行层缺口**（不是「噪声打印仍在字面内」那种擦边,是安全网本身反手打了一枪），但因为根因已定位、修复已写好且测试通过（只是未提交/未进运行进程），且没有第二张 live、没有真吃 taker、没有整条切 live,不到红档的门槛——归入黄档的高严重度子类，标注给 CMD，不建议单独因此 ESCALATE。

---

## 总姿态：**黄**

## 调整菜单

- **1 维持现状（`952cb97`）**：挂单/成交两端在 005 字面内执行,AUG2920 的 skip 由 006 自动判定,无需改动;013 reconcile 缺口的修复已经在代码里,不需要 RISK 建议改门。
- **2 本小时（AUG2919）空仓观察，不改门**：市场层 0 活档 + 代码层 `dead`/`allow_session` 双闸已挡,剩余时间按现状空仓即可。
- 未选 3（无新 skip 触发需求；006 已自动判 AUG2920 skip,不需要额外的「停挂等下一小时」建议）。
- 未选 4（无改门/切 live/吃 taker/放宽成交门至 0.45/放宽 p 的理由；013 reconcile 缺口是代码问题不是门问题,修复路径已存在,不构成 ESCALATE 的门变更范畴）。
- 未选 5（010 已并行派 LEARN，`docs/briefs/2026-08-29-LEARN-379.md` 覆盖同一时间线,无需 RISK 重复指派）。

**额外标记（非菜单项，供 CMD 路由）**：`btchour/engine.py` 工作区有未提交的 013-reconcile 修复（针对 379 根因，`tests/test_live_one.py` 新增回归测试,15/15 通过），但当前跑着的 `python3 -m btchour run --playbook flex`（pid 293095，21:44 UTC 起）尚未重启,内存里仍是未修复版本——按 RISK.md「工程故障伪装成风险 → 转 MON」路径,建议 CMD 转 MON/ENG 确认是否/何时重启该进程并提交修复,RISK 不建议、不执行重启这一操作本身。

## 是否 ESCALATE：**否**

## 关键数字（≤3）与出处

1. **挂单 impulse = −35.75（≈brief −36，NO 32–42¢ 带内合规）**，2026-08-29T22:00:19.614599Z / 18:00:19 EDT，`journal#60818` + `trades#379.raw`；ask 0.33。
2. **成交前一 tick impulse = −1.40，ask 0.32 ≠ rest 0.25，全程未到 −$100**，2026-08-29T22:02:35.953Z（`journal#60852`），距 `flatten_reconcile` ts_ms 1788040975425（22:02:55.425Z）<20 秒——373/374/375/377/378/379 同一缺口第 6 次坐实。
3. **pnl = −0.0132，result=flatten_reconcile**，`trades#379.raw`；006 机械判定 `lost=True`（`pnl<0`）→ `skip_next=True` → AUG2920 skip（`python3 -m btchour board` 现场核对 `AUG2919 | flatten_reconcile | −0.0132 | skip下一小时`），不建议豁免。

（未建议改成交门到 0.45；未建议吃 taker；未建议放宽 `p=0.30`；未建议改 007；未建议豁免或改写 006；未建议切 live；未执行/未建议重启进程或提交代码，仅标记路由给 CMD/MON。）
