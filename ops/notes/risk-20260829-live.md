# RISK 姿态报告 — 3 笔 live_one（2026-08-29）

角色: RISK（decisions 010 触发，与 LEARN 并行）。只判「该不该挂 / 该不该成交」，不改门、不下单、不切 live。
事实来源: CMD 抽取的三笔 + 本轮对 `/workspace/data/btchour.sqlite`（`trades` + `journal`）的独立核实。判据: `docs/decisions.md` 005（挂单边、成交同向 |impulse|≥$100）、006（亏损空一小时）、007（clip 后不 hop）、008（不放宽成交门）。

---

## 372 AUG2820 YES T77699 — cancelled wait_wrong_side

- **该不该挂**：不该。按现行 005，YES 要 3 分钟 `|impulse|≥$100` 才挂；reason 字段写着动量 +9（sqlite `reason` 同源），差两个数量级。ask 0.48 也已在 28–42¢ 带外（sqlite `ask=0.48`）。挂单本身踩了两条：动量不够 + 价位出带。
- **该不该成交**：无实际成交（`live_fill=0.0`），0 张后撤，撤单动作合规，不构成风险敞口。
- **一句理由**：弱阳里在带外挂 YES，等错边撤——唯一守规矩的是撤单，不是挂单。

## 373 AUG2911 NO T77699 — t_wait_stop −0.2127

- **该不该挂**：不该。挂时（2026-08-29T14:05:21Z）journal/`reason` 动量 +0.45（弱阳），005 要求 NO 挂单 `impulse<0`，不满足；当时执行的是「看见 32–42¢ 就挂」的旧门，事后正是 005 用来堵这个洞的反面案例。
- **该不该成交**：不该。已用 sqlite 独立核实：成交时刻（2026-08-29T14:39:47.091233Z）`journal.impulse = +31.7251...`，方向与 NO 相反，且当时 `status` 已是 `no_coupon`（`ask=0.28<=rest`，早于成交已判失效）。挂到成交全程 impulse 从未到达 NO 所需的同向 `≤-$100`。005「成交仍要同向 |impulse|≥$100 且 ask 还在 rest」这条在这笔上没有生效却仍成交，出场 bid 0.04 / −85.1%。
- **一句理由**：静盘挂 NO 停 34 分钟，被反向拉升逆向选择打穿止损；因果顺序是「先挂等砸」，不是金带子的「先砸后挂」。

## 374 AUG2913 NO T77899 — t_clip +0.0353

- **该不该挂**：字面能挂，但是黄色地带。挂时（2026-08-29T16:13:46.547584Z）journal impulse −0.3587，字面满足 005 的 `impulse<0`，ask 0.37 也在 32–42¢ 带内；但 −0.36 是噪声级负打印，不是「看见方向在砸」。
- **该不该成交**：按 005 字面不该。用 sqlite 独立核实：挂单到成交（16:13:46→16:15:06，约 80 秒）期间 journal impulse 最深仅 −6.391（16:14:43.789938Z），从未触及 NO 所需的同向 `≤-$100`；成交前最后一条记录（16:14:58）仍是 −1.40，`status=wait`。也就是说成交门本该拦截，却仍然成交。
- **一句理由**：字面过门 + 成交确认门未生效，进场前提是噪声不是方向；出场守了 10%–50% 带（+14.1%），赢的是运气不是规矩。本小时已 clip，正确不 hop，当前 AUG2913 空仓。

---

## 总姿态：**黄**

理由：当前无 live 单挂着（372/373 已平，374 已 clip，本小时按 007 不 hop），没有立即敞口危险；但 373、374 两笔独立核实后都指向同一个模式——**成交时点的「同向 |impulse|≥$100 才该成交」这条 005 字面要求，在实际执行里没有生效**（373 fill 时反向 +31.7 仍成交；374 fill 前从未到 −100 仍成交）。374 是 005 生效之后发生的，说明这不只是旧门遗留问题，现行门的落地也有缺口。这不是「门定错了」，是「门没被强制执行」——不构成改门理由，但值得盯防，不能当作绿灯。

## 调整菜单（1–5，可多选）

- **1 维持 952cb97**（挂单边逻辑本身没错，372/373 的挂单侧错误在旧门下发生，005 已经堵上）
- **2 本小时空仓观察，不改门**（AUG2913 已 clip，当前本就空仓，符合 007）

未选 3（无新 skip 触发，006/007 已经覆盖本小时状态）。未选 4（无任何理由改门/切 live/吃 taker/放宽 p/成交门 0.45）。5（LEARN 对照）LEARN 已并行完成，不重复立题。

## 是否 ESCALATE：**否**

补充说明（非菜单项，仅供 CMD 参考，不是改门建议）：373/374 都显示「成交前同向 |impulse|≥$100 否则不该成交」这条 005 文字在 80 秒/34 分钟窗口内未被强制执行——这是执行/时序问题，不是数值问题，改 005 的数字解决不了它。是否派 ENG 核对撤单/成交判定时序，交由 CMD 决定；RISK 不建议以此为由改门、放宽、或切 live。

## 关键数字（≤3）与出处

1. **373 成交时 journal impulse = +31.7（反向，NO 门要求同向 ≤−$100）**，2026-08-29T14:39:47.091233Z，sqlite `journal`；出场 bid 0.04 / −85.1%，sqlite `trades#373.raw.exit_note`。
2. **374 挂到成交（80 秒）期间 journal impulse 最深仅 −6.39，从未到 −$100**，2026-08-29T16:14:43.789938Z，sqlite `journal`；成交价 clip 0.30 / +14.1% / +0.0353，sqlite `trades#374`。
3. **372 挂单 ask 0.48 在 YES 28–42¢ 带外，reason 动量 +9**，2026-08-28T23:07:53Z，sqlite `trades#372.raw.reason`。

（未建议把成交门放到 0.45；未建议吃 taker；未建议放宽 p；未建议切 live。）
