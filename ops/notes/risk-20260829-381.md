# RISK 姿态报告 — 381 AUG2923 NO T78099.99（2026-08-29，弱跌 −4 挂进，无 $100 成交，t_trail 止损，pnl −0.0612）

角色: RISK（decisions 010 触发；brief `docs/briefs/2026-08-29-RISK-381.md`）。只判「381 该不该按现行门（005）挂」，不下单、不改代码、不改门、不切 live、不对用户播报。
事实来源: `/workspace/data/btchour.sqlite`（`trades#381` raw + `journal#63223–63302`，2026-08-30T02:44:21–02:53:59Z / 22:44:21–22:53:59 EDT）独立抽查；`python3 -m btchour board`（只读，009 允许，23:01:14 EDT 现场核对）；代码路径核对 `btchour/exits.py`（158–168 行 `t_trail` 逻辑）。对照 `ops/notes/risk-20260829-380.md`。

---

## 该不该按现行门挂：字面合规，但与 380 是同一量级的擦边弱跌

`trades#381` 挂单响应 2026-08-30T02:06:53.016857Z（22:06:53 EDT），`raw.reason` 核实：`dump_gap NO 看见 0.42 rest 0.25 动量 -4 p=49.6% clip>=0.29`——`impulse = -4`，`ask = 0.42`，落在 NO 32–42¢ 带内，`rest = 0.25`。005 对 NO 的字面要求只是 `impulse<0` 且 32–42¢ 看见——满足，挂单不违规。

但 **`impulse=-4` 与 380（同样 `-4`）完全同一量级**，是本轮里除 373（+0）/374（−0）之外最弱的动量样本，仍落在 RISK.md 黄档定义「挂在噪声负打印（如 −0）但仍在 005 字面内」的模糊边界。005 没有给出 impulse 的下限数值，只要求方向与价带；377/380/381 三笔 `t_trail` 止损里，380/381 两笔的挂单动量都是 `-4`，说明「弱跌」挂单本身不是孤立事件，而是这条门在弱动量区间反复放行的结果——这是门的字面行为，不是执行异常，RISK 不建议改门（008 禁区），但值得记录供 CMD/LEARN 对照。

## 该不该成交：无 $100 确认，方向反转——与 380 同一结构性缺口，本次是第 8 次坐实

journal 逐笔核实：挂单后动量持续走弱至 22:48:46.970（`impulse=-0.6265`，`ask=0.59`——已经不等于挂单时的 `rest=0.25` 或挂单 `ask=0.42`），随后 **journal 断档 4 分钟**（22:48:46.970 → 22:52:47.585，无采样），`filled_at`（2026-08-30T02:51:03.772280Z / 22:51:03 EDT）恰好落在断档窗口内，断档结束后首条采样（`journal#63283`，22:52:47.585）`impulse=+40.264`——强反向（NO 需要同向为负，实际是正且远超 $100 门槛量级的反向跳动）。这与 373/374/375/377/378/379/380 同属一个已知结构性缺口（post-only rest 单被交易所真实撮合，内部 impulse/ask 门不对交易所生效），**381 是第 8 次坐实**，断档窗口模式与 380 完全一致（断档前弱化、断档后强反转、成交落在断档中间），不是新 bug，不构成改门理由（008 禁区）。

## 出场核实：t_trail 是引擎既有正常路径，算术核对无误

`exit_reason=t_trail`，`btchour/exits.py:160` 逻辑核实：`peak_bid=0.35` 越过做T下限（floor）后武装追踪止损，回撤到 `bid=0.20` 触发 `settings.swing_trail=0.04` 门槛（0.35−0.20=0.15≥0.04）退出，`exit_note` 记录一致（"trail 0.04 from peak 0.35 → 0.20 (roi -24.5%)"）。`flatten.response.average_fill_price=0.8000`，换算 NO 侧 `exit_price=1−0.80=0.20`，与记账一致；`pnl = 0.20 − 0.0112(fee) − 0.25 = −0.0612`，与 `trades#381.pnl` 及 `board` 表格核对无误。全程只 1 张 live，未吃 taker（开仓 rest 0.25；平仓 IOC 交叉是 013 批准的例外），未切 live，`013`「真成交后按 `average_fill_price` 落账」被正确遵守。

## 006 / 007 现场核对：均已按门自动生效，无需人工干预

`board` 现场快照（23:01:14 EDT）：`AUG2923 | 1 | trail | −0.0612 | skip下一小时`——`pnl<0` 命中 006，`AUG3000`（下一小时，跨日按 001 命名规则）已被自动判 `skip小时`，`活档0/可挂0`，与代码判定一致，不建议豁免。当前 `leftover/working` 为空，`trades` 表无 382，本小时（AUG3000）无新挂单，007 的「clip 后不 hop」与此无直接冲突（本轮是 006 的整小时 skip，而非 007 的同小时不翻面）。

## 姿态：**黄**，理由与 380 一致且证据链延长一环

红档定义（RISK.md）是「静/弱阳真单、第二张 live、吃 taker、整条切 live」——381 都不占：挂单是 NO 32–42¢ 带内合规挂单，非静盘/弱阳挂 YES；全程只 1 张 live；未吃 taker；未切 live。判黄的理由：（a）挂单动量 `-4` 与 380 完全同量级，是本轮弱跌挂单里最弱之一，落在字面合规但接近模糊边界；（b）成交确认无 $100 且方向反转，是同一已知缺口第 8 次坐实，断档模式与 380 高度重复，提示这不是偶发而是该缺口在弱动量场景下的稳定表现；（c）出场 `t_trail` 是引擎正常止损路径，算术核对无误，不额外加严重度。三者叠加不到红档门槛（无第二张 live、无真吃 taker、无整条切 live）。

---

## 调整菜单

1. **维持现状（`952cb97`）**：挂单/成交/出场三段均在既有门与代码路径内正确执行（挂单合规，成交是已知缺口第 8 次坐实，出场算术无误），006/007 已自动生效，无需改动。
2. **本小时（AUG3000）空仓观察，不改门**：board 现场核实活档 0、可挂 0，且 006 已判定 `skip小时`，双闸已挡，无需额外动作。
3. 未选（无新增「停挂等下一小时」建议需求；006 已自动判 AUG3000 skip）。
4. 未选（无改门/切 live/吃 taker/放宽成交门至 0.45/放宽 `p=0.30` 的理由；成交确认缺口第 8 次坐实、挂单动量 `-4` 与 380 同量级，都是已知模式的延续证据，不构成升级充分条件；是否值得转 LEARN 对照「弱跌 `-4` 挂单是否系统性偏弱」由 CMD 判断，不是本报告的改门建议）。
5. 未选（010 已并行派 `docs/briefs/2026-08-29-LEARN-381.md`，无需 RISK 重复指派；但建议 CMD 关注 LEARN-381 是否也发现 380/381 挂单动量同为 `-4` 这一重复模式，供后续是否值得转 ARCH 评估 005 弱动量下限做参考，本次不建议改门）。

## 是否 ESCALATE：**否**

## 关键数字（≤3）与出处

1. **挂单 `impulse = -4`，`ask = 0.42` 在 NO 32–42¢ 带内，`rest = 0.25`**，2026-08-30T02:06:53.016857Z / 22:06:53 EDT，`trades#381.raw.reason`；005 字面合规，但与 380 挂单动量完全同量级（`-4`），是本轮最弱挂单样本之一。
2. **成交瞬间前后动量断档：断档前 `impulse≈-0.63`（22:48:46.970 EDT，`ask=0.59≠rest 0.25`），断档后首条 `impulse=+40.264`（22:52:47.585 EDT）**，`filled_at`（22:51:03.772 EDT）落在断档窗口内，`journal#63282/63283`——373/374/375/377/378/379/380/381 同一缺口第 8 次坐实，断档模式与 380 高度重复，仅作观察记录，不建议改门。
3. **`pnl = -0.0612`，`exit_reason=t_trail`（peak 0.35→0.20，引擎正常追踪止损，`swing_trail=0.04`）**，`trades#381.raw`；006 判定本小时（AUG2923）亏损 → `AUG3000` skip（`board` 现场核对 `AUG2923 | trail | −0.0612 | skip下一小时`，当前 `AUG3000` 已 `skip小时`），不建议豁免。

（未建议改成交门到 0.45；未建议吃 taker；未建议放宽 `p=0.30`；未建议改 006/007；未建议豁免 `AUG3000` skip；未建议切 live。）
