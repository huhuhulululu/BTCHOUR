# RISK 姿态报告 — 383 AUG3003 NO T78099.99（2026-08-30，挂单动量 −7.2 合规，成交 −4.7/ask0.28 仍无 $100，t_clip +0.1135，同一缺口第 10 次坐实）

角色: RISK（decisions 010 触发；brief `docs/briefs/2026-08-30-RISK-383.md`）。只判「383 该不该按现行门（005）挂 / 是否还该继续挂」，不下单、不改代码、不改门、不切 live、不对用户播报。
事实来源: `/workspace/data/btchour.sqlite`（`trades#383` raw + `journal#64992–65065`，2026-08-30T06:00–06:09:27Z / 02:00–02:09:27 EDT）独立抽查；`python3 -m btchour board`（只读，009 允许）。对照 `ops/notes/risk-20260830-382.md`（同型第 9 次坐实）。

---

## 该不该按现行门挂：字面合规

`trades#383` 挂单响应 2026-08-30T06:01:06.392777Z（02:01:06 EDT）。独立核实 `journal#64992`（06:01:06.152631Z）`impulse=-7.2355`，与 CMD 报的「动量 −7.2」一致；`raw.reason`「dump_gap NO 看见 0.34 rest 0.25 动量 -7」同源。005 对 NO 的字面要求是 `impulse<0` 且 32–42¢ 看见——`ask=0.34` 落在带内，`rest=0.25`，挂单不违规，29¢ 飞刀未接。挂后动量继续走弱至 `journal#65005`（06:01:58.660970Z，`impulse=-17.9248`），比挂单时更强，不是噪声打印。

## 该不该成交：无 $100 确认，仍是同一已知结构性缺口——本次第 10 次坐实

独立核实 `journal#65058`（06:08:32.038114Z）`impulse=-4.6907`，与 `filled_at`（06:08:32.041166Z，`trades#383.raw`）几乎同一秒，与 CMD 报的「成交 −4.7」一致。挂单到成交全程（`journal#64992–65058`，覆盖 06:01:06–06:08:32，中间 06:04:04–06:07:14 有约 3 分钟采样断档）内 `impulse` 从未回到同向 `|impulse|≥$100`——最深只到 `-17.9248`（`journal#65005`）。CMD 报成交时 `ask=0.28`，与挂单时 `rest=0.25` 仍差 3¢（`ask≠rest`，sqlite 无独立 quote 表可核对 ask，仅可信 CMD 现场读数，但方向与「exchange 撮合不看内部 ask/impulse 门」的既有结论一致）。

**这与 373/374/375/377/378/379/380/381/382 同属一个已知结构性缺口（post-only rest 单被交易所真实撮合，内部 impulse/ask 门不对交易所生效），383 是第 10 次坐实**——区别只在于这次结果是赢（`pnl=+0.1135`），而前几次多是小亏/止损。**赢不代表门起作用了**：确认链条（同向 `$100` + `ask` 回到 `rest`）依旧没有被满足，只是行情方向恰好对我们有利。用户已明确提醒「不要把 +0.1135 写成门变好」——本报告不做此结论。

## 出场核实：013 合规，board 现场对账一致

`exit_reason=t_clip`，`flatten.response.average_fill_price=0.6200`（`slip_ticks=2`，013 批准的交叉例外），`exit_price=0.38`，`pnl=0.1135`，与 `trades#383.pnl` 一致。`fill_count=1.00`（开仓与平仓均非 0），未触发 013 禁止的 `fill_count=0` 记账问题。`board` 现场核对：本小时挂单表「空仓」（无 leftover/working，无 384），`AUG3003 | 1 | clip | +0.1135 | 可做`。

## 006 / 007 现场核对：本小时不该再挂，下一小时可做

- **007**：本小时（AUG3003）已 clip → 不 hop，不开新档、不翻面。`board` 现场确认本小时挂单表空仓，无第二张 live、无 384，与「还该不该继续挂」的答案（本小时不该）一致。
- **006**：赢不 skip（0 成交才不叠，本次是赢不是 0 成交）。`board` 现场显示 `AUG3004` 标注「可做」，与 006 一致，是下一小时的独立判断，不代表本小时可以继续挂。

## 姿态：**黄**

红档定义（RISK.md）是「静/弱阳真单、第二张 live、吃 taker、整条切 live」——383 都不占：挂单动量 `-7.2`→挂后深至 `-17.9`，非静盘/弱阳；全程只 1 张 live；未吃 taker；未切 live。判黄而非绿的理由：成交确认无 `$100`、`ask≠rest` 的已知结构性缺口第 10 次坐实，确认链条本质没有被满足；本次赢钱是行情方向凑巧，不是门在生效，不构成把姿态提到绿的理由。

---

## 调整菜单

1. **维持现状（`952cb97`）**：挂单/成交/出场三段均在既有门与代码路径内正确执行（挂单合规，出场 013 合规），006/007 已自动生效，无需改动。
2. **本小时（AUG3003）继续空仓观察，不改门**：board 现场核实活档 0、无 leftover/无 384，007 已自动挡下本小时 hop，无需额外动作。
3. 未选（无「停挂等下一小时」的额外需求；006 判定本小时为赢，`AUG3004` 已按门允许做 T，无需人工豁免）。
4. 未选（无改成交门至 0.45、吃 taker、放宽 `p=0.30`、整条切 live 的理由；本次是已知缺口的又一次延续，不是新问题，不构成升级充分条件）。
5. **建议派 LEARN 做对照**：373–383 已连续 10 笔坐实「成交无 $100 确认 + ask 未回到 rest」同一结构性缺口，样本量已足够大，建议 LEARN 把这条累计到对照结论里，且明确本次赢钱（+0.1135）不改变缺口本身是否被满足的判断——不把这次回放/结果当作门变好的证据。

## 是否 ESCALATE：**否**

## 关键数字（≤3）与出处

1. **挂单 `impulse=-7.2355`，`ask=0.34` 在 NO 32–42¢ 带内，`rest=0.25`**（`journal#64992` 06:01:06.152631Z 独立核实，与 `trades#383.raw.reason` 一致）；005 字面合规。
2. **成交瞬间 `impulse=-4.6907`**（`journal#65058` 06:08:32.038114Z，与 `filled_at` 06:08:32.041166Z 几乎同一秒），持有期内最深仅到 `-17.9248`（`journal#65005`），从未回到同向 `|impulse|≥$100`——373/374/375/377/378/379/380/381/382/383 同一缺口第 10 次坐实。
3. **`pnl=+0.1135`，`exit_reason=t_clip`（`average_fill_price=0.62`→`exit_price=0.38`）**（`trades#383.raw`）；board 现场核对本小时挂单表「空仓」（无 leftover、无 384，007 已挡 hop），`AUG3004` 按 006（赢不 skip）标「可做」——本次赢钱不改变第 2 点缺口判断。

（未建议改成交门到 0.45；未建议吃 taker；未建议放宽 `p=0.30`；未建议改 006/007；未建议切 live；未把 +0.1135 解读为门变好。）
