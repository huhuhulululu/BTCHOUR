# RISK 姿态报告 — 385 AUG3005 NO T78099.99（2026-08-30，dump_gap NO 字面合规挂单、成交无 $100 且落在 journal 断档里、成交后动量反手 +40 但 exits=0 系当前退出门未到阈值，非异常）

角色: RISK（decisions 010 触发；brief `docs/briefs/2026-08-30-RISK-385.md`）。只判「385 该不该按现行门（005）挂 / 成交有没有 $100 / 还该不该继续挂」，不下单、不改代码、不改门、不切 live、不对用户播报、不 flatten。
事实来源: `/workspace/data/btchour.sqlite`（`trades#385` raw + `journal#65903–65973`，2026-08-30T08:00:02–08:10:25Z / 04:00:02–04:10:25 EDT）独立抽查；`btchour/exits.py` L1–211 代码核实 `impulse_wait` 退出条件；`btchour/config.py` L87–88 核实 `impulse_wait_stop=0.80`、`impulse_wait_scratch_seconds=480`。对照 `ops/notes/risk-20260830-384.md`（383/384 同型 dump_gap NO 前例）。

---

## 该不该按现行门挂：独立核实与 CMD 一致，字面合规

`trades#385` 挂单响应 2026-08-30T08:00:10.984442Z（04:00:10 EDT）。独立核实 `journal#65905`（08:00:10.737033Z）`impulse=-10.059`（≈−10.1，CMD 报 −10.1 一致，抽查通过）、`ask=0.42`、`rest=0.25`，`raw.reason`「dump_gap NO 看见 0.42 rest 0.25 动量 -10」同源。005 对 NO 的字面要求是 `impulse<0` 且 32–42¢ 看见（不要求 $100，那是涨/coupon YES 的门）——挂单本身合规，与 383/384 同一 `dump_gap NO` 子路径。

## 成交有没有 $100：独立核实全程未到 $100，且成交时刻落在 journal 断档里

独立核实挂单持有期（08:00:10–08:07:24Z）内 `journal` 逐条扫描，`impulse` 最深见 **−19.171**（`journal#65932`，08:02:00.615353Z），随后回摆至 −0.44（`#65950`，08:03:12.019271Z），全程未接近 $100 量级——CMD「最深 −19.2，无 $100」独立核实成立。`trades#385.raw.filled_at=2026-08-30T08:07:24.091912Z`，但 `journal` 在 `#65950`（08:03:12）与 `#65951`（08:08:59.443743Z）之间有 **5 分 47 秒空档**，成交时刻恰好落在此空档内、无对应行——CMD「成交在 journal 断档里」独立核实成立。`scans` 表同期（08:07:00–08:08:59Z）逐 4 秒仍在正常吐 `opportunity_count=0` 的行情快照，说明扫描本身没停，缺的只是这一档 `journal` 逐笔记录，不是引擎挂死（MON 范畴，非本判断）。005 对 NO 从未要求 $100 门槛，故成交无 $100 **不构成入场违规**，但与 383「有成交无 $100 确认」是同一未解子问题的又一样本。

## 04:08 仍 open 未 flatten：代码级核实 exits=0 是当前退出门尚未到阈值，非引擎异常，但敞口方向已明显不利

`trades#385.status='open'`，全库最新 `journal#65973`（08:10:25.517979Z）仍是同一状态，未见任何 flatten/exit 记录，独立核实「04:08 仍 open」成立。成交后 journal 断档结束的第一行 `#65951`（08:08:59.443743Z）`impulse=+40.360`、`spot=78176.20`（strike 78099.99，已高出 **$76.2**，较入场时 spot 78122.75 又推高约 **$53**）——CMD「动量 +38 反手」方向与量级独立核实基本一致（断档期间无逐秒记录，+38 应为断档中段估值，断档恢复后首行读数 +40.4 略高，方向、量级均确认反手成立，不构成分歧）。
独立核实 `btchour/exits.py`：本仓 `play=impulse_wait`（`wait_t=True`），退出只看两条件——① `roi_now <= -impulse_wait_stop`（`config.py` `impulse_wait_stop=0.80`，需回撤到几乎归零附近的止损线，远未触发）；② 持仓 `held_seconds >= impulse_wait_scratch_seconds`（`=480`秒）才检查 scratch。当前持仓时长（08:10:25 − 08:07:24 ≈ **181 秒**）远未到 480 秒。**`exits=0` 是现行退出门尚未到阈值的必然结果，不是引擎故障**，但同时说明：即便标的已经明显反手、朝对 NO 不利的方向走出 $76 价差，现行门在成交后近 8 分钟内结构性不会有任何保护动作——这是「继续挂」要正视的敞口，不是「继续挂」本身违规。

## 004 / 006 / 007 / 013 现场核对：均未破

- **004**：`trades` 表 `status='open'` 仅 385 一笔，独立查询确认无第二张 live，「不加第二张」既是 CMD 要求也是 004 结构性保证（1 张未平前引擎不会再开）。
- **006**：385 尚未结算/未平仓，无 `pnl`，不适用「亏了空一小时」。
- **007**：本小时（AUG3005）385 是第一笔且尚未 `clip`，不触发「clip 后不 hop」；反正 004 已锁死第二张。
- **013**：385 当前 `status='open'`（非 `closed`/`cancelled`），未见被对账机制误刮成残留或误标 `paper_close` 的迹象，「013 不刮 working/open」独立核实无异常。

## 姿态：**黄**

红档定义（RISK.md）是「静/弱阳真单、第二张 live、吃 taker、整条切 live」——385 都不占：挂单动量 −10.1 是真实 dump_gap（非静盘/弱阳）、全程只 1 张 live、未吃 taker、未切 live。判黄而非绿的理由：① 成交无 $100 确认、且恰好落在 journal 断档里，是 383 已暴露的同一未解子问题的重复样本，不是新违规但也不是「门在良好运作」；② 成交后动量已反手 +40、价差已拉开 $76，现行 `impulse_wait` 退出门在 480 秒 scratch 窗口内结构性不会动作，敞口是真实存在的、需要持续盯，不是「继续挂」违规但也不能写成「安全」。

---

## 调整菜单

1. **维持现状（`952cb97`）**：挂单字面合规、成交无 $100 属 NO 路径不要求项、004/006/007/013 均未破，无需改动。
2. 未选（385 已是本小时仅有一笔且 004 已锁死加仓，无需额外「本小时空仓观察」的人工动作；效果已由结构保证）。
3. 未选（无「停挂/等下一小时」的额外需求；385 仍在 480 秒 scratch 窗口内，尚未到该窗口触发评估的时点）。
4. 未选（无改成交门至 0.45、吃 taker、放宽 `p=0.30`、整条切 live 的理由；385 是已知 `dump_gap NO` 子路径的又一次「无 $100 确认仍成交」样本，不构成升级充分条件）。
5. **建议派 LEARN 做对照**：383（有成交无 $100 确认）+ 385（同型、且成交落在 journal 断档里、成交后动量反手 +40 但退出门未到阈值）已形成可归纳的重复样本，建议 LEARN 把「NO 路径无 $100 确认成交」与「成交后动量反手、480 秒 scratch 窗口内结构性无保护」两个子问题分开累计，不与 YES/coupon 的 $100 门混为一谈；不把本次未 flatten 解读为门坏了或该现在改。

## 是否 ESCALATE：**否**

## 关键数字（≤3）与出处

1. **挂单 `impulse=-10.059`（≈−10.1），`ask=0.42`、`rest=0.25`**（`journal#65905` 2026-08-30T08:00:10.737033Z 独立核实，与 `trades#385.raw` 及 CMD 报的 −10.1/0.42 一致）；005 dump_gap NO 字面合规，无需 $100。
2. **持有期 `impulse` 最深 −19.171**（`journal#65932` 08:02:00.615353Z），成交 `filled_at=08:07:24.091912Z`（`trades#385.raw`）落在 `journal#65950`（08:03:12）→`#65951`（08:08:59）的 **5 分 47 秒断档**内——全程未见 $100 量级，成交无 $100 确认独立核实成立。
3. **断档结束首行 `impulse=+40.360`、`spot=78176.20`（strike 78099.99，价差 $76.2）**（`journal#65951` 08:08:59.443743Z），当前持仓约 181 秒（截至最新 `journal#65973` 08:10:25Z）；`btchour/exits.py`+`config.py` 核实 `impulse_wait_stop=0.80`／`scratch_seconds=480` 均未到，`exits=0` 是现行门结构性结果，非异常；`trades#385.status='open'` 未 flatten，004 锁死无第二张。

（未建议改成交门到 0.45；未建议吃 taker；未建议放宽 `p=0.30`；未建议改 005/006/007/013；未建议切 live；未建议现在 flatten；未把无 $100 成交或反手未平写成门变好或变坏。）
