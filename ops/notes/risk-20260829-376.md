# RISK 姿态报告 — 376 AUG2915 YES T78199（2026-08-29，已撤、0 成交）

角色: RISK（CMD 派发，`docs/briefs/2026-08-29-RISK-376.md`）。只判「该不该继续按现行 005 挂」，不下单、不改代码、不改门、不切 live、不对用户播报。
事实来源: `/workspace/data/btchour.sqlite`（`trades#376` + `journal`）独立核实；代码路径核对 `btchour/strategy.py`（`coupon_sides` / `impulse_wait_wrong_side` / `impulse_wait_flipped`）、`btchour/engine.py`（`_wait_cancel_reason` 326–337 行）。判据: `docs/decisions.md` 005（涨=3分钟`impulse≥$100`才挂YES；静/弱阳不挂）、006（0 成交不叠 skip）、007（clip 后不 hop——本笔未 clip，不适用）、011（近 ATM 阶梯是工作盘，空仓≠机会少）。

---

## 该不该挂（已发生，核实结果：该挂）

`trades#376` 独立核实：2026-08-29T18:00:35.752Z（14:00:35 EDT），`journal.impulse=104.38`，已 ≥ `impulse_min=$100`；`raw.ask=0.30`、`rest=0.25`，落在 YES 28–42¢ 带内。`coupon_sides()` 在 impulse≥100 时只返回 `["yes"]`——挂 YES 合规，不是「弱阳挂 YES」。

## 该不该撤（已发生，核实结果：该撤，代码执行正确）

18:02:44.795Z（14:02:44 EDT），`journal.impulse` 从峰值 ~123.4（18:02:17Z）回落到 **97.61**，跌破 `impulse_min=$100`。此时 `coupon_sides(97.61)` 已只返回 `["no"]`，`side="yes"` 命中 `impulse_wait_wrong_side` → `engine._wait_cancel_reason` 判 `wait_wrong_side` 自动撤单。核对 `impulse_wait_flipped`：97.61 未到 −100，属「量级不足回落」而非「整段反手」，即此刻已经是**弱阳**（低于$100门槛的正向），005「静/弱阳不挂 YES」在此刻适用——撤单是把已经变成弱阳的 YES 挂单拉下来，执行正确，不是 bug，也不构成 373–375 那类「成交时点确认门对交易所撮合不生效」的缺口（376 从未成交，`live_fill=0.0`，缺口不适用于纯撤单场景）。

## 0 成交怎么算（forward，核实结果：不 skip）

按 006「0 成交不是亏损」，本笔撤单不触发下一小时空仓；按 007，本小时未 clip，不受「不 hop」限制。当前核实 `AUG2915` 本小时仅这一笔交易记录，撤单后无 resting 敞口（无第二张 live 冲突风险）。

## 还该不该继续按现行门挂（核心问题，结论：该，按门继续）

005 门本身没有被打穿、也没有理由现在收紧或放宽：
- 挂单、撤单两步都是门内正常运作（涨到 $100 挂 YES → 回落到弱阳自动撤），不是「蠢挂」也不是「引擎漏洞」。
- 0 成交不构成 skip 理由（006）；未 clip 不受 007 限制；当前无 live 敞口，符合 004（一张一张来）。
- 撤单后最新 journal（18:05:42Z）`impulse≈−59`（未到 −$100）、`T78099.99 ask=0.61`（超出 32–42¢/28–42¢ 带）→ 当前无满足 005 的活口，但这是**市场当下没给带内点**，不是门失效；011 要求的是「近 ATM 阶梯是工作盘」，不是「随时必须有单」。
- 弱阳（如再看到 impulse 回到 +$40 附近）依然不许挂 YES；跌且 32–42¢ 看见才挂 NO；再来一次 ≥$100 涨且 28–42¢ 带内可以再挂 YES——这些都是现行 005 字面，本报告不建议改动。

---

## 总姿态：**绿**

理由：挂单、撤单全程都在 005 字面内正确执行，没有静/弱阳挂 YES 的违规，没有第二张 live，没有吃 taker，没有切 live 迹象；0 成交按 006 不构成空仓/skip 判罚。现行门没有被破坏，也没有发现新缺口，`AUG2915` 可以继续按门等下一次带内点。

## 调整菜单

- **1 维持现状（`952cb97`）**：挂单/撤单逻辑都对，继续按 005 在 `AUG2915` 等下一次符合门的点（涨$100→YES 28–42¢，或跌且 32–42¢→NO）。
- 未选 2（不建议空仓观察——没有违规或异常需要暂停，011 要求继续把近 ATM 阶梯当工作盘盯）。
- 未选 3（无 skip 触发：0 成交不叠 skip，006/007 均不适用于停挂）。
- 未选 4（无改门/切 live/吃 taker/放宽理由——门执行本身正确）。
- 未选 5（LEARN 已按 010 并行处理该笔撤单，无需额外指派）。

## 是否 ESCALATE：**否**

## 关键数字（≤3）与出处

1. **挂单时 impulse=104.38**（2026-08-29T18:00:35.752Z / 14:00:35 EDT，`journal`），ask 0.30 / rest 0.25，≥`impulse_min=100`——挂 YES 合规。
2. **撤单时 impulse=97.61**（2026-08-29T18:02:44.795Z / 14:02:44 EDT，`journal`），跌破 $100 门槛转为弱阳 → `wait_wrong_side` 自动撤，代码执行正确。
3. **`live_fill=0.0`**（`trades#376.raw.live_fill`）——本小时唯一一笔，现无 resting 敞口，0 成交不叠 skip（006）。
