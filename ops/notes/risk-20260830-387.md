# RISK 姿态报告 — 387 AUG3007 NO T77999.99（2026-08-30，弱跌 −0.8 挂 NO 字面合规但逼近门槛边缘、成交无 $100 确认（NO 路径本不要求）、成交瞬间动量剧烈反手至 +42.3、`t_clip` +0.0645 为机制性平仓、且平仓时刻恰好落在一段 journal 记录空档内）

角色: RISK（decisions 010 触发；brief `docs/briefs/2026-08-30-RISK-387.md`）。只判「387 该不该按现行门（005）挂 / 成交有没有 $100 / 本小时已 clip 该不该 hop」，不下单、不改代码、不改门、不切 live、不对用户播报、不 git push。
事实来源: `/workspace/data/btchour.sqlite`（`trades#387` raw + `journal#67017–67145`，2026-08-30T10:12:00–10:30:30Z / 06:12:00–06:30:30 EDT）独立抽查；`btchour/strategy.py` L20–58（`coupon_sides`/`coupon_rest_ready`）、`btchour/config.py` L74/82（`impulse_min=100.0`、`impulse_wait_rest_min=0.0`）核实 NO 门字面；`btchour/exits.py` L122–166 + `config.py` L66/67/69（`swing_target=0.10`、`swing_max_clip=0.50`、`swing_runner_gap=0.12`）核实 `t_clip` 为规则性触发，非人工判断。对照 `ops/notes/risk-20260830-386.md`（385/386 同型样本、385 已有断档前例）。

---

## 该不该按现行门挂：字面合规，但信号强度是目前样本里最弱的一次

独立核实 `journal#67017`（2026-08-30T10:12:15.924801Z）`impulse=-0.779500000004191`（≈−0.8），与 `trades#387` 挂单时刻（`raw.response.ts_ms` 换算 10:12:16.153Z）相差仅 0.23 秒，同源确认焦点里的 **−0.8**。代码级核实 `btchour/strategy.py::coupon_rest_ready`：`impulse_wait_rest_min=0.0` 时 NO 侧字面只要求 `impulse < 0`（`need <= 0` 分支，L43–46），不设幅度门槛——−0.7795 满足即可挂，`ask=0.38` 在 32–42¢ 带内（`raw.reason`「dump_gap NO 看见 0.38 rest 0.25 动量 -1」）。**但**这是继 386（−4.2）之后信号更弱的一次：−0.8 只比 0 深了不到一美元，是「刚好压线」而非「明确下跌」，比 386 报告已判黄的边缘案例还要靠近门槛零线。字面合规成立，风险判断上应视为比 386 更边缘。

## 成交有没有 $100：全程未见 $100，成交瞬间动量已反手至 +42.3（与 386 型态一致但幅度更大更快）

独立核实持有期（10:12:16–10:23:14Z）逐条 journal，`impulse` 最深见 **−32.999**（`journal#67050`，10:14:24.670173Z），随后一路反弹，全程未接近 $100 量级——「成交无 $100」独立核实成立。`trades#387.raw.filled_at=2026-08-30T10:23:14.065317Z`，最近 `journal#67125`（10:23:14.062644Z）相差仅 2.7 毫秒，记录连续无断档，同期 `impulse=+42.309`（`tape_impulse=+41.477`）——与焦点「成交 tick 动量 +42」独立核实一致（略高至 +42.3）。NO 路径本不要求 $100（005 原文、`impulse_wait_rest_min=0.0` 均确认），故不构成入场违规；但这是继 383/384/385/386 之后第 5 个「NO 被动成交、无 $100」样本，且这次反手幅度（−33.0 → +42.3，约 75 点摆动）在成交前后几乎瞬间发生,比 386 的 −27.8 → +43.4（跨约 2 分钟）更快更陡。

## 新发现：flatten 决策时刻恰好落在一段约 6 分钟的 journal 记录空档内，无法独立复核平仓瞬间的行情

独立扫描 `journal` 全表（不限 event_ticker）在 10:23:20–10:29:25Z 区间，仅两条记录：`journal#67127`（10:23:21.886925Z，`impulse=+41.02`）与 `journal#67128`（10:29:23.377352Z，`impulse=-0.99`），中间空档 **6 分 01 秒**。`trades#387.raw.flatten.response.ts_ms=1788085755947` 换算 **2026-08-30T10:29:15.947Z**，恰好落在这段空档正中间——**flatten 决策发生时，journal 没有任何逐条记录可供独立核实当时的 spot/impulse**。这与 `ops/notes/risk-20260830-386.md` 提到的 385 断档（5 分 47 秒）是同一类问题的第 2 次出现，但这次更严重：385 的断档发生在挂单持有期（不影响平仓判断），387 的断档直接盖住了平仓那一刻。代码级可核实退出条件本身是规则触发（见下），但记录空档意味着"退出时行情到底是什么样"目前只能信 `raw.exit_note` 单一来源，缺少交叉验证。

    30|## `t_clip` 机制核实：规则性平仓，非人工判断，+0.0645 不代表门变好

独立核实 `btchour/exits.py` L141–156：`t_clip`（「做T clip」分支）触发条件是 `bid >= floor`（`floor` 由 `swing_target=0.10` 算出）且 `gap = model_p - bid < swing_runner_gap(0.12)`——`raw.exit_note`「做T clip 25.8% at bid 0.33 (band 10%-50%); gap 9.8% gone, 落袋」与代码逐字对应（9.8% < 12% 的 `swing_runner_gap` 阈值，触发落袋而非继续持有等 `t_trail`/`t_stop`）。`raw.flatten.response`：`fill_count=1.00`、`average_fill_price=0.6700`，符合 013（真实成交落账，非 `paper_close` 空成交）。`trades#387.pnl=0.0645` 核算：`exit_price(0.33) − rest(0.25) − exit_fee(0.0155) ≈ 0.0645`，算术核实成立。**本报告不将这 +0.0645 解读为 005 门运作改善或应放宽的信号——退出是写死规则（`floor`/`gap` 阈值）命中触发，且触发瞬间本身处于记录空档，谈不上「验证了门更准」。**

## 004 / 006 / 007 / 008 / 013 现场核对：均未破

- **004**：本小时 `trades` 表（`event_ticker='KXBTCD-26AUG3007'`）仅 387 一笔 `live_one`，无第二张同时 live。
- **006**：387 结果为盈利（`pnl=+0.0645`），非亏损，「亏了空一小时」不适用。
- **007**：387 是本小时仅一笔且已平仓（`status=closed`），无 hop 记录，字面与结构性均满足「不 hop」。
- **008**：`taker=0`，未吃 taker；成交门未放宽到 0.45（`ask=0.38` 本在 32–42¢ 带内）；未放宽 `p=0.30`（`model_p=0.4728`，与门无关）。
- **013**：flatten `fill_count=1.00`、`average_fill_price=0.6700`，非空成交误记；账目出场价来自交易所真实成交，合规。

---

## 姿态：**黄**

红档定义（RISK.md）是「静/弱阳真单、第二张 live、吃 taker、整条切 live」——387 都不占：−0.8 仍是真实下跌打印（非静盘/弱阳，`impulse<0` 字面成立）、全程只 1 张 live、未吃 taker、未切 live。判黄的理由：① −0.8 是目前样本里最贴近「压线合规」的一次，比 386 的 −4.2 更边缘，字面通过不代表信号质量可靠；② 成交无 $100 是连续第 5 个 NO 路径同型样本，模式已足够稳定值得 LEARN 归纳，而非继续当孤立个案；③ **新问题**：flatten 决策瞬间落在一段 6 分钟 journal 空档正中间，独立复核缺少交叉数据源，这是audit 缺口，不是挂单/成交违规,但值得记录以免累积成盲区；④ +0.0645 是规则命中的机制性小额盈利，明确不代表门在变好或应放宽（遵照用户提醒）。

## 调整菜单

1. **维持现状（`952cb97`）**：挂单、成交、平仓均字面合规，004/006/007/008/013 均未破，`t_clip` 按现行参数正确触发并干净落账，无需改动。
2. 未选（387 已平仓，本小时无未平敞口）。
3. **本小时已 clip，明确不 hop**（007 结构性已满足，本项为确认而非新增动作）。
4. 未选（无改成交门至 0.45、吃 taker、放宽 `p=0.30`、整条切 live 的理由）。
5. **建议派 LEARN 做对照**：383/385/386/387 四笔已构成「NO 被动成交、无 $100 确认」的稳定重复模式（384 撤单不计入），且 385/387 两次都出现 journal 记录空档（385 断档 5 分 47 秒在持有期、387 断档 6 分 01 秒直接盖住平仓瞬间）——建议 LEARN 一并评估：(a) NO 路径长期零 $100 确认是否该有独立观察指标；(b) 记录空档是否有系统性成因（扫描间隔/节流），若持续出现会侵蚀未来复盘的可信度。两项均供归纳参考，不作为现在改门依据。

## 是否 ESCALATE：**否**

## 关键数字（≤3）与出处

1. **挂单 `impulse=-0.779500000004191`（≈−0.8）**（`journal#67017` 2026-08-30T10:12:15.924801Z 独立核实，与 CMD 报的 −0.8 一致，比 386 的 −4.2 更弱）；`btchour/strategy.py::coupon_rest_ready` + `config.py` L82（`impulse_wait_rest_min=0.0`）核实 NO 侧字面只要求 `impulse<0`，无幅度门槛，挂单合规但逼近边缘。
2. **持有期最深 `impulse=-32.999`（`journal#67050` 10:14:24.670173Z），成交瞬间 `impulse=+42.309`（`journal#67125` 10:23:14.062644Z，距 `filled_at` 仅 2.7 毫秒）**——全程无 $100 量级，成交前后约 75 点剧烈反手,独立核实成立,NO 路径不要求 $100 故不违规。
3. **`t_clip` 平仓 `pnl=+0.0645`**（`trades#387.pnl`）；flatten 时刻 `2026-08-30T10:29:15.947Z`（`raw.flatten.response.ts_ms` 独立换算）落在 `journal#67127`（10:23:21.887Z）与 `journal#67128`（10:29:23.377Z）之间的 **6 分 01 秒记录空档**正中,独立核实成立——退出是 `exits.py` 规则命中（`floor`/`gap<swing_runner_gap`）触发的机制性平仓,**不代表门变好**,但平仓瞬间缺少 journal 交叉验证,记入观察项。

（未建议改成交门到 0.45；未建议吃 taker；未建议放宽 `p=0.30`；未建议改 005/006/007/008/013；未建议切 live；未把 +0.0645 或本次平仓写成「门在改善」；本小时已 clip 不 hop 独立核实成立，不建议 hop。）
