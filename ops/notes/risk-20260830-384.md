# RISK 姿态报告 — 384 AUG3004 NO T78199.99（2026-08-30，挂单动量 −9.7 字面合规、坐 37 分钟 0 成交、整点 wait_invalid 过期撤、385 已接续且未破 004）

角色: RISK（decisions 010 触发；brief `docs/briefs/2026-08-30-RISK-384.md`）。只判「384 该不该按现行门（005）挂 / 0 成交整点撤后还该不该继续挂」，不下单、不改代码、不改门、不切 live、不对用户播报。
事实来源: `/workspace/data/btchour.sqlite`（`trades#384/385` raw + `journal#65600–65909`，2026-08-30T07:22–08:00:27Z / 03:22–04:00:27 EDT）独立抽查；`btchour/engine.py` L640–651 代码核实撤单机制。对照 `ops/notes/risk-20260830-383.md`（同型 dump_gap NO 第 10 次坐实）。

---

## 该不该按现行门挂：字面合规，独立核实动量与 CMD 一致

`trades#384` 挂单响应 2026-08-30T07:23:06.944114Z（03:23:06 EDT）。独立核实 `journal#65617`（07:23:06.695452Z）`impulse=-9.666`、`ask=0.33`，与 CMD 报的「动量 −9.7 / ask 0.33」一致（`raw.reason` 同源「dump_gap NO 看见 0.33 rest 0.25 动量 -10」，四舍五入到整数即 −10，精确值 −9.666→−9.7 更准）。005 对 NO 的字面要求是 `impulse<0` 且 32–42¢ 看见——`ask=0.33` 落在带内，`rest=0.25`，挂单不违规，这是 **dump_gap NO 子路径**（不要求 $100 门槛，那是涨/coupon YES 的要求），与 383 同一子路径、字面合规判断一致。

## 坐 37 分钟 0 成交：独立核实 ask 峰值实际比 CMD 报的更高（0.94 非 0.93），仍未成交

独立核实 `journal#65881–65883`（07:52:36.573687–07:52:44.164169Z，`event=AUG3004`，`reject` 含 `T78199.99 ask=0.94`）——持有期内 `ask` 峰值为 **0.94**，比 CMD 报的「到过 0.93」略高（方向一致，风险不低于 CMD 原判）。`trades#384.raw` 全程 `fill_count=0.00`、`live_fill=0.0`，确认零成交。价格从挂单时的 0.33 一路走到 0.94（标的持续下探，NO 公允值走高），但我方 `rest=0.25` 是被动买盘，远低于当时市场愿意成交的价位，导致「行情走对方向但订单结构性不可能成交」——这是 005 rest 定价机制本身的特征（不吃 taker、不追价），不是执行异常，RISK 无权也不建议改。

## 04:00 整点撤：代码级核实为过期撤，不是 +$100 翻面

`trades#384.status=cancelled`，`result=wait_invalid`。独立核实 `btchour/engine.py` L640–651：`wait_invalid` 由 `play in {lock_wait, impulse_wait}` 且 `not is_next_session_book(market, now)` 触发——纯粹是小时滚动后该单不再属于 `next_session` 而被机械撤单，与「反向 impulse 回到同向 $100」的确认逻辑（383 讨论的缺口）**无关**，两者是不同机制。CMD「过期撤不是 +$100 翻面」的判断经代码核实成立。

## 006 / 004 / 013 现场核对：0 成交不叠 skip，385 未破单张限制

- **006**：0 成交不是亏损，不触发「亏了空一小时」。`trades#385`（`event_ticker=KXBTCD-26AUG3005`）已于 08:00:10.984442Z（04:00:10 EDT）挂出，紧随 384 撤单，独立核实 `journal#65905`（08:00:10.737033Z）`event` 已切至 `AUG3005`、`impulse=-10.06`、`ask=0.42`，与 CMD「385 NO T78099 −10 / 0.42 resting」一致。
- **004**：当前 `trades` 表中 `status='working'` 仅 `385` 一笔，独立查询确认无第二张 live，未破「已有 1 张不再挂第二张」。
- **013**：385 仍在 resting、未成交、未平仓，不构成「working 被当残留刮掉」的场景，本笔不适用，亦未见违规。

## 姿态：**黄**

红档定义（RISK.md）是「静/弱阳真单、第二张 live、吃 taker、整条切 live」——384 都不占：挂单动量 −9.7（非静盘/弱阳，是真实走弱）、全程只 1 张 live、未吃 taker、未切 live、385 未破单张限制。判黄而非绿的理由：384 与 372/376（0 成交撤）、382/383（同型弱跌，383 且有「无 $100 确认仍成交」缺口）是同一 `dump_gap NO` 子路径的又一次样本，本次结果是「0 成交空转 37 分钟、ask 一度到 0.94 仍因 rest=0.25 结构性不可能成交」——不是亏损，但也不是门在良好运作，只是被动定价机制下的又一次空转。不把 0 成交写成门变好，也不因此判红。

---

## 调整菜单

1. **维持现状（`952cb97`）**：挂单合规，撤单机制（wait_invalid=整点过期，非 $100 翻面）代码级核实成立，004/006/013 均未破，无需改动。
2. **本小时（AUG3004）已随整点自动结束**：撤单已发生，385 已在新小时（AUG3005）按门挂出，此项已由代码自动完成，无需人工动作。
3. 未选（无「停挂等下一小时」的额外需求；006 判定 0 成交不叠 skip，AUG3005 已按门挂出 385，无需人工豁免）。
4. 未选（无改成交门至 0.45、吃 taker、放宽 `p=0.30`、整条切 live 的理由；384 是已知 `dump_gap NO` 子路径的又一次 0 成交样本，不构成升级充分条件）。
5. **建议派 LEARN 做对照**（brief 已并行派发）：372/376（0 成交撤）+ 382/383（同型弱跌，383 有成交无 $100 确认）+ 384（0 成交、ask 到 0.94 仍未成交）已形成足够样本，建议 LEARN 把「rest=0.25 结构性错过真实走势」和「无 $100 确认仍成交」两个子问题分开累计，不混为一谈；不把本次 0 成交解读为门变好或变坏。

## 是否 ESCALATE：**否**

## 关键数字（≤3）与出处

1. **挂单 `impulse=-9.666`（≈−9.7），`ask=0.33` 在 NO 32–42¢ 带内，`rest=0.25`**（`journal#65617` 2026-08-30T07:23:06.695452Z 独立核实，与 `trades#384.raw.reason`「动量 -10」及 CMD 报的 −9.7/0.33 一致）；005 dump_gap NO 字面合规。
2. **持有期 `ask` 峰值 0.94**（`journal#65881–65883` 07:52:36–07:52:44Z，`T78199.99 ask=0.94>0.42`，比 CMD 报的 0.93 略高），全程 `fill_count=0.00`/`live_fill=0.0`（`trades#384.raw`）——rest=0.25 结构性未获成交，非执行异常。
3. **`wait_invalid` 代码级核实**（`btchour/engine.py` L640–651：`not is_next_session_book` 触发，非 +$100 反向确认）；**385 已于 08:00:10.984442Z 接续挂出**（`trades#385.raw`，独立核实 `journal#65905` 同一时刻 `event` 切至 AUG3005），当前 `status='working'` 仅 385 一笔——004 未破。

（未建议改成交门到 0.45；未建议吃 taker；未建议放宽 `p=0.30`；未建议改 006/007/013；未建议切 live；未把 0 成交解读为门变好或变坏。）
