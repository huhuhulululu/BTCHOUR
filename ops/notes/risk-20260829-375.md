# RISK 姿态报告 — 375 AUG2914 NO T77999（2026-08-29，已成交，第二版）

角色: RISK（CMD 派发，`docs/briefs/2026-08-29-RISK-375.md`）。只判「该不该挂 / 该不该成交」，不下单、不改代码、不改门、不切 live、不对用户播报。
事实来源: `/workspace/data/btchour.sqlite`（`trades#375` + `journal` + `scans`）独立核实，代码路径核对 `btchour/engine.py`（`refresh_working` / `_wait_cancel_reason` / 一张锁）、`btchour/strategy.py`（`coupon_sides` / `impulse_wait_wrong_side`）、`btchour/config.py`（`impulse_min=100.0`）。判据: `docs/decisions.md` 004（一张 live_one）、005（NO 挂单 `impulse<0` 且 32–42¢；成交仍要同向 `|impulse|≥$100`）、006（亏了空一小时）、007（clip 后不 hop）。

> **更新说明**：CMD 抽查确认 375 已于 13:16:18 EDT 成交（1 张，`status=open`）。第一版「该继续停」结论已过期，本版改判「该不该成交」，总姿态与菜单同步重判。

---

## 该不该挂：该挂

`trades#375` 挂单时刻（2026-08-29T17:06:26.770231Z / 13:06:26 EDT）核实：`journal.impulse = -0.9318`，`raw.ask = 0.42`，`raw.rest = 0.25`。005 对 NO 的字面要求是 `impulse<0` 且 32–42¢ 看见——两条都满足，挂单本身合规。补充说明：-0.93 是噪声级负打印，接近 RISK.md 里「黄」的示例（挂在噪声负打印但仍在 005 字面内），不是「看见方向在砸」的强信号，但没有踩线，不构成撤销理由。

## 挂单后走势回顾（不变，供对照）：从未到 ±$100

独立核实挂单后的路径（journal，2026-08-29T17:06:26Z → 17:15:21Z）：
- 最深负向 impulse = **-28.31**（2026-08-29T17:07:27.786066Z），从未触及 `impulse_min=$100`（`btchour/config.py`）。
- 13:11:28 附近一度转正（scan `opportunity_count=0`，`diag=no_impulse`），随后再次转负，13:15:21 EDT 仍是 -0.97（弱负，仍守 NO 该守的边）。全程围绕 0 上下噪声波动，成交前**从未到 ±$100**。

## 该不该成交：**不该**（005 字面确认门未被满足，但这是交易所侧的结构性缺口，不是引擎/代码 bug）

独立核实：`trades#375` 现在 `status=open`，`raw.live_status=executed`，`raw.live_fill=1.0`，`raw.filled_at=2026-08-29T17:16:18.194901Z`（13:16:18 EDT），`raw.peak_bid=0.27`（未出）。

用 journal 交叉核实成交前后动量（成交瞬间前后有 ~84 秒采样空档，两端锚点足以判断方向）：
- 17:15:21Z（13:15:21 EDT）：spot 78041.63，impulse **-0.97**（仍是弱负）。
- 17:16:45Z（13:16:45 EDT，成交后 27 秒）：spot 78075.99，impulse **+41.58**，此后一路飙升到 17:17:08Z 的 +58.90。
- 成交瞬间（17:16:18Z，spot≈78058，落在上述两点之间）按 CMD 口径 impulse **+16**——方向已反（对 NO 是逆风），量级远未到 005 成交确认条款要求的同向 `|impulse|≥$100`。journal 两端锚点方向一致确认了这个反转，量级判断以 CMD 口径为准。

按 005 字面「成交仍要同向 `|impulse|≥$100` 且 ask 还在 rest」，这笔**不该成交**；但成交发生在交易所侧真实撮合——post-only rest 单一旦对手盘吃价，交易所不会检查我们的内部 impulse 门。代码核实：`refresh_working` 对 `live_one` 行（`btchour/engine.py` 526–562 行）只做「轮询交易所单状态 → 有 fill 就登记」，本身没有、也不该有「按内部动量否决交易所真实成交」的机制——这不是代码 bug，是 post-only 挂单机制的结构性风险，此前已提示过（第一版报告 + `ops/notes/risk-20260829-live.md` 第 374 笔）。

**模式确认，第三次重复**：
- 373：成交时反向 impulse +31.7（逆风）。
- 374：成交时全程未到 -$100（方向对但强度不够）。
- 375：**同时踩了两条**——成交时 impulse +16 反向（逆风，同 373），且挂单到成交全程最深也只 -28.31、从未到 -$100（不足量，同 374）。

三笔独立事件都落在同一个已知缺口——「成交时点的 005 同向 `|impulse|≥$100` 确认条款对交易所真实撮合不生效」——不是新问题，是第三次坐实同一个结构性缺口，值得持续盯防，不构成现在改门的理由（改门仍须走 LEARN→ADV→用户）。

## 当前敞口状态

1 张 NO 实盘持仓，成交价 0.25，`peak_bid` 曾到 0.27（未出）。成交后 spot 继续上冲（impulse +41.58→+58.90），方向对 NO 不利，敞口目前逆风、尚未出场。按 007，本小时已成交，不 hop、不再开新行权价；按 006，若最终结果是亏损，`AUG2915` 整小时 skip T——这两条是既有决策的自动推论，不是本报告新增建议，仅供 CMD 复盘对照。

---

## 总姿态：**黄**

理由：不构成「红」的定义情形（没有第二张 live、没有吃 taker、没有切 live）；但这是「成交确认门对交易所真实成交不生效」这个已知缺口的**第三次**独立坐实（373、374、375），且当前敞口方向逆风、尚未出场——不是绿灯，需要盯到这笔平仓，也建议 CMD 留意该缺口的累计频率（3/3 笔 live_one 命中），但仍是「盯防」而非「改门」判断。

## 调整菜单

- **1 维持 952cb97**：挂单逻辑本身没错，成交发生在交易所侧、不是引擎/门的执行错误，没有改门理由。
- **2 本小时空仓观察，不改门**：已成交，按 007 不 hop，本小时不再开新仓，等这笔出场结果。
- 未选 3（无新 skip 触发；006/007 已覆盖本小时后续动作，已经没有可挂的空间，无需额外「停挂」建议）。
- 未选 4（无理由改门/切 live/吃 taker/放宽成交门至 0.45——即便是第三次同缺口，仍不构成改门升级的充分条件，只是频率证据）。
- 未选 5（LEARN 已按 010 并行处理该笔）。

## 是否 ESCALATE：**否**

补充（非菜单项，仅供 CMD 参考，不是改门建议）：3/3 的 live_one 成交样本都显示「成交时同向 `|impulse|≥$100` 才该成交」这条 005 文字对交易所真实撮合未被强制执行。是否需要 ENG 评估能否在真实撮合层加一道「成交后核对」的事后标记（仅记录，不影响已发生的成交），交由 CMD 决定；RISK 不以此为由建议改门、放宽或切 live。

## 关键数字（≤3）与出处

1. **成交时刻 impulse ≈ +16（反向），spot 78058**，2026-08-29T17:16:18.194901Z / 13:16:18 EDT，CMD 抽查口径；journal 交叉核实区间 17:15:21Z（-0.97）→17:16:45Z（+41.58）方向一致确认反转，`trades#375.raw.filled_at` / `live_fill=1.0`。
2. **挂单到成交全程最深 impulse = -28.31，从未到 -$100**（`impulse_min=100.0`，`btchour/config.py`），2026-08-29T17:07:27.786066Z，journal——与 374 同型「无 $100 成交」。
3. **`peak_bid=0.27` 未出**，`trades#375.raw.peak_bid`——当前 1 张 NO 实盘敞口仍在场内，方向逆风（成交后 impulse 一路冲到 +58.90，17:17:08Z，journal）。

（未建议撤单/加仓；未建议切 live；未建议成交门放到 0.45；未建议吃 taker。）
