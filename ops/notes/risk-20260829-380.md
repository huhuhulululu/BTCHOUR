# RISK 姿态报告 — 380 AUG2921 NO T78199.99（2026-08-29，成交后 t_trail 止损，pnl −0.0804）

角色: RISK（decisions 010 触发；brief `docs/briefs/2026-08-29-RISK-380.md`）。只判「本小时（AUG2921）还挂不挂」「AUG2922 是否 skip」「无 $100 成交且反向 +46 是黄还是红」，不下单、不改代码、不改门、不切 live、不对用户播报。
事实来源: `/workspace/data/btchour.sqlite`（`trades#380` raw + `journal#61846–61860`，2026-08-30T00:00:00–00:18:10Z / 20:00–20:18 EDT）独立抽查；`python3 -m btchour board`（只读，009 允许，20:31:52 EDT 现场核对）；代码路径核对 `btchour/exits.py`（155–168 行 `t_trail` 逻辑）、`btchour/strategy.py`（803–807 行 `remember_swing_exit`、809–842 行 `remember_session_exit`、885–899 行 `allow_swing`）、`btchour/store.py`（259–288 行 `swing_memories`）。对照 `ops/notes/risk-20260829-378.md`、`ops/notes/risk-20260829-379.md`。

---

## 该不该挂（已发生，核实结果：挂单字面合规，但是本轮最弱的一次）

`trades#380` 挂单响应 2026-08-30T00:00:06.669Z（20:00:06 EDT），raw.reason 核实：`impulse = -4`，`ask = 0.37`，落在 NO 32–42¢ 带内，`rest = 0.25`。005 对 NO 的字面要求只是 `impulse<0` 且 32–42¢ 看见——满足,挂单不违规。但 −4 是 373（+0）/374（−0）之后本轮第三弱的样本,比 379（−36）弱一个量级,越接近 005 说的「静/弱阳不挂」的模糊边界（RISK.md 黄档定义里的擦边)。

## 该不该成交：无 $100 确认,且方向反转——第 7 次坐实同一结构性缺口,本次样本更极端

journal 逐笔核实,挂单后动量持续走弱至 20:14:16（`impulse=-0.39`）,随后无采样至 20:17:35 骤然转为 `impulse=+40.20`；`journal#61854`（2026-08-30T00:17:43.256Z,impulse **+46.32**）与 `filled_at`（2026-08-30T00:17:43.259687Z)相差 <4 毫秒,是成交那一刻的动量快照。005 的成交确认条款「仍要同向 `|impulse|≥$100` 且 ask 还在 rest」——本笔不只是未到 $100 门槛,连**方向都反了**（NO 需要同向为负,实际 +46.32 是强烈反向)。这与 373/374/375/377/378/379 同属一个已知结构性缺口（post-only rest 单被交易所真实撮合,内部 impulse/ask 门不对交易所生效),**380 是第 7 次坐实**,且是本轮里方向反转幅度最大的一次,不是新 bug,不构成改门理由（008 禁区）。

## 出场核实：t_trail 是引擎既有正常路径,不是执行 bug

`exit_reason=t_trail`,`btchour/exits.py:160` 逻辑核实：`peak_bid=0.34` 越过做T下限（floor）后武装追踪止损,回撤到 `bid=0.18` 触发 `settings.swing_trail=0.04` 门槛（0.34−0.18=0.16≥0.04)退出。`flatten.response.average_fill_price=0.8200`,换算 NO 侧 `exit_price=1−0.82=0.18`,与记账一致；`pnl = 0.18 − 0.0104(fee) − 0.25 = −0.0804`,算术核对无误。这与 379 的 013-reconcile 误平（代码 bug)不同类——380 全程是引擎设计内的正常止损执行,`013`「真成交后按 average_fill_price 落账」被正确遵守,不是执行层缺陷。

## 本小时（AUG2921）还挂不挂：不挂——市场层、代码层双闸都挡住,还剩约 28 分钟

1. **市场层没有点**：`board` 现场快照（20:31:52 EDT)：188 档,`$600` 内 12 档,YES 28–42¢ / NO 32–42¢ 活档均 **0**,姿态列「空仓·带塌了」,可挂 0,动量 −8。
2. **代码层已挡（007 的实现)**：`swing_memories`（`store.py:280`)对 380（`status=closed`,`play=impulse_wait`)调用 `remember_swing_exit`→`SwingMemory(dead=True, play="impulse_wait")`；`allow_swing`（`strategy.py:890-893`)对 `memory.dead=True` 且 `memory.play="impulse_wait"`（不在 `{impulse_t, swing_t}` 白名单)直接 `return False`——本小时（AUG2921)不再允许新的 coupon wait 开新行权价,赢/亏一视同仁,与 board 「本小时挂单」栏空仓一致。

## AUG2922 是否 skip：是——006 机械命中,不建议豁免

`remember_session_exit`（`strategy.py:817`)判据 `lost = (pnl is not None and pnl < 0) or reason in {...}`——`pnl=-0.0804<0` 直接命中,不看 `reason` 字符串是不是 `t_trail`（`t_trail` 本身也不在显式亏损 reason 集合里,但 `pnl<0` 已经足够触发)。`skip_next=True`,`last_loss_event=AUG2921`,下一个整点 `AUG2922` 会被 `refresh_session`/`allow_session` 钉成 skip。`board`「近几小时」现场核对：`AUG2921 | 1 | trail | −0.0804 | skip下一小时`,与代码判定一致。这是正常止损亏损（非执行 bug),006 字面直接适用,不建议豁免。

## 无 $100 成交 + 反向 +46：黄,不是红

红档定义（RISK.md)是「静/弱阳真单、第二张 live、吃 taker、整条切 live」——380 都不占：挂单是 NO 32–42¢ 带内合规挂单（不是静盘/弱阳挂 YES),全程只 1 张 live,未吃 taker（开仓 rest 0.25;平仓 IOC 交叉是 013 批准的例外),未切 live。判黄的理由：（a)成交确认无 $100 且方向反转,是第 7 次坐实的已知缺口,本次反转幅度（+46.32)是历史最大值,比 378/379 的「同向但不到 $100」更极端,标注为该缺口的高严重度子类,但因没有第二张 live、无真吃 taker、无整条切 live,不到红档门槛；（b)出场 `t_trail` 是引擎正常止损路径,不是像 379 那样的执行 bug,不额外加严重度。

---

## 总姿态：**黄**

## 调整菜单

- **1 维持现状（`952cb97`)**：挂单/成交两端在 005 字面内执行（挂单合规,成交是已知缺口的第 7 次坐实),出场 `t_trail` 是引擎正常路径,AUG2922 的 skip 由 006 自动判定,无需改动。
- **2 本小时（AUG2921)空仓观察,不改门**：市场层 0 活档 + 代码层 `dead` 双闸已挡,剩余 ~28 分钟按现状空仓即可。
- 未选 3（无新增「停挂等下一小时」建议需求;006 已自动判 AUG2922 skip,且当前小时本身已被 007 代码层挡死,无需额外动作)。
- 未选 4（无改门/切 live/吃 taker/放宽成交门至 0.45/放宽 `p=0.30` 的理由；成交确认缺口是已知结构性问题,第 7 次坐实且方向反转只是更极端的频率/幅度证据,不构成升级充分条件;是否值得转 ENG 评估接线加事后否决标记,交由 CMD 判断,不是本报告的改门建议)。
- 未选 5（010 已并行派 `docs/briefs/2026-08-29-LEARN-380.md`,无需 RISK 重复指派)。

## 是否 ESCALATE：**否**

## 关键数字（≤3）与出处

1. **挂单 impulse = −4,ask 0.37 在 NO 32–42¢ 带内**,2026-08-30T00:00:06.669Z / 20:00:06 EDT,`trades#380.raw.reason`；005 字面合规但是本轮第三弱样本（弱于 379 的 −36)。
2. **成交瞬间 impulse = +46.32（反向,与 filled_at 相差 <4ms)**,2026-08-30T00:17:43.256Z,`journal#61854`——373/374/375/377/378/379/380 同一缺口第 7 次坐实,本次反转幅度历史最大,仅作观察记录,不建议改门。
3. **pnl = −0.0804,exit_reason=t_trail（peak 0.34→0.18,引擎正常追踪止损)**,`trades#380.raw`；006 机械判定 `lost=True`（`pnl<0`)→ `AUG2922` skip（`board` 现场核对 `AUG2921 | trail | −0.0804 | skip下一小时`),不建议豁免。

（未建议改成交门到 0.45；未建议吃 taker；未建议放宽 `p=0.30`；未建议改 007；未建议豁免 AUG2922 skip；未建议切 live。）
