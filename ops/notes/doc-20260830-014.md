# DOC — 014 对齐清单

日期: 2026-08-30
角色: DOC
对齐对象: `docs/briefs/2026-08-30-DOC-014.md` / `docs/briefs/2026-08-30-014-all-roles.md` / ENG 落地（`btchour/config.py` `impulse_wait_rest_min=100.0`）

## 应对齐的句子（写「跌 impulse<0 才挂 NO」，应改成「跌 |impulse|≥$100 且 32–42¢ 看见才挂 NO」）

### 本次已改（写权路径内）

1. `catalog/rules/plays.md:12`（灵活编排「跟带子走」行）
   - 旧：`跌（impulse\<0）且 32–42¢ 看见才挂 NO；涨（\|impulse\|≥$100）挂 YES。`
   - 新：`跌 \|impulse\|≥$100 且 32–42¢ 看见才挂 NO；涨 \|impulse\|≥$100 挂 YES。`
2. `catalog/rules/plays.md:23`（`impulse_wait` 表格行）
   - 旧：`跌（impulse\<0）且 32–42¢ 看见才挂 NO；涨（\|impulse\|≥$100）挂 YES（28–42¢）。`
   - 新：`跌 \|impulse\|≥$100 且 32–42¢ 看见才挂 NO；涨 \|impulse\|≥$100 挂 YES（28–42¢）。`

### 检查未发现（board.md）

- `catalog/rules/board.md` 全文搜索「跌」无命中；未见「跌 impulse<0」字样，无需改。

### 发现但不在本 brief 写权路径内，留给下一轮 / 其他角色

3. `catalog/rules/swing.md:29`：`跌（impulse\<0）且 32–42¢ 看见才挂 NO。涨 = |impulse|≥$100 才挂 YES（28–42¢）。` —— 同样的「跌=impulse<0」旧句，`swing.md` 不在本 DOC brief 可写路径（brief 只列 `plays.md` / `board.md`），未改，标记待下一份 DOC brief 或 CMD 点名。
4. `catalog/rules/swing.md:31`：`门：整条阶梯 $600。跌挂 NO 32–42¢，涨挂 YES 28–42¢…` —— 未含明确 `impulse<0` 字样（只是「跌挂 NO」），暂不判定为需改句，供 CMD/ARCH 复核。

### 未动（decisions.md 正文，CMD 定稿权）

- `docs/decisions.md` 005：`跌 = \`impulse < 0\` 且 32–42¢ 看见才挂 NO（29¢ 飞刀不接）` —— 这是 CMD 已 accepted 的决定正文，本次未改一个字。若 014 候选 2（`impulse_wait_rest_min` 默认 100，005 修订：跌侧挂也要 `|impulse|≥$100`）经 LEARN∥ADV 总判后由 CMD 定稿，需要 CMD 自己在 `decisions.md` 追加新条目（不改 005 原文，按「追加写入，不修改已有条目」的规则），本 DOC 会话不代做。

## 未做的事（禁区自查）

- 未改 `docs/decisions.md` 正文。
- 未碰 `btchour/`。
- 未 `git push`。
- 未对用户说话。
- 未发明新门：本次改动只是把 plays.md 里已经和 ENG 落地代码（`impulse_wait_rest_min=100.0`，涨跌同门）对齐的措辞更新，没有引入新阈值或新玩法。
