# LEARN — 011 用满阶梯：为什么 AUG2914 188 档只做成 1 笔噪声 live（2026-08-29）

角色: LEARN（brief `docs/briefs/2026-08-29-LEARN-011-ladder.md`，decisions 011 提权立题）。
事实来源: `catalog/snapshot/latest.json`（只读抽查）、`data/btchour.sqlite` journal/trades（只读）、`catalog/snapshot/sweep.json`（对照，绿了也不是达成）、`btchour/strategy.py` / `btchour/learn.py` 代码走读。

---

## 一、先纠偏「188 档」这个分母（抽查通过）

`latest.json` synced 17:53:59Z，`current_hour.market_count` = **188**，与 CMD 报数一致。但 188 是从 $70,100 起的**全阶梯行权价计数**，不是可操作面：

| 层 | 档数 | 出处 |
|---|---|---|
| 整点全阶梯 | 188 | latest.json `current_hour.market_count` |
| spot ± $600 内 | **12** | 同快照，strike 距 spot 78043 ≤ $600 |
| 32–42¢ 活档（yes 或 no ask 在带内） | **0** | 同快照（收盘前 6 分钟，最近档 NO 0.12，次近 0.96） |

$100 一档、±$600 恒定只有 ~12 档；带内活档任一时刻 0–2 档，只有真动量把 spot 推过多个行权价时才会多档穿带（AUG2520 三档 0.20–0.29 就是那种时刻）。**011 的提权不被这纠偏否定**：优势的单位是「每小时刷新一条阶梯 × 动量时刻多档穿带」，不是 188 个同时可打的点。但心跳照 011 报三个数（188 / 12 / 带内活档数）时，第三个数才是机会计数。

## 二、AUG2914 本小时：1 笔不是「少做了」，是「多做了一笔噪声」

journal 17Z 共 **442 次扫描**：impulse 全小时区间 **−80.2 ～ +64.4，0 次到 ±$100**；诊断 no_coupon 267 次（最近档 ask 全在带外：0.44–0.66 或 0.26 飞刀）、wait 122 次（全是 375 那张在挂）、no_impulse 53 次。带内 32–42¢ 基本只在 17:06 那一瞬存在（T77999.99 ask 0.42 上沿），375 就挂在那一瞬。

结论：按现行 005 门，这个小时的正当产出是 **0 笔**。375（live_one，0.25 进 0.33 出，t_clip +0.0645）是静盘噪声字面过门 + 反向行情逆向选择成交（`ops/notes/learn-20260829-375.md` 已判「不是达成」），侥幸摸到 clip 不改变性质。**AUG2914 空仓本来是正当的**——「188 档只做成 1 笔」在这个小时不是策略漏机会，是分母用错了。

## 三、真正漏掉阶梯的地方：真机会小时被噪声单的连锁后果烧掉

把镜头拉到全天（journal 逐小时 |impulse| 峰值）：最近 24 小时只有 **7 个小时**出现过 ≥$100 真动量；08-29 白天是 14Z（+205）、15Z（−122.7）、16Z（+125.7）三个，17Z（AUG2914）没有。三个真动量小时全部没打出规矩单：

1. **AUG2911（14Z）**：373 在噪声里挂 NO，14:39 反向成交，t_wait_stop −85.1%，pnl −0.2127。
2. **AUG2912（15Z）——全天最痛**：15:03:37Z impulse **−111** 真砸进行中，T77799.99 NO **ask 0.38** 在带内（随后 T77899.99 0.41 也在带），诊断 status=wait 共 **221 次扫描**——这是金带子 AUG2520/AUG2608 的标准形态。但 trades 表该小时 **0 笔**：373 的亏损触发 006 空小时，整小时锁死。全天唯一「多档真 coupon」小时就这样烧掉。
3. **AUG2913（16Z）**：374 又在噪声里挂 NO（16:13），16:15 成交、t_clip +14.1%（+0.0353 运气）；两分钟后 16:17:16Z 真涨 **+105** 到来，T78099.99 YES **ask 0.38** 带内、诊断 wait——但 374 已 clip → 007 dead，不再挂。真涨 coupon 让位给了噪声 clip。

机制归一：`coupon_rest_ready` 的 NO 侧只要 `impulse < 0`（`impulse_wait_rest_min` 默认 **0**），−0.93 / −5.5 这类噪声也算「跌」。噪声 rest 本身是逆向选择通道（373/375 反向成交、374/375 无 $100 成交，三连已证），**而且它的亏损/clip 通过 006/007 把相邻真机会小时也锁死**。006、007 都是该守的门，错的不是门，是让噪声单去触发它们。

另：005 第五条成交门 / 淡化撤在 live loop 事实上不生效（375 复盘已升级 ENG，本轮重申，不重复展开）。

## 四、AUG2915 起的提案（不改门数字，最多 3 纸 / 1 真）

1. **用满 3 纸的正路径 = 真动量多档时刻**。`pick_dump_wait` / `allow_swing` / engine 本就支持同小时 3 档近 ATM rest（AUG2520 三档先例）。当同向 |impulse| ≥ $100 且带内 ≥2 档（AUG2912 15:03 型：0.38 + 0.41），**3 纸名额全部挂上**，不要只挂最近一档。排序沿用现行代码次序：带内优先 → 距 ATM 最近 → ask 最逼近 0.25 → EV。
2. **1 真的选档**：3 纸中距 ATM 最近且 ask ≤ 0.42 的那档给 live_one；已有 1 张 resting 不加第二张（004 不动）。
3. **正当空仓的定义**（写进心跳，不许含糊）：整小时至今无同向 ≥$100 print **且** 32–42¢ 带内 0 档 → 空仓正当（AUG2914 17:20 后即此形态），但必须报 188 / 12 / 0 三数。反之 AUG2912 型（带内活档 + 真砸 + 被 006 锁）不许报「没机会」，board 须标「skip 烧掉真 coupon」。
4. **交 ADV 的改门议题（须用户点头，本轮不动）**：给 `impulse_wait_rest_min` 一个非零量级（挂 NO 前置同向动量下限），掐断噪声 rest → 逆向成交 → 006/007 锁死真小时的链条。这是 005 参数，LEARN 只立题不改。对开面给 ADV：量级门会不会把 AUG2608 型「砸势初段 0.25 rest」也挡掉（AUG2912 15:03 挂时 impulse 已 −111，不会被 $40–60 级别的门挡）。
5. **不回退项（重申）**：不看见 32–42¢ 就挂、不 0.45、不 taker、不 p=0.30、本小时 AUG2914 已 clip 不 hop、不把 15m 坐等当默认。

## 五、总判决

**判决词: 可采纳候选**（指第四节 1–3 的用满阶梯操作提案；第 4 条改门议题单独标「须 ADV + 用户」。AUG2914 的 375 本身维持「不是达成」，sweep `flex_skip_16h` 0/0/0、`flex_nowait` 绿单均为对照，不是达成也不是现行路径。）

### 关键数字（≤3）

1. **188 → 12 → 0**：全阶梯 188 档，$600 内 12 档，快照时刻带内活档 0 —— `catalog/snapshot/latest.json`（17:53:59Z，market_count 抽查一致）。
2. **442 次扫描 0 次 |impulse| ≥ $100（峰值 −80.2 / +64.4）**：AUG2914 按门正当产出 0 笔，375 是噪声多做 —— `data/btchour.sqlite` journal 17Z。
3. **AUG2912 15:03:37Z impulse −111、T77799.99 NO ask 0.38 带内、221 次 wait 诊断、0 笔挂单**：全天唯一真砸 coupon 被 373（pnl −0.2127）触发的 006 空小时烧掉 —— journal 15Z + trades#373。

### 是否建议进 ADV

**是**。两个议题：① `impulse_wait_rest_min` 非零量级门（改 005，须用户）；② 真动量时刻 3 纸全挂的对开面（会不会三档互踩 / 放大单小时敞口，AUG2520 三档先例 vs 004 单真约束）。

### 禁区自查

未改代码、未改门、未 git commit/push、未对用户播报；未建议 hop（AUG2914 已 clip）；未把回放绿 / flex_nowait 写成达成或现行路径；未建议看见就挂 / 0.45 / taker / p=0.30；未把 15m 坐等写成小时盘默认。latest.json 只读未提交。

---

## CMD 抽查（2026-08-29 14:01 EDT）

| 数字 | LEARN | CMD | 判 |
|---|---|---|---|
| 整点档 | 188 | latest `market_count` 188 | 过 |
| AUG2914 journal | 442 次，峰值 −80.2/+64.4，0 次 ±$100 | sqlite 471 行（LEARN 写稿后又扫了几十次），min −80.16 max +64.44，≥100 = 0 | 过（峰值对） |
| AUG2912 15:03:37Z | −111 / T77799 NO 0.38 / 221 wait / 0 成交 | 15:03:37 impulse −110.6 status=wait T77799 ask=0.38；wait 221；trades 该小时空 | 过 |
| AUG2913 真涨让位 | T78099 YES 0.38 | 16:15:58 起 +100 先是 T78199 YES **0.29**（带内）；T78099 当时 0.43 带外 | **半过**：真涨存在、007 锁死成立，档号写错 |

14:00 sweep：`flex_skip_16h` 仍 0/0/0，不是达成。`flex_nowait` 绿单不是现行路径。

CMD 收口：第四节 1–3（真动量多档把 3 纸挂满、1 真给最近 ATM、心跳报三数）可先执行，**不改 005 数字**。`impulse_wait_rest_min` 非零 = 改 005，须用户点头才派 ADV。本小时已过 14:00，AUG2915 可做 T（上小时 clip 赢，不 skip）。
