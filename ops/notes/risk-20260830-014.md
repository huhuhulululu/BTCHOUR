# RISK 姿态报告 — 014 停 live_one + 旧 005 字面复核（2026-08-30）

角色: RISK（brief `docs/briefs/2026-08-30-RISK-014.md`；同源 `docs/briefs/2026-08-30-014-all-roles.md`）。只判「旧 005 字面还该不该往 live 送」「停 live_one 该不该现在做」，不下单、不改代码、不改门正文、不切 live、不对用户播报、不 git push。
事实来源: `/workspace/data/btchour.sqlite`（`trades` 表 372–388 独立核实）；`git log`/`git diff`/`ps` 对工作区与运行进程的独立核实；`ops/notes/risk-20260830-382~388.md`（本 RISK 已核实的逐笔挂单动量）；`ops/notes/adversarial-20260830-014.md`（ADV 已交付）；`docs/decisions.md` 003/004/005/013。LEARN 判决词（`ops/notes/learn-20260830-014.md`）截至本报告成文时**尚未落地**，本报告不等 LEARN，先给 RISK 独立灯，供 CMD 总判参考。

---

## 1. 灯：**黄**（非绿、非红）

红档定义（RISK.md：静/弱阳真单、第二张 live、吃 taker、整条切 live）字面上均未破——独立核实 `trades` 表 372–388 全程仅 1 张 `live_one` 同时 resting（004 未破）、`taker` 均为 0（008 未破）、`mode` 全部 `paper`（整条 loop 未切 live，003 未破）。不判红。

不判绿的理由：17 张 `live_one` 里连续多笔（382→388）挂单动量单调走弱且从未在成交时刻回到同向 `|impulse|≥$100`（见第 3 节数字），388 已探到 −0.17，逼近纯噪声——这正是 RISK.md 里「看起来在成交但挂在静盘上」的模式，只是发生在 NO 侧而不是 YES 侧的字面表述里。字面没有被违反，但字面本身（`impulse_wait_rest_min=0.0`）已经不足以把「静/弱跌」和「真跌」分开，继续按此字面挂被判黄。

## 2. 要不要 ESCALATE：**否**

ESCALATE 清单（改 005–008、切 live、吃 taker、放宽 p、成交门 0.45）没有一项被本报告触碰。本报告建议的动作是**收紧**（停送真单），不是放宽，不需要走「改门只能建议、由 CMD 问用户」的那条不可逆审批线；而且用户 07:42 EDT 已经给过前提性授权（「如果没有新的改善或变化，就不要做了」）——执行「没有改善就停」不是新开一张改门申请，是兑现已给的指令。005/006/007/008 正文本身是否要改（例如 `impulse_wait_rest_min` 该不该定在 100），仍须等 LEARN→ADV→ARCH→CMD→用户，本报告不建议现在动那条线。

## 3. 旧 005 字面（`impulse_wait_rest_min=0.0`）还该不该继续往 live 送：**不该**

独立核实 `trades` 表（不依赖 CMD 摘要）：

- `live_one` 共 **17** 张：372（YES 撤/`wait_wrong_side`）、376（YES 撤/`wait_wrong_side`，独立核实 `raw.reason`「动量 +104」——**已经满足 $100，不是被弱信号挡下，是被翻面挡下**，与全员 brief「373–388 全挡」的表述不完全对应，采信 ADV 已指出的这一点）、384（0 成交撤/`wait_invalid`）；其余 **14 张全部 closed**，pnl 加总 **+0.0677**（与 CMD 摘要一致，独立核实成立）。
- 14 张里，NO 侧挂单动量逐笔独立核实（`journal` 表，见各笔 risk 笔记）：382 **−15.15** → 383 **−7.24** → 385 **−10.06** → 386 **−4.23** → 387 **−0.78** → 388 **−0.17**。近 6 笔没有一笔在挂单或成交时刻见到同向 `|impulse|≥$100`；377/378/379/380/381 同样如此（此前笔记已坐实，属同一缺口第 5–15 次）。
- **净 +0.0677／14 笔**是噪声量级（单笔 fee 已到 0.013–0.015），不构成「新的改善」；`t_scratch`（388，pnl **−0.0708**）是 480 秒到点机制性止损离场，**不写成纪律成功**——止损触发说明规则在按代码设计工作，不说明「继续挂」这件事本身是对的。
- 用户给的达成线是 AUG2520 多档 NO / AUG2608 dump NO clip（挂 −226、成交 −191 量级）。373–388 挂单峰值最深仅到 −17.9（journal 瞬时读数，382 挂单 −15.15），没有一笔在**同一数量级**上逼近达成线。按用户 07:42 的标准（没有新改善就不要做了），旧字面继续挂**没有**产出这种改善，不该继续往 live 送。

**但要澄清一点，避免把「停」写成「005 已经被修好」**：ADV 已经指出（`ops/notes/adversarial-20260830-014.md`），CMD 起草的补丁默认 `live_one=False` 时，新增的 `wait_fade` 撤单分支不可达，`impulse_wait_rest_min=100` 单独看等于回到 005 决策原文已经否决过的「3 分钟 $100 才挂」，且会同时挡掉 384 那种实盘已见的真砸盘小时（最深仅 −41）。本报告同意 ADV 的定性：**该补丁目前只是「安全停机」，不等于「005 字面已被验证修订」**——这条判断留给 LEARN/ADV/ARCH 总判，本报告不代其下结论，只确认「停」本身该做。

## 4. 停 live_one 之后 paper 还扫不扫：**扫，不停**

代码级独立核实 `btchour/engine.py` L891–903（`run_cycle`）：`live_one` 只包裹 `cancel_stale_live_rests` / `reconcile_live_one`（真单通道），`scan_once`（L903）与 `settle_open`（L902）在 `live_one` 之外无条件执行。停 `live_one` 只掐真单发送通道，不影响 003（默认 paper）、011/012（阶梯扫描是工作盘，要继续用满）。MON 已独立核实 scan 间隔 ~4s、无滞留（`ops/notes/mon-20260830-014.md`）——停 live_one 后这条应保持不变，纸盘继续扫、继续记录，为下一次达成判断攒样本，不是整条 loop 停摆。

## 5. 不重启除非滞留：**MON 结论字面成立，但有一条事实必须报给 CMD**

MON 07:49 EDT 已核实无滞留（scan ~4s、末扫落后仅 ~3s），字面上「不重启」成立，本报告不推翻 MON 的判断，也不建议 RISK 越权判断进程死活。

但独立核实发现一个与本题直接相关的事实，MON 07:49 的检查时点在这之前，需要补一条：**014 补丁已经提交到代码库，但当前运行进程还没吃到它**。

- 补丁提交 `d7e2ae1`（`014: green tests for rest_min=100, live_one off, live wait_fade`）落地时间 **2026-08-30 11:50:27 UTC（07:50:27 EDT）**。
- 当前 `run` 子进程 PID `320036` 启动时间 **2026-08-30 07:55:24 UTC（03:55:24 EDT）**——比补丁提交早了近 **4 小时**。
- 代码级核实 `btchour/cli.py` L63 `settings = load_settings()` 只在进程 `main()` 启动时调用一次；`btchour/engine.py` L972 `run_loop` 复用同一个 `settings` 对象，循环内不重新读配置。
- 结论：**运行中的 `live_one` 此刻实际值仍是旧的 `True` / `impulse_wait_rest_min=0.0`，不是补丁写的 `False` / `100`**。补丁只在磁盘上生效，尚未在运行系统里生效——第 3 节「不该继续送」的判断，此刻仍在被旧字面执行。

这不是滞留（进程没死、扫描没停），所以不落在「滞留才重启」的口子里；但这意味着「要不要重启去让补丁生效」是一个**新的、独立于滞留判断的问题**，答案不该由 RISK 单方给（RISK 不碰代码、不碰进程），只能把这条事实摆给 CMD：**如果 CMD/LEARN/ADV 已经认可「先停 live_one」这个动作本身，那么不重启就等于补丁没真正生效，旧字面会继续挂到下一次进程自然重启（supervisor 只在扫描滞留时才会拉起新子进程）**。ENG-014 brief 已明确「不重启 loop」是 ENG 的禁区，本报告不越权建议由谁执行，只确认这个信息缺口存在。

---

## 调整菜单

1. 未选（维持现状 `952cb97`）：不选，因为第 3 节的证据（14 笔净 +0.0677 噪声、6 笔连续无 $100 确认、无新达成样本）已经落入用户 07:42 的「没有改善就不要做了」条件。
2. 未选（本小时空仓观察，不改门）：不够——问题不是单个小时，是跨 6–15 笔的持续模式，应升到「停」而非「本小时观察」。
3. **选：建议停挂 / 现在做（skip 规则内，不是等下一小时）**。理由见第 3 节；这是收紧动作，且用户已预先点头，不需要走「改门须问用户」那条线。同时把第 5 节的事实（补丁未在运行进程生效）一并交给 CMD，因为「建议停」如果不配合「让运行进程吃到新配置」，建议本身不会真正落地。
4. 未选（ESCALATE：改 005–008、切 live、吃 taker、放宽 p、成交门 0.45）：无——本报告不建议、也未发现需要这类改动的理由。
5. **选：并入已在进行的 LEARN/ADV 总判，不新开题**：ADV 已交付（`ops/notes/adversarial-20260830-014.md`，建议降级为「安全停机补丁」，不称金带改进/005 可采纳修订）；LEARN 判决词尚未落地。本报告的「该停」判断独立于 LEARN 的最终判决词，但若 LEARN 判「已否定」或「弱候选」，「停 live_one」这个动作本身（区别于 `rest_min=100` 这个具体数值）仍然成立，因为它是用户 07:42 指令的直接执行，不依赖补丁数值是否被采纳。

## 是否 ESCALATE：**否**

## 关键数字（≤3）与出处

1. **`live_one` 17 张 / 14 张 closed / 净 pnl +0.0677**（`trades` 表独立 sqlite 核实，与 CMD 摘要一致；376 独立核实 `raw.reason`「动量 +104」——已达 $100、是被 `wait_wrong_side` 而非弱信号挡下，修正「373–388 全挡」的表述）——量级是噪声，不是新改善。
2. **NO 挂单动量连续 6 笔走弱且全无同向 $100 确认**：382 −15.15 → 383 −7.24 → 385 −10.06 → 386 −4.23 → 387 −0.78 → 388 −0.17（各笔 `journal` 表独立核实，出处见 `ops/notes/risk-20260830-382~388.md`）；388 `t_scratch` pnl −0.0708 是机制性止损，不写成纪律成功。
3. **补丁提交 `d7e2ae1` 于 2026-08-30 11:50:27 UTC（07:50:27 EDT），运行进程 PID 320036 于 07:55:24 UTC（03:55:24 EDT）启动**——早于补丁近 4 小时；`btchour/cli.py` L63 + `engine.py` L972 独立核实 `load_settings()` 只在进程启动时读一次，运行中 `live_one`/`impulse_wait_rest_min` 此刻仍是旧值（`True`/`0.0`），补丁尚未在运行系统里生效，是否重启激活不属 RISK 决定范围，转 CMD。

（未建议改成交门到 0.45；未建议吃 taker；未建议放宽 `p=0.30`；未建议改 005/006/007/008 正文；未建议切 live；未把 388 的 −0.0708 或 14 笔净 +0.0677 写成纪律成功或门变好；未越权建议重启，只报告运行进程与补丁的时间差事实。）
