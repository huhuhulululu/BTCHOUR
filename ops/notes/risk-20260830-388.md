# RISK 姿态报告 — 388 AUG3008 NO T78099.99（2026-08-30）

角色: RISK（decisions 010 触发；brief `docs/briefs/2026-08-30-RISK-388.md`）。只判 388 该不该按现行门挂 / journal 断档语义 / scratch 记账 / flatten 是否过 013 / 空仓守没守住，不下单、不改代码、不改门、不切 live、不对用户播报、不 git push。
事实来源: `/workspace/data/btchour.sqlite`（`trades#388` raw + `journal#67290–67421` + `scans#99159–99183`，2026-08-30T11:05:00–11:18:35Z / 07:05:00–07:18:35 EDT）独立抽查；`btchour/strategy.py::coupon_rest_ready`、`btchour/exits.py` L93–115（`t_scratch`）、L810–841（`remember_session_exit`）、`btchour/engine.py` L166–176（journal 写入条件）、`btchour/config.py`（`impulse_min=100.0`、`impulse_wait_rest_min=0.0`、`impulse_wait_scratch_seconds=480.0`、`swing_target=0.10`）核实代码逐字对应。对照 `ops/notes/risk-20260830-387.md`（386/387 同型样本）。

## 1. 灯：**黄**

红档定义（004 第二张 live / 006 亏了不空一小时 / 007 已 clip 又 hop / 008 放宽门吃 taker / 整条切 live）388 均未破；判黄是因为挂单动量三笔连续走弱（386 −4.2 → 387 −0.8 → 388 **−0.17**，`journal#67290`），388 已逼近静盘噪声值，字面仍合规但信号质量持续恶化，值得记入观察而非当噪音忽略。

## 2. 要不要 ESCALATE：**否**

004/006/007/008/013 均未破，没有需要用户点头改门的理由；388 的问题是「信号质量趋弱」的观察项，不是门被绕过。

## 3. 弱跌 −0.2 挂，005 成交门挡没挡住

没挡住，但字面合规：独立核实 `journal#67290`（2026-08-30T11:05:02.779705Z）`impulse=-0.174833…`（≈−0.2），`btchour/strategy.py::coupon_rest_ready` 在 `impulse_wait_rest_min=0.0` 下 NO 侧只要求 `impulse<0`（无幅度门槛），`ask=0.33` 在 32–42¢ 带内——挂单合规，但 −0.2 是三笔连续样本里最弱的一次（386 −4.2、387 −0.8、388 −0.17），比「压线」更贴近零。

## 4. journal 断档 77.7s 成交落在里面，撤单/成交语义有没有缺位

**没有缺位，断档是选择性写日志，不是进程滞留**：独立核实 `scans#99159–99183` 在整个 07:05:33–07:07:07 EDT 窗口连续 ~4s 一条（含成交瞬间 `scans#99168` 11:06:08.254782Z，与 `trades#388.raw.filled_at` 11:06:08.256930Z 相差 2.2 毫秒），扫描从未停；`btchour/engine.py` L169（`if abs(merged) >= 40 or diagnosis.get("status") != "no_impulse"`）证明 `journal` 只在动量≥40 或有活跃诊断时才落一行，这段动量始终 <1 且成交后诊断转 `no_impulse`，按代码design 就不写——journal 的 77.7s 空白是过滤器行为，成交本身被 `scans`/`trades` 完整、精确记录。**未能独立核实的一点**：CMD 报的「两端动量 −0.2 / −4.3」，本次独立抽查在断档两端只查到 `journal#67301`（11:05:45.38，−0.77）与 `journal#67302`（11:07:03.09，+40.86），未见 −4.3；不影响结论（断档=选择性日志），但数字对不上，记录存疑。

## 5. scratch −0.0708 + 引擎 skip AUG3009，账和 skip 对不对

**对**：`btchour/exits.py` L93–115，`impulse_wait_scratch_seconds=480.0`，`raw.exit_note`「after 481s peak 6.3% < 10%」与持有期 11:06:08.257→11:14:09.531（≈481s）算术一致；`btchour/strategy.py::remember_session_exit` L817–820 将 `t_scratch` 直接归入 `lost` 集合（不看 pnl 正负都判损失），388 的 `pnl=-0.0708` 双重命中「亏损」判定，`skip_next=True` 会在 AUG3009 事件出现时挂起——本次不把 scratch 写成纪律「成功」,它是到点机制性止损离场,账目算术核实成立,skip 触发逻辑与代码一致。

## 6. flatten YES 0.81 滑 8 tick，013 过了没有

**过了**：`trades#388.raw.flatten.response`：`fill_count="1.00"`、`average_fill_price="0.8100"`，非 `paper_close` 空成交，`exit_price=0.19`（=1−0.81，NO 侧换算）与 `raw.exit_price` 一致；`book_side="bid"`、`price=0.83`、`slip_ticks=8` 说明交叉挂 0.83 实际按 0.81 成交（比意图价更优，非更差滑点）——013「真实成交后落账」满足。

## 7. 空仓、无第二张、不 hop，守没守住

**守住**：`trades` 表 388 为本 sqlite 最大 id，AUG3008 全程无 389；`python3 -m btchour status` 只读核查 `open=0`；结构性核实 `btchour/store.py::swing_memories`（L265–283）对任何已关闭的 `impulse_wait`/`impulse_t`/`swing_t` 交易（不限 `t_clip`）都调用 `remember_swing_exit` 把该 event 标记 dead，388 的 `t_scratch` 关闭同样锁死 AUG3008 本小时再入场，07:17–07:18 EDT 之后的 `no_coupon` 拒绝（ask>0.42 / ask<=rest）是常规门拒绝,不依赖 hop 锁也会被挡。

## 8. loop paper，不要切 live；scan ~4s 不要因写侧断档重启

**均满足**：`trades#388.mode="paper"`，`live_one=true`/`live_order_id` 是 004 允许的单张真单通道,非整条切 live；`scans` 表独立核实 07:05:33–07:18:35 EDT 全程 ~3.7–4.0s 一条,包括争议的 journal 断档窗口,进程健康,**不建议重启**,journal 空白按第 4 条已确认是选择性写日志。

## 9. 要不要派 ADV

**建议并入 384 已建议的题,不新开**：LEARN 已对 384 建议派 ADV，CMD 未派；388 补的证据是「NO 挂单动量逐笔走弱（−4.2→−0.8→−0.17）且已逼近静盘噪声」，这条应作为该 ADV 题的追加样本一并评估，本报告不建议为 388 单独起题，也不建议改门。

---

## 调整菜单

1. **维持现状（`952cb97`）**：004/006/007/008/013 均未破，388 挂单/成交/平仓字面合规。
2. **建议本小时（及 AUG3009）加强观察**：AUG3009 已被引擎结构性 skip（第 5 条），本项为确认，不改门。
3. 未选（本小时已 scratch 平仓，无未平敞口，007 结构性已满足）。
4. 未选（无改成交门 0.45、吃 taker、放宽 `p=0.30`、整条切 live 的理由）。
5. **建议 LEARN/ADV 把 388 并入 384 已建议的对抗题**：连续三笔 NO 挂单动量走弱（386 −4.2、387 −0.8、388 −0.17）已构成趋势,值得归纳评估 005 是否需要幅度下限,但改门仍须 LEARN→ADV→用户,本报告不建议现在动。

## 是否 ESCALATE：**否**

## 关键数字（≤3）与出处

1. **挂单 `impulse=-0.174833…`（≈−0.2）**（`journal#67290` 2026-08-30T11:05:02.779705Z 独立核实）——三笔连续样本里最弱一次（386 −4.2 → 387 −0.8 → 388 −0.17），`btchour/strategy.py::coupon_rest_ready` + `impulse_wait_rest_min=0.0` 确认 NO 侧字面无幅度门槛,挂单合规但趋弱。
2. **`scans#99159–99183` 连续 ~4s 一条（含 `#99168` 11:06:08.254782Z 与 `filled_at` 11:06:08.256930Z 相差 2.2ms）**——独立核实进程扫描全程未停,journal 77.7s 断档是 `engine.py` L169 选择性写日志过滤器行为,不是滞留,不建议重启;CMD 报的「两端动量 −4.3」未能在断档另一端独立复核到,记入存疑。
3. **`t_scratch` `pnl=-0.0708`，`held≈481s`（`raw.exit_note` 481s / peak 6.3% < `swing_target` 10%）**——`btchour/exits.py` 规则命中的机制性到点止损,`remember_session_exit` 把 `t_scratch` 计入 `lost`,`skip_next` 会挂起 AUG3009,账目与 skip 逐字核实一致,**不写成纪律成功**。

（未建议改成交门到 0.45；未建议吃 taker；未建议放宽 `p=0.30`；未建议改 005/006/007/008/013；未建议切 live；未把 −0.0708 或本次 scratch 写成「纪律成功」；388 不单独起 ADV 题,建议并入 384 已建议的对抗题。）
