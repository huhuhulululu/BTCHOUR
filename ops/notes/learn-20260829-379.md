# LEARN 复盘 — live_one 379 AUG2919 NO T78099（2026-08-29）

角色: LEARN（decisions 010 触发）。brief: `docs/briefs/2026-08-29-LEARN-379.md`。
立题: −36 挂进、无 $100 成交、400ms 内被 013 `reconcile` 按进场价刮掉。像不像金带子。
事实来源: 本轮 sqlite 抽查（`data/btchour.sqlite` trades#379 raw + journal/scans 22:00–22:23Z）+ 工作区 `btchour/engine.py` 与 git 历史（只读）。与 CMD brief 数字核对一致。

---

## 时间线（journal + scans + trades#379 raw + git，UTC）

| 时刻 | 事件 | 动量 | spot |
|---|---|---|---|
| 22:00:19.60 | 挂 NO T78099 rest 0.25，ask 0.33，p(NO)=45.3%，live_one，reason dump_gap（raw ts_ms 1788040819602；journal 22:00:19.35 tick −35.75） | **−35.75** | 78155.2 |
| 22:00–22:02 | 挂着不动。ask 0.33→0.35→0.37→0.36→0.32；动量 −36 一路淡化到 −1.4，**全程从未到 −$100**，也未反手 ≥+$100（无撤单触发） | | |
| 22:02:35.95 | 成交前最后一 tick：ask **0.32**（不是 rest 0.25），p(NO)=45.5%；此后 journal 断档至 22:06:02 | **−1.40** | 78152.1 |
| 22:02:55.020 | **进场单 executed** rest 0.25（`01a04f89`；时刻来自 CMD 核交易所 fills，sqlite 只能从 flatten 反证仓位存在）。scans 显示 spot 正在上抬：22:02:51 78155.3 → 22:02:55 78157.1 | | 78157 |
| 22:02:55.425 | **flatten_reconcile 成交**（距 fill **405ms**）：IOC 买 YES 0.75（`01a04f8c`，fill_count=1，fee 0.0132）→ NO 侧出场 **0.25 = 进场价**，pnl **−0.0132** = 纯 taker 费 | | |
| 22:03–22:08 | spot 继续拉：22:03:53 78171.6 → 22:08:40 78206.9，动量 +40.6——成交又是上冲点火 tick，与 378 同型 | +40.6 | 78207 |
| 22:19:57 起 | spot 又跌破行权 78099.99（78099.0 → 22:20:04 78096.5）；scans 22:23:25 终止于 78101.0，结算方向 sqlite 判不了 | | |

sqlite 关键状态：`live_status` 停在 **resting**、`live_fill=0`——成交从未走 promote，没有 clip 窗口，`exits.py` 一个分支都没碰到。`status=closed / result=flatten_reconcile` 是 reconcile 直接写的。380 未挂已核：trades 表 379 之后无新行（同小时亏损后 `allow_session` 挡，006）。

## 这笔出场是哪来的

**不是任何策略分支，是 013 实现的误伤。** 当时运行的是 commit `22e6172`（21:44Z，git log 已核）：`reconcile_live_one` 只要交易所持有我们 live_one ticker 的仓、sqlite 行还在，就 flatten——**不看 `status`**。013 决策文字写的是「sqlite **已平** 但交易所还持仓 → 再 flatten」（378 那种残留，378 已于 21:36:36Z 手动补平 YES 0.50 / pnl 重述 +0.2325，与 379 无残留关系）。379 的时序是：进场 22:02:55.020 成交 → 下一轮 `run_cycle` 的 reconcile 排在 `refresh_working`（promote 成交）**之前** → 看见交易所持仓 1 张、sqlite `live_fill=0` `status=working` → 当成残留 IOC 平掉。持仓 405ms，出场 0.25 恰好等于进场价（NO bid 刚被同一笔砸穿压到 0.25 附近），损失只有费。**工作区已有未提交补丁**（`M btchour/engine.py` + `M tests/test_live_one.py`）：`status != "closed" → continue`，注释直书「379: working just filled」。补丁方向对，但未提交、未挂 decisions 附注——CMD 处置，LEARN 不碰。

## 成交门为什么又没挡住（第 4 次实证）

挂时 −35.75 满足 005 字面（NO 只需 impulse<0 且 32–42¢ 看见），比 378 的 −14 大但离 −$100 仍差 2/3。成交时动量 −1.4 且正在转正、ask 0.32 ≠ rest 0.25、全程无 −$100——005「成交仍要同向 |impulse|≥$100 且 ask 还在 rest」在 live_one 路径**结构性缺位第 4 次实证**（374/377/378/379；377 笔记已核 engine.py，live 只读 fill_count，$100 成交门只接 paper 的 `wait_book_crossed`）。噪声成交家族至此 **5 笔**：374 +0.0353 / 375 +0.0645 / 377 −0.0132 / 378 +0.2325（重述后）/ 379 −0.0132，净 ≈ +0.31——仍全部是掷硬币出场和微结构馈赠，不是策略收益。

## 金带子对照

四条规矩（manual.md）：**「先有方向」不成立**——挂时 −36 只是噪声级回落，成交时 −1.4 且 20 秒后动量 +40.6，spot 6 分钟 +55，又是「上冲点火时 NO 被砸穿薄簿」，与 AUG2520/AUG2608「真砸盘里 NO 升值」完全相反。「进得便宜」形式成立（0.25 挂单费 0）。「10%–50% 落袋」**无从谈起**——没有兑现带，−5.3%（纯费）。「做完就停」**无意义**——不是做完，是被自己的对账单打断。378 好歹是「形似神反」；379 连形都没有：**这不是一笔交易，是一次工程事故**（进场无门 + 出场误伤，两个独立接线洞叠在 405ms 里）。反事实（如果没被刮掉）不可判定：spot 先 +107 冲过行权又在 22:19 跌回行权下方，scans 22:23 断。

---

## 总判决

**判决词: 不是达成**（像不像金带子：**否**——不是交易，是工程事故；−0.0132 是费，不是策略出场）

噪声级动量字面过门挂进 → live 路径无成交门、同向淡化不撤 → 上冲点火瞬间被砸到 0.25 → 405ms 后被 013 reconcile（运行版无 status 守卫）当 378 式残留按进场价刮掉。回放 `flex_skip` 16h/24h 仍 0/0/0（sweep 22:00:41Z），同样不是达成；`flex_nowait` 的 1 笔绿是对照路径，不是现行路径。**−0.0132 不影响达成判定，但暴露了两个真实接线洞。**

### 关键数字（≤3）

1. **进场 fill 22:02:55.020Z → flatten_reconcile fill 22:02:55.425Z，持仓 405ms；出场 0.25 = 进场价，pnl −0.0132 = 纯费** —— trades#379 raw（ts_ms 1788040975425）；sqlite `live_fill=0`/`live_status=resting` 未 promote，运行版 commit `22e6172` 的 reconcile 不看 status，013 文字（「sqlite 已平」）与实现落差
2. **挂时动量 −35.75（22:00:19Z），成交前最后 tick −1.40 / ask 0.32（22:02:35.95Z），全程未到 −$100** —— journal；live 成交门结构性缺位第 4 次实证（374/377/378/379），噪声成交家族第 5 笔
3. **成交又在上冲点火：spot 22:02:35 78152 → 22:08:40 78207（动量 +40.6），22:19:57 又跌破行权 78099.99** —— scans/journal；方向噪声级、反事实不可判定

### 是否建议进 ADV

**本笔判决不进 ADV**（无改门候选；「不是达成」不需要对开）。两项转 CMD 处置（都不动 005/007 门）：

1. **013 reconcile status 守卫补丁走正式流程**：工作区已有未提交修复（`engine.py` + `test_live_one.py`），与 013 决策文字对齐（只平 sqlite 已 closed 的残留）。这是实现 bug 修正、不是改门，但须 ENG/CMD 提交并在 013 下追附注，避免下次 fill-promote 竞态复发。LEARN 不 commit。
2. **live 成交门缺位维持 ENG 升级并再加权**：4 次实证 / 家族 5 笔，377 笔记设的 ≥4 阈值已过两笔。379 额外说明：这个洞和 reconcile 洞会叠加——无门成交的仓恰好是最可能处于「fill 未 promote」竞态窗口的仓。

### 禁区自查

未改门、未改代码（engine.py 补丁是别人工作区已有的，本轮只读）、未 git commit/push、未对用户播报；未建议 0.45 / taker / p=0.30 / 看见就挂、未建议改 007；未把回放绿或这笔写成达成；未把 flex_nowait 当现行路径。

---

## LEARN 抽查底稿

| 数字 | brief/CMD | sqlite/git 本轮 | 判 |
|---|---|---|---|
| 挂 | 18:00:19 EDT，rest 0.25，ask 0.33，−36，p=45.3% | raw ts_ms 1788040819602=22:00:19.602Z；journal 22:00:19.35Z −35.75 / ask 0.33 / p 0.45313 | 过 |
| 挂着 | −36→−1.4，ask 0.33→0.32，无 −$100 | journal 22:00–22:02:35 逐 tick 核对，极值 −37.95（22:00:11，挂前）挂后未过 −36 | 过 |
| 成交前最后 tick | −1.4 / ask 0.32 | 22:02:35.953Z impulse −1.4017 / ask 0.32 | 过 |
| 进场 executed | 18:02:55.020 EDT | sqlite 无法直核 .020（fill 未 promote）；由 flatten 成功 + leftover_size −1.0 反证仓位存在，时刻采 CMD 核交易所 fills | 过（注记） |
| 刮 | ~400ms 后 reconcile IOC YES 0.75，−0.0132 | flatten ts_ms 1788040975425=22:02:55.425Z，avg_fill 0.75 / fee 0.0132 / exit 0.25 / pnl −0.0132；间隔 405ms | 过 |
| 未 promote | live_status resting / live_fill 0 | raw 同 | 过 |
| 013 本意 vs 实现 | working 不该刮 | git：运行版 22e6172 无 status 守卫；工作区未提交补丁已加 `!= "closed" → continue` | 过 |
| 380 未挂 | allow_session 挡 | trades 表 379 后无新行；22:06 journal 仅 wait 诊断无下单 | 过 |

新增事实（CMD 未列）：① 378 出场账已重述——首次 IOC 未成交后 21:36:36Z 手动补平 YES 0.50，pnl +0.2325（非笔记里的 +0.0938），噪声家族净额相应更新 ≈ +0.31；② 22:19:57 起 spot 跌破行权 78099.99，scans 22:23:25 终止于 78101.0，本小时结算方向 sqlite 判不了。
