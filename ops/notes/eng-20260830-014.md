# ENG — 014 收口

日期: 2026-08-30  
角色: ENG  
对应: 014（停 live_one 默认 + 跌侧挂门 $100 + live wait_fade）

## CMD 已起（本回合未改逻辑，仅测 + 补测试）

| 文件 | 014 行为 |
|---|---|
| `btchour/config.py` | `live_one=False`；`impulse_wait_rest_min=100` |
| `btchour/strategy.py` | `coupon_rest_ready`：同向 \|impulse\|≥rest_min 才挂；−20/−12 不挂，−160 仍挂 |
| `btchour/engine.py` | `_wait_cancel_reason`：live_one 真单淡化 → `wait_fade`；纸盘淡化仍坐 |

## ENG 改动

| 文件 | 内容 |
|---|---|
| `tests/test_impulse_wait.py` | −12/−20 弱跌不再期望挂；−160 仍挂；纸盘 `test_keeps_the_rest_when_the_dump_impulse_fades` 未动 |
| `tests/test_live_one.py` | `test_load_settings_defaults_live_one_off_and_rest_min_100`；`test_cancels_a_live_no_when_the_dump_fades`（NO 淡到 −20 → wait_fade） |
| `.env.example` | 注释 + `BTCHOUR_IMPULSE_WAIT_REST_MIN=100`、`BTCHOUR_LIVE_ONE=0` |

## 测试

```bash
python3 -m unittest tests.test_impulse_wait tests.test_live_one tests.test_strategy tests.test_engine
```

结果: **102 passed, 0 failed**（2026-08-30）

## 禁区遵守

- 未 git push
- 未重启 loop / 未切 live
- 未提交 `.env` / `data/` / `latest.json` / catalog snapshot
