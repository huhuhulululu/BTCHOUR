# RISK 姿态报告 — 382 AUG3001 NO T78099.99（2026-08-30，弱跌 −15 挂进，成交 −5/ask0.32 无 $100，t_trail 止损，pnl −0.0035）

角色: RISK（decisions 010 触发；brief `docs/briefs/2026-08-30-RISK-382.md`）。只判「382 该不该按现行门（005）挂 / 是否还该继续挂」，不下单、不改代码、不改门、不切 live、不对用户播报。
事实来源: `/workspace/data/btchour.sqlite`（`trades#382` raw + `journal#63891–64019`，2026-08-30T04:00:04–04:09:57Z / 00:00:04–00:09:57 EDT）独立抽查；`python3 -m btchour board`（只读，009 允许，现场核对）；代码路径核对 `btchour/strategy.py`（`allow_swing` 884–899 行、`SessionMemory`/`_skip_hour` 780–882 行）。对照 `ops/notes/risk-20260829-381.md`（380/381 同型个案）。

---

## 该不该按现行门挂：字面合规，且比 380/381 的挂单信号更强

`trades#382` 挂单响应 2026-08-30T04:00:04.694371Z（00:00:04 EDT）。`journal#63891`（04:00:04.436633Z）独立核实 `impulse=-15.1545`，与 `raw.reason`「`dump_gap NO 看见 0.33 rest 0.25 动量 -15 p=46.6% clip>=0.29`」一致。005 对 NO 的字面要求是 `impulse<0` 且 32–42¢ 看见——`ask=0.33` 落在带内，`rest=0.25`，挂单不违规。

**与 380/381（挂单动量均为 `-4`）相比，382 挂单动量 `-15` 是本轮三笔 `t_trail` 样本里信号最强的一次**，不落在 RISK.md 黄档定义「挂在噪声负打印（如 −0）」的模糊边界——挂单本身没有 380/381 那种擦边问题。

## 该不该成交：无 $100 确认，方向反转——同一已知结构性缺口，本次第 9 次坐实

`journal` 逐笔核实：挂单后动量持续走弱至 04:02:35.619（`impulse=-5.0855`，与 brief「成交时 −5.1」一致），`filled_at`（2026-08-30T04:02:35.622052Z）与该采样点几乎同一秒，**这次没有 381 那种 4 分钟断档**——journal 采样连续覆盖了挂单到成交全程，说明成交发生在内部 impulse 已经从 `-15` 收窄到 `-5` 之后，未回到同向 `|impulse|≥$100` 门槛，`ask`（brief 报 0.32，sqlite 无独立 quote 表可核对，仅可信 CMD 现场读数）也已偏离挂单时的 `rest=0.25`。

成交后行情**剧烈反转**：`journal#63927`（04:02:39.597，`impulse=+0.32`）→ `#63928`（04:03:37.664，`impulse=+43.84`，spot 78140→78180）→ 峰值 `#63941`（04:04:26.935，`impulse=+77.04`，spot 78213）。这与 373/374/375/377/378/379/380/381 同属一个已知结构性缺口（post-only rest 单被交易所真实撮合，内部 impulse/ask 门不对交易所生效），**382 是第 9 次坐实**。不同于 381 的断档模式，本次是「连续采样下的正常收窄+成交+反转」，说明缺口不依赖采样断档也会发生，进一步确认这是结构性而非监控盲区导致，仍不构成改门理由（008 禁区）。

## 出场核实：t_trail 算术无误，本次是三笔 `t_trail` 里损失最小的一次

`exit_reason=t_trail`，`peak_bid=0.31`，触发 `swing_trail=0.04`（`exit_note`「trail 0.04 from peak 0.31 → 0.27」，0.31−0.27=0.04≥0.04）。实际 IOC 平仓 `flatten.response.average_fill_price=0.7400`（`slip_ticks=2`，013 批准的交叉例外），换算 NO 侧 `exit_price=1−0.74=0.26`，`pnl=0.26−0.0135(fee)−0.25=−0.0035`，与 `trades#382.pnl` 核对一致。`fill_count=1.00`（非 0），未触发 013 禁止的 `fill_count=0` 记账问题。全程只 1 张 live，未吃 taker，未切 live。

**三笔 `t_trail`（380 −0.0804、381 −0.0612、382 −0.0035）损失依次收窄**，仅作观察记录，不构成任何门槛判断依据（样本太小，不建议 LEARN 据此下结论）。

## 006 / 007 现场核对：均已按门自动生效，「不该继续挂」已由代码执行

`board` 现场快照：`AUG3001 | 1 | trail | −0.0035 | skip下一小时`——`pnl<0` 命中 006，下一小时 `AUG3002` 已被自动判 skip，`活档/可挂` 当前 `本小时挂单（最多3）` 显示「空仓」，`trades` 无 383。代码核对 `btchour/strategy.py:allow_swing`（892–899 行）：`memory.dead` 且 `memory.play=="impulse_wait"`（非 `impulse_t`/`swing_t`）→ 返回 `False`，即本轮 `impulse_wait` 平仓后本小时（AUG3001）**不允许再开新档、不翻面**——这是 007「clip 后不 hop」在代码里的更广覆盖（对 `t_trail`/`t_stop` 等所有 impulse_wait 平仓同样生效），与「382 之后本小时还该不该继续挂」的答案（不该）自动一致，无需人工干预。

## 姿态：**黄**，理由与 380/381 同类但挂单信号本身更干净

红档定义（RISK.md）是「静/弱阳真单、第二张 live、吃 taker、整条切 live」——382 都不占：挂单动量 `-15` 是本轮最强的 NO 挂单信号，非静盘/弱阳挂 YES；全程只 1 张 live；未吃 taker；未切 live。判黄的理由：（a）成交确认无 $100 且方向剧烈反转，是同一已知缺口第 9 次坐实，且本次证明缺口不依赖断档也会发生；（b）出场 `t_trail` 算术核对无误，损失是三笔样本里最小的一次；（c）006/007 类逻辑已自动挡下本小时/下一小时的继续挂单，无需人工建议。三者叠加不到红档门槛（无第二张 live、无真吃 taker、无整条切 live），且挂单本身比 380/381 更不落在模糊边界，比 381 判黄的严重度略低。

---

## 调整菜单

1. **维持现状（`952cb97`）**：挂单/成交/出场三段均在既有门与代码路径内正确执行（挂单合规且信号强于近两例，成交是已知缺口第 9 次坐实，出场算术无误），006/007 类逻辑已自动生效，无需改动。
2. **本小时（AUG3001）空仓观察，不改门**：board 现场核实活档 0、可挂 0，`allow_swing` 已因 `memory.dead` 阻断本小时新档，双闸已挡，无需额外动作。
3. 未选（无新增「停挂等下一小时」建议需求；006 已自动判 AUG3002 skip，无需豁免）。
4. 未选（无改门/切 live/吃 taker/放宽成交门至 0.45/放宽 `p=0.30` 的理由；成交确认缺口第 9 次坐实是已知模式延续，不构成升级充分条件）。
5. 未选（010 已并行派 LEARN；建议 CMD 关注 LEARN-382 是否也注意到「挂单信号变强（−15 vs −4）但成交确认仍缺 $100」与「三笔 t_trail 损失依次收窄」这两个观察点，供后续是否值得转 ARCH 评估门槛下限做参考，本次不建议改门）。

## 是否 ESCALATE：**否**

## 关键数字（≤3）与出处

1. **挂单 `impulse=-15.1545`，`ask=0.33` 在 NO 32–42¢ 带内，`rest=0.25`**（`journal#63891` 04:00:04.436633Z 独立核实，与 `trades#382.raw.reason` 一致）；005 字面合规，且是 380/381（均 `-4`）以来挂单信号最强的一次，不落在「噪声负打印」模糊边界。
2. **成交瞬间 `impulse=-5.0855`**（`journal#63926` 04:02:35.619248Z，与 `filled_at` 04:02:35.622052Z 几乎同一秒），无同向 `|impulse|≥$100` 确认，随后 `impulse` 于 04:03:37 反转至 `+43.84`（`journal#63928`）——373/374/375/377/378/379/380/381/382 同一缺口第 9 次坐实，本次无断档，证明缺口不依赖采样盲区。
3. **`pnl=-0.0035`，`exit_reason=t_trail`（peak 0.31→0.27，`average_fill_price=0.74`→`exit_price=0.26`，算术核对无误）**（`trades#382.raw`）；006 判定本小时（AUG3001）亏损 → `AUG3002` 已自动 skip（`board` 现场核对 `AUG3001 | trail | −0.0035 | skip下一小时`），且 `allow_swing`（`btchour/strategy.py:892-899`）已阻断本小时二次挂单，无需人工建议豁免或停挂。

（未建议改成交门到 0.45；未建议吃 taker；未建议放宽 `p=0.30`；未建议改 006/007；未建议豁免 `AUG3002` skip；未建议切 live。）
