# RISK 姿态报告 — 377 AUG2915 NO T77999.99（2026-08-29，已成交、trail 小亏）

角色: RISK（CMD 派发，`docs/briefs/2026-08-29-RISK-377.md`）。只判「本小时还挂不挂」「AUG2916 是否 skip」，不下单、不改代码、不改门、不切 live、不对用户播报。
事实来源: `/workspace/data/btchour.sqlite`（`trades#377` + `journal` 18:16:01–18:21:08Z）独立核实；`python3 -m btchour board`（只读、009 允许）现场核对当前 `AUG2915` 剩余时间的敞口/skip 状态；代码路径核对 `btchour/strategy.py`（`remember_session_exit` 807–842 行 / `_skip_hour` 845–853 行 / `allow_swing` 884–899 行）。判据: `docs/decisions.md` 005（NO 挂单只需 `impulse<0` 且 32–42¢；成交/淡化/反手撤仍要同向 `|impulse|≥$100`）、006（亏了下一小时整小时不做 T）、007（clip 后不 hop）、011（近 ATM 阶梯是工作盘）、012（不因噪声单刷量）。

---

## 该不该挂（已发生，核实结果：挂单本身合规，但是噪声级负打印）

`trades#377` 挂单时刻（2026-08-29T18:16:45.525160Z / 14:16:45 EDT）核实：`journal.impulse = -24.74`（brief 记 −25，一致），`raw.ask = 0.38`，落在 NO 32–42¢ 带内，`rest = 0.25`。005 对 NO 的字面要求只是 `impulse<0` 且 32–42¢ 看见——两条都满足，挂单不违规。但 −25 远低于本策略用来判断「真动量」的 $100 门槛，是 RISK.md 黄档定义里「挂在噪声负打印但仍在 005 字面内」的典型样本，不是 011 说的「真动量小时打出人手那种 clip」。

## 该不该成交（已发生，核实结果：成交发生在交易所侧，同一个已知缺口第 4 次坐实）

独立核实成交瞬间：`trades#377.raw.filled_at = 2026-08-29T18:18:51.898084Z`，最近一条 journal（18:18:51.895844Z，与成交时刻相差 <3ms）显示 `impulse = -13.23`。005 的成交确认条款要求「仍要同向 `|impulse|≥$100`」，−13.23 远未达标——与 373（反向 +31.7）、374（全程未到 −$100）、375（反向 +16 且全程未到 −$100）同属一个已知结构性缺口：post-only rest 单被交易所真实撮合时，我们的内部 impulse 门不对交易所生效。这是**第 4 次**独立坐实，不是新 bug（`btchour/engine.py` 的 `refresh_working` 本身不该也没有「按内部动量否决交易所真实成交」的机制），不构成改门理由（008 禁区）。

## 出场核实

`exit_reason = t_trail`，`peak_bid = 0.30 → exit_price = 0.25`，`pnl = -0.0132`（roi −5.3%）。挂单→成交→出场全程仅约 2 分 11 秒（18:16:45→18:18:56 flatten 回执），trail 止损按设计生效，无第二张 live、无吃 taker、无切 live 迹象。当前 `AUG2915` 已无 resting 敞口（`python3 -m btchour board` 现场核对：本小时挂单栏「空仓」）。

## 本小时（AUG2915）还挂不挂：不挂——门本身已经挡住，市场当下也没有点

两层原因都指向不挂，不是 RISK 主观加码：
1. **代码层已挡**：377 的 `play="impulse_wait"` 已成交后出场，`remember_swing_exit` 置 `dead=True`；`allow_swing` 对 `memory.dead` 且 `memory.play="impulse_wait"`（不在 `{impulse_t, swing_t}` 白名单内）直接 `return False`——本小时不再允许新的 coupon wait 开新行权价，等同 007「clip 后不 hop」的代码实现（007 字面写「clip」，但代码对任何 impulse_wait 完成态一视同仁，不分输赢，374/375/377 这类 NO 小亏同样触发）。
2. **市场层也没有点**：`board` 现场快照（14:31 EDT，即 377 出场后 ~10 分钟）显示当前动量 −53（继续走弱但仍未到 −$100），188 档中 `$600` 内 12 档、32–42¢ 活档 **0**，唯一候选 `T78099 YES dist 71 ask 0.29` 标注「可挂：否」——阶梯本身当下没有满足 005 的点，不是空仓=没机会（011），是这一刻真没有。

## AUG2916 是否 skip：是——006 自动触发，已在 board 上确认

`remember_session_exit` 对 `pnl=-0.0132<0` 判定为 `lost`，写入 `last_loss_event="KXBTCD-26AUG2915"`，`skip_next=True`；`_skip_hour` 计算下一个 ticker 即 `AUG2916`。`python3 -m btchour board` 「近几小时」表当场核对：`AUG2915 | 1 | trail | −0.0132 | skip下一小时`——系统已自动标记，RISK 确认无需人工干预，也没有理由用 011/012 去豁免这次 skip（011/012 讲的是「不要坐等」，不是「取消亏损后的整小时 skip 门」）。

---

## 总姿态：**黄**

理由：不触发红（无第二张 live、无吃 taker、无切 live、无静盘/弱阳挂 YES）；挂单和出场全程在 005/006/007 字面内正确执行，且已经平仓、无持续敞口——比 375（仍暴露）更干净。判黄不是因为这笔本身出了问题，是因为挂单入场信号（−25）是噪声级，命中 RISK.md 黄档的字面定义（「挂在噪声负打印但仍在 005 字面内」），且成交确认门对交易所真实撮合不生效的缺口第 4 次坐实——两条都是**观察项**，不是违规，不需要改门。

## 调整菜单

- **1 维持现状（`952cb97`）**：挂单/成交/出场三步都在门内正确执行，`AUG2916` 的 skip 已由 006 自动触发，无需改动。
- **2 本小时（`AUG2915`）空仓观察，不改门**：hop 已被代码层挡住（dead memory），且当前 32–42¢ 活档为 0，剩余 ~28 分钟按现状空仓即可，不建议、也不需要人工干预去催单。
- **3 `AUG2916` 停挂/等下一小时（skip 规则内）**：006 已自动标记 skip，RISK 确认执行，不建议豁免。
- 未选 4（无改门/切 live/吃 taker/放宽成交门至 0.45 的理由——缺口是已知结构性问题，第 4 次坐实只是频率证据，不构成升级充分条件）。
- 未选 5（本笔尚未见 `ops/notes/learn-20260829-377.md`；是否派 LEARN 走 010 复盘由 CMD 决定，不是本报告的判断范围）。

## 是否 ESCALATE：**否**

## 关键数字（≤3）与出处

1. **挂单 impulse = −24.74（噪声级，未到 $100 门槛）**，2026-08-29T18:16:45.525160Z / 14:16:45 EDT，`journal`；ask 0.38 在 NO 32–42¢ 带内，005 字面合规但属黄档典型样本。
2. **成交瞬间 impulse = −13.23（与要求的 `|impulse|≥$100` 差距悬殊）**，2026-08-29T18:18:51.895844Z（距 `trades#377.raw.filled_at` <3ms）——373/374/375/377 同一缺口第 4 次坐实，仅作观察记录，不建议改门。
3. **pnl = −0.0132（roi −5.3%，t_trail）**，`trades#377.raw.exit_reason/exit_price`；006 已自动判定 `AUG2916` skip（`python3 -m btchour board` 「近几小时」表现场确认「skip下一小时」）。

（未建议改门；未建议切 live；未建议成交门放到 0.45；未建议吃 taker；未建议豁免 006 的 skip。）
