# ADV — 014 对抗复核

**攻得倒。**

1. **对照路径攻击未命中，但也不给加分。** `flex_nowait` 24h 的 1 笔绿单和 cheap 的亏单都不是现行路径；本候选主要依据 373–388 真挂，不能借这些对照证明改善。
2. **回放攻击命中。** `sweep.json` 现行 16h/24h 都是 0/0/0；定点重放虽能让 AUG2520 的 T78699 在 −119 时进场并 clip，但 `replay.py` 根本不执行新增的 live `wait_fade`，所以任何回放绿/空白都不能验证这条 live 规则。
3. **旧门回退攻击命中。** `impulse_wait_rest_min=100` 正是 005 已拒的「3 分钟 $100 才挂」；它能挡 382–388 的弱跌，却也会挡 384 这种实盘已见的真砸盘小时（最深仅 −41、NO 0.33→0.94）。而且「373–388 全挡」不实：376 挂时 +104，YES 初始门仍会放行。
4. **放宽成交攻击未命中。** 候选没有把门放到 0.45、没有吃 taker、没有放宽 `p=0.30`，008 可保持不动。
5. **金带子只在信号层部分保住，执行层会拒。** AUG2608 实录挂 −225.71、成交 −191.38，期间未淡到 $100 内，故 $100 门和 `wait_fade` 都不杀；AUG2520 分钟带也只证明一档 −119 可过，未证明人手多档。更关键的是默认 `live_one=False` 无条件不发交易所，代码没有「遇到金带子自动恢复」分支，因此 AUG2520/AUG2608 真单都会被拒；新增 `wait_fade` 在默认配置下也不可达。

**建议降级：是。降级为“安全停机补丁”，不得称为金带改进或 005 可采纳修订；006/007/008 不动。**
