# RISK 姿态报告 — 378 AUG2918 NO T78099.99（2026-08-29，已成交并 clip 赢 +0.0938）

角色: RISK（decisions 010 触发，与 LEARN 并行；brief `docs/briefs/2026-08-29-RISK-378.md`）。只判「本小时还挂不挂」「AUG2919 是否 skip」「无 $100 成交/ask≠rest 是否黄或红」，不下单、不改代码、不改门、不切 live、不对用户播报。
事实来源: `/workspace/data/btchour.sqlite`（`trades#378` raw + `journal` 21:00:01–21:18:17Z）独立抽查；`python3 -m btchour board`（只读，009 允许）现场核对 `AUG2918` 当前敞口/skip 状态；代码路径核对 `btchour/strategy.py`（`allow_swing` 885–899 行 `impulse_wait` dead 分支、`remember_session_exit` 809–842 行 `lost` 判定）。判据: `docs/decisions.md` 005（NO 挂单只需 `impulse<0` 且 32–42¢；成交仍要同向 `|impulse|≥$100` 且 ask 还在 rest）、006（0成交不叠skip；亏才skip下一小时）、007（clip 后不 hop）、011（近ATM阶梯是工作盘）、012（不因噪声单刷量当产出）。CMD 已核数字与本轮 sqlite 抽查一致，另有 `ops/notes/learn-20260829-378.md`（010 并行）独立核实同一时间线，数字互证。

---

## 该不该挂（已发生，核实结果：挂单本身合规）

`trades#378` 挂单时刻 2026-08-29T21:00:08.617587Z（17:00:08 EDT）核实：`journal.impulse = -13.98`（brief 记 −14，一致），`raw.ask = 0.41`，落在 NO 32–42¢ 带内，`rest = 0.25`。005 对 NO 的字面要求只是 `impulse<0` 且 32–42¢ 看见——两条都满足，挂单不违规，但 −14 远低于 $100 门槛，是真动量与噪声之间的边界样本，不是 011 说的「真动量小时打出人手那种 clip」。

## 该不该成交（已发生，核实结果：成交发生在交易所侧，同一已知缺口第 5 次坐实）

成交前最后一 tick（2026-08-29T21:12:20.362422Z）：`impulse = -3.49`（brief 记 −3.5，一致），`ask = 0.37`（≠ `rest = 0.25`）；`filled_at = 2026-08-29T21:12:24.134681Z`，与该 tick 相差 <4 秒，无更近采样。005 的成交确认条款要求「仍要同向 `|impulse|≥$100` 且 ask 还在 rest」——两条都未满足（−3.49 远未到 −$100；ask 0.37 已脱离 rest 0.25）。这与 373/374/375/377 同属一个已知结构性缺口：post-only rest 单被交易所真实撮合时，内部 impulse/ask 门不对交易所生效（`btchour/engine.py` `refresh_working` 只登记交易所侧 fill，不该也没有「按内部动量否决真实成交」的机制）。这是**第 5 次**独立坐实，不是新 bug，不构成改门理由（008 禁区）。

## 出场核实

`exit_reason = t_clip`，`exit_price = 0.36`，`pnl = +0.0938`（roi +37.5%），`flatten` 与 `filled_at` 相差 <1 秒（同秒抢跑落袋）。挂单→成交→出场全程约 12 分 16 秒，无第二张 live、无吃 taker、无切 live 迹象。当前 `AUG2918` 已无 resting 敞口（`python3 -m btchour board` 现场核对：17:21 EDT 本小时挂单栏「空仓」）。

## 本小时（AUG2918）还挂不挂：不挂——代码层已挡，市场当下也没有点

1. **代码层已挡**：378 的 `play="impulse_wait"` 成交出场后 `remember_swing_exit` 置 `dead=True`；`allow_swing` 对 `memory.dead` 且 `memory.play="impulse_wait"`（不在 `{impulse_t, swing_t}` 白名单）直接 `return False`——本小时不再允许新的 coupon wait 开新行权价，即 007「clip 后不 hop」的代码实现，赢/亏一视同仁。
2. **市场层也没有点**：`board` 现场快照（17:21:55 EDT）显示动量 −2（近乎静盘），188 档中 `$600` 内 12 档、YES 28–42¢ 活档 1（`T78199 dist 40 ask 0.38`）、NO 32–42¢ 活档 0；`T78199` 标注「可挂：否」——动量 −2 远不到 YES 所需 `|impulse|≥$100`，是静/弱阳，005/RISK.md 都不许挂。姿态列已显示「已clip不hop」。

## AUG2919 是否 skip：否——006 未触发，赢不叠 skip

`remember_session_exit` 对 `pnl=+0.0938>0` 且 `reason="t_clip"`（不在 `t_stop/t_wait_stop/t_scratch/t_fade/invalidate/flatten_time` 亏损集合内）判定 `lost=False`，返回空 `SessionMemory()`（`skip_next=False`）——006「亏了空一小时」不适用于赢单，AUG2919 不需要 skip。`board`「近几小时」表现场核对：`AUG2918 | 1 | clip | +0.0938 | 可做`，与代码判定一致。

## 无 $100 成交 / ask≠rest：黄，不是红

红档定义（RISK.md）是「静/弱阳真单、第二张 live、吃 taker、整条切 live」——本笔都不占：挂单是真跌趋势内的合规 NO 挂单（不是静盘/弱阳挂 YES），全程只 1 张 live，未吃 taker，未切 live。但成交确认门缺口第 5 次坐实（373/374/375/377/378），属 RISK.md 黄档定义「挂在噪声负打印但仍在 005 字面内 → 建议空仓观察，不改门」的延伸样本（本笔更极端：确认时点 −3.49 几乎贴零）。赢钱不改变这个判断——RISK 判的是门执行是否有漏洞，不是盈亏结果；377（亏）与 378（赢）用同一条判据都落黄档。

---

## 总姿态：**黄**

理由：不触发红（无第二张 live、无吃 taker、无切 live、无静/弱阳真单挂 YES）；挂单、出场两端都在 005/006/007 字面内正确执行，已经平仓、无持续敞口。判黄是因为成交确认时点（−3.49，ask≠rest）第 5 次坐实同一个「交易所真实撮合不受内部 impulse/ask 门约束」的结构性缺口——这是观察项，不是违规，不需要也不建议改门（008 禁区）。

## 调整菜单

- **1 维持现状（`952cb97`）**：挂单/成交/出场三步都在门内正确执行，AUG2919 不 skip 已由 006 自动判定，无需改动。
- **2 本小时（AUG2918）空仓观察，不改门**：hop 已被代码层 `dead` 标记挡住，且当前 YES/NO 活档均不满足门槛（动量 −2），剩余 ~38 分钟按现状空仓即可。
- 未选 3（无新 skip 触发；006 判定赢单不 skip AUG2919，不需要「停挂等下一小时」的额外建议）。
- 未选 4（无改门/切 live/吃 taker/放宽成交门至 0.45 的理由——缺口是已知结构性问题，第 5 次坐实只是频率证据，不构成升级充分条件；是否值得转 ENG 评估接线加事后标记，交由 CMD 判断，不是 RISK 的改门建议）。
- 未选 5（010 已并行派发 LEARN，`ops/notes/learn-20260829-378.md` 已完成复盘，判「不是达成/形似神反」；无需 RISK 重复指派）。

## 是否 ESCALATE：**否**

## 关键数字（≤3）与出处

1. **挂单 impulse = −13.98（≈brief −14，未到 $100 门槛）**，2026-08-29T21:00:08.617587Z / 17:00:08 EDT，`journal`；ask 0.41 在 NO 32–42¢ 带内，005 字面合规。
2. **成交前一 tick impulse = −3.49，ask 0.37 ≠ rest 0.25，全程未到 −$100**，2026-08-29T21:12:20.362422Z（距 `filled_at` 21:12:24.134681Z <4 秒），`journal` + `trades#378.raw`——373/374/375/377/378 同一缺口第 5 次坐实，仅作观察记录，不建议改门。
3. **pnl = +0.0938（t_clip，roi +37.5%）**，`trades#378.raw.exit_reason/pnl`；006 判定 `lost=False` → `AUG2919` 不 skip（`python3 -m btchour board` 现场核对 `AUG2918 | clip | +0.0938 | 可做`）。

（未建议改成交门到 0.45；未建议吃 taker；未建议放宽 `p=0.30`；未建议切 live；未建议豁免或加码 006/007。）
