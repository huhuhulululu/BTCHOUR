# RISK 姿态报告 — 386 AUG3006 NO T77999.99（2026-08-30，dump_gap NO 字面合规挂单、成交无 $100 确认（NO 路径本不要求）、成交后动量剧烈反手 +43 但 `t_trail` 已按现行门自动止盈平仓 +0.0062，非「门变好」证据）

角色: RISK（decisions 010 触发；brief `docs/briefs/2026-08-30-RISK-386.md`）。只判「386 该不该按现行门（005）挂 / 成交有没有 $100 / 该不该按现行门继续挂 / 本小时已出是否该 hop」，不下单、不改代码、不改门、不切 live、不对用户播报、不 git push。
事实来源: `/workspace/data/btchour.sqlite`（`trades#386` raw + `journal#66633–66798`，2026-08-30T09:29:00–09:46:00Z / 05:29:00–05:46:00 EDT）独立抽查；`btchour/strategy.py` L20–58（`coupon_sides`/`coupon_rest_ready`）、`btchour/config.py` L74/82（`impulse_min=100.0`、`impulse_wait_rest_min=0.0`）核实 NO 门字面；`btchour/exits.py` L160–166 + `config.py` L163（`swing_trail=0.04`）核实 `t_trail` 退出机制。对照 `ops/notes/risk-20260830-385.md`（383–385 同型 `dump_gap NO` 前例）。

---

## 该不该按现行门挂：独立核实与 CMD 一致，字面合规

`trades#386` 挂单响应 2026-08-30T09:29:29.893Z（05:29:29 EDT），独立核实 `journal#66640`（09:29:29.654215Z）`impulse=-4.234166666530655`（≈−4.2，CMD 报 −4.2 一致）、`spot=78035.81`、`raw.reason`「dump_gap NO 看见 0.34 rest 0.25 动量 -4 p=45.7%」同源。代码级核实 `btchour/strategy.py` `coupon_sides`/`coupon_rest_ready`：NO 侧 `impulse_wait_rest_min=0.0`（`config.py` L82，注释「hang when 32–42¢ is visible; $100 is the fill」），字面只要求 `impulse<0` 且 ask 在 32–42¢ 且未 `impulse_wait_flipped`——−4.2 满足，ask=0.34 在带内。挂单本身合规，与 383/384/385 同一 `dump_gap NO` 子路径，无需 $100（$100 只是涨/coupon YES 的门，005 原文亦如此）。

## 成交有没有 $100：独立核实全程未到 $100，且这次成交**不在**断档里（与 385 不同）

独立核实挂单持有期（09:29:29–09:40:59Z）内 `journal` 逐条扫描，`impulse` 最深见 **−27.807**（`journal#66682`，09:32:21.080622Z），全程未接近 $100 量级——CMD「挂后最深 −27.8，无 $100」独立核实成立。`trades#386.raw.filled_at=2026-08-30T09:40:59.636281Z`，最近 `journal#66766`（09:40:59.633946Z，同一 ticker `no_coupon ask=0.53`）与成交时刻相差仅 **2.3 毫秒**，说明这次成交发生在 journal 正常记录区间内，**不是**像 385 那样落在断档里（385 的断档达 5 分 47 秒）。505 对 NO 从未要求 $100 门槛，故成交无 $100 **不构成入场违规**，是 383/384/385 已确认的同一「NO 挂单被动成交、不需要 $100」模式的第 4 个样本，但此次断档缺失，样本质量比 385 更干净。

## 该不该按现行门继续挂 / 本小时已出是否该 hop：字面无冲突，结构性已锁死

独立核实 `trades` 表 `event_ticker='KXBTCD-26AUG3006'` 本小时仅 **386 一笔**，`status='closed'`。007（clip 后不 hop）与 004（1 张 live 不加第二张）在此已是结构性事实：386 已平仓、本小时无第二笔挂单记录，「不 hop」既是规则要求也是数据现状，无需额外人工动作。继续挂（下一小时/下一次同型信号）字面上仍属 005 允许的 `dump_gap NO`（−4.2 满足 `impulse<0` 且 32–42¢），无需改门即可继续按现行门操作。

## `t_trail` 平仓机制核实：代码级确认按现行门自动止盈，非人工判断，pnl 数字微小不代表门变好

成交（09:40:59Z）后动量剧烈反手，独立核实峰值 `impulse=+43.400`（`journal#66775`，09:42:41.435678Z，与 CMD 报「+40~+43」一致，实际略高至 43.4）。`raw.flatten.response.ts_ms=1788083036217` 对应 **2026-08-30T09:43:56.217Z**（05:43:56 EDT），与 CMD 报的 flatten 时刻一致。独立核实 `btchour/exits.py` L160–166：`t_trail` 触发条件是 `peak - bid + 1e-12 >= settings.swing_trail`（`config.py` L163 `swing_trail=0.04`），`raw.exit_note`「trail 0.04 from peak 0.33 → 0.27」与代码逐字对应——峰值 NO 隐含价 0.33 回撤 0.04 至 0.27 即自动平仓，是现行门写死的机制触发，不是人工临场判断。`raw.flatten.response`：`fill_count=1.00`、`average_fill_price=0.7300`（买 YES 平 NO 仓），符合 013（`fill_count>0` 才能记平仓，非 `paper_close` 空成交）。`pnl=0.006200000000000039`（trades#386.pnl）核算：`(0.27−0.25)−0.0138≈0.0062`，独立算术核实成立。**该数字微小，且用户已在事实清单中提醒「送钱，不要把 +0.0062 写成门变好」——本报告不将此笔小额盈利解读为 005 门运作改善或应放宽的信号，仅确认退出机制按现行参数正确触发、账目在 013 下干净落地。**

## 004 / 006 / 007 / 008 / 013 现场核对：均未破

- **004**：本小时 `trades` 表仅 386 一笔 `live_one`，无第二张同时 live 的记录。
- **006**：386 结果为盈利（`pnl=+0.0062`），非亏损，「亏了空一小时」不适用；即便适用也不影响本判断（无亏损触发条件）。
- **007**：386 是本小时仅一笔且已平仓，无 hop 记录，字面与结构性均满足「不 hop」。
- **008**：`taker=0`，未吃 taker；未见成交门放宽到 0.45（ask=0.34 在 32–42¢ 带内即触发，非放宽）；未放宽 `p=0.30`。
- **013**：flatten `fill_count=1.00`、`average_fill_price=0.7300`，非 `fill_count=0` 空成交误记 `paper_close`；账目出场价来自交易所真实成交，合规。

## 姿态：**黄**

红档定义（RISK.md）是「静/弱阳真单、第二张 live、吃 taker、整条切 live」——386 都不占：挂单动量 −4.2 是真实下跌打印（非静盘/弱阳）、全程只 1 张 live、未吃 taker、未切 live。判黄而非绿的理由：① 挂单动量 −4.2 相对 383–385 样本更弱，属于「刚过 `impulse<0` 门槛」的边缘案例，虽字面合规但离「明确下跌」还有距离；② 成交无 $100 确认是 383/384/385/386 连续第 4 个同型样本，是已知未解子问题（NO 路径不要求 $100 本身不违规，但反复出现值得 LEARN 归纳）；③ 成交后动量反手幅度极大（−27.8 → +43.4，近 71 点摆动），本次靠 `t_trail` 自动止盈干净收尾是运气与机制配合的结果，不能因为这次赢小钱就认为「继续挂」在结构上更安全——用户明确提醒过不要这样解读。

---

## 调整菜单

1. **维持现状（`952cb97`）**：挂单字面合规、成交无 $100 属 NO 路径不要求项、004/006/007/008/013 均未破，`t_trail` 按现行参数正确触发并干净平仓，无需改动。
2. 未选（386 已平仓，本小时无未平敞口，无「空仓观察」的额外动作需要）。
3. 未选（007 已结构性满足不 hop，无需额外「停挂」指令）。
4. 未选（无改成交门至 0.45、吃 taker、放宽 `p=0.30`、整条切 live 的理由；−4.2→+43.4 的剧烈反手恰恰说明**不该**放宽门，风险已经够大）。
5. **建议派 LEARN 做对照**：383/384/385/386 四笔已形成「`dump_gap NO` 挂单被动成交、全程无 $100 确认」的稳定重复模式，且 386 与 385 在成交后走势上高度相似（大幅反手 30–40+ 点）但结局不同（385 仍 open 靠 480 秒 scratch 窗口未触发保护；386 靠 `t_trail` 0.04 自动止盈干净平仓）——建议 LEARN 对比这两条退出路径（`impulse_wait_stop`/`scratch_seconds` vs `swing_trail`）在同型反手行情下的保护差异，供未来判断哪条退出机制更可靠，但不作为现在改门的依据。

## 是否 ESCALATE：**否**

## 关键数字（≤3）与出处

1. **挂单 `impulse=-4.234166666530655`（≈−4.2），`ask=0.34`、`rest=0.25`**（`journal#66640` 2026-08-30T09:29:29.654215Z 独立核实，与 `trades#386.raw` 及 CMD 报的 −4.2 一致）；`btchour/strategy.py`（`coupon_rest_ready`）+ `config.py` L82（`impulse_wait_rest_min=0.0`）核实 005 对 NO 字面只要求 `impulse<0`，无需 $100，挂单合规。
2. **持有期 `impulse` 最深 −27.807**（`journal#66682` 09:32:21.080622Z），成交 `filled_at=09:40:59.636281Z`（`trades#386.raw`）与最近 `journal#66766`（09:40:59.633946Z）相差仅 2.3 毫秒——全程无 $100 量级，且**本次成交不在断档里**（对比 385 的 5 分 47 秒断档），独立核实成立。
3. **成交后反手峰值 `impulse=+43.400`**（`journal#66775` 09:42:41.435678Z）；`t_trail` 于 09:43:56.217Z（`raw.flatten.response.ts_ms` 独立换算）以「peak 0.33 → 0.27」自动平仓（`exits.py` L160–166 + `config.py` L163 `swing_trail=0.04` 代码核实），`fill_count=1.00`、`average_fill_price=0.7300` 符合 013，`pnl=+0.0062`（`trades#386.pnl`）算术核实成立——**微小盈利，不解读为 005 门变好，仅确认退出机制按现行参数正确触发**。

（未建议改成交门到 0.45；未建议吃 taker；未建议放宽 `p=0.30`；未建议改 005/006/007/008/013；未建议切 live；未把 +0.0062 或本次干净平仓写成「门在改善」；本小时已出不 hop 独立核实成立，不建议 hop。）
