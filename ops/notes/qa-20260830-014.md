# QA — 014 独立复核

日期: 2026-08-30
角色: QA（`docs/roles/QA.md`）。brief: `docs/briefs/2026-08-30-QA-014.md`
对应: ENG commit `d7e2ae1`（014：live_one 默认 False、`impulse_wait_rest_min=100`、live wait_fade）
模型: claude-sonnet-5-thinking-high

## 独立测试

```
python3 -m unittest tests.test_impulse_wait tests.test_live_one tests.test_strategy tests.test_engine
```

结果: **102 passed, 0 failed**（本地独立跑，与 ENG 笔记一致，非转述）。

## Diff 核对（`git show d7e2ae1` + 当前 `btchour/` 三文件）

| 文件 | 核对 |
|---|---|
| `config.py` | `impulse_wait_rest_min` 0.0→100.0，`live_one` True→False，env 默认同步；HEAD 与 d7e2ae1 一致，之后无人再改 |
| `strategy.py` | `coupon_rest_ready`：`rest_min>0` 时需同向 `|impulse|≥rest_min`；`rest_min<=0` 保留旧语义（env 覆盖用）。边界 `-100`/`100` 单测覆盖 |
| `engine.py` | `_wait_cancel_reason` 新增 `wait_fade` 分支，嵌在 `settings.live_one and _is_live_one(row)`，且外层已限定 `play=="impulse_wait"`（`lock_wait` 不受影响）。撤单走既有 `_cancel_working`（`exchange_index` 逻辑未改） |

未发现与 ENG 笔记描述不符之处；未发现新增代码路径破坏既有测试。

## QA.md 固定回归（逐项过）

| 项 | 结果 |
|---|---|
| `test_does_not_hang_no_on_a_flat_atm_forty_two` | pass，逻辑未受影响（走 `impulse=0` 分支） |
| live_one 已有 resting 不挂第二张 | `test_places_one_live_rest_and_refuses_a_second` / `test_leftover_live_blocks_a_second_live_one` pass |
| 撤单带 `exchange_index=2` | `wait_fade` 复用既有 `_cancel_working`（engine.py:489–531），未新开撤单路径；`live_rest_one` 未设响应内 `exchange_index` 时回退 `CRYPTO_EXCHANGE_INDEX`（=2）不变 |
| 不吃 taker / 成交仍要 $100 | `wait_book_crossed(..., min_impulse=settings.impulse_min)` 未改，`impulse_min` 仍 100，与新 `rest_min` 是两个独立旋钮 |

## 缺口 / 需 CMD 知悉（非阻断本 diff，但影响「生效」判断）

1. **`wait_fade` 默认配置下不可达**：`live_one` 默认 False 时 `_is_live_one(row)` 永假，该分支跑不到；即便重开 `live_one`，YES 侧因 `impulse_wait_wrong_side` 的判定阈值恰好与 `coupon_rest_ready` 的 `rest_min` 相同（均为 100），YES 永远先撤在 `wait_wrong_side`，`wait_fade` 只对 NO 侧可达。这与 `ops/notes/adversarial-20260830-014.md` 第 5 点「wait_fade 在默认配置下也不可达」独立交叉核实一致，非 QA 新发现，仅确认属实。
2. **该分支未被 `replay.py` 覆盖**：`rg wait_fade|refresh_working` 在 `btchour/replay.py` 零命中，回放绿不能作为这条规则的验证依据（同 ADV 已指出）。ENG 新增的 `test_cancels_a_live_no_when_the_dump_fades` 是全 mock 客户端单测，能保证分支逻辑正确，但不构成交易所语义验证。
3. 以上两点是「重开 live 前必须重测」的技术依据，不是本次 diff 的 bug；本次 diff 范围内（`live_one=False` 停机 + 跌侧挂门）逻辑与测试自洽。

## 是否补测

未补。走查未发现 ENG 未覆盖的分支：NO 侧 hang 边界（`-100`/`-160`/`-20`/`-12`）、YES 侧未变行为、paper 淡化仍坐、live_one 淡化撤、`load_settings` 默认值均有对应单测。缺口 1/2 属「需真单/replay 验证才能重开」，不是可补的单测缺口（补 mock 单测不会改变结论，ADV 已指出并要求重开前专测/真单验证）。

## 结论

**可合并。**

- 102 passed 独立复现，diff 与笔记一致，四条必测项全部命中，QA 固定回归清单无破坏。
- 附带条件：`live_one` 仍应保持默认 False（当前即是）；`wait_fade` 分支在重开 live 前须按 ADV 要求另行验证，不得以本次回放/单测绿为据（该点已由 ARCH 草稿 `ops/notes/adr-draft-20260830-014.md` 标记「待用户」，非本 diff 阻断项）。
